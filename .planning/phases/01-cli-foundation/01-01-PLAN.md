---
phase: 01-cli-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - tsconfig.json
  - tsup.config.ts
  - vitest.config.ts
  - .gitignore
  - .gitattributes
  - .npmrc
  - src/cli.ts
  - README.md
autonomous: true

must_haves:
  truths:
    - "Project installs cleanly with `npm install` on Node 20+"
    - "`npm run typecheck` exits 0 against an empty/stub source tree"
    - "`npm run build` produces `dist/cli.js` with `#!/usr/bin/env node` shebang"
    - "`npm test` runs vitest and exits 0 (zero tests is fine)"
    - "`npm link` registers a `smartc` binary that resolves to `dist/cli.js`"
  artifacts:
    - path: "package.json"
      provides: "Project manifest with type:module, bin.smartc, scripts, deps"
      contains: "\"type\": \"module\""
    - path: "tsconfig.json"
      provides: "TypeScript config — NodeNext, strict, ES2023"
      contains: "\"module\": \"NodeNext\""
    - path: "tsup.config.ts"
      provides: "Bundler config — single ESM bundle with shebang banner"
      contains: "#!/usr/bin/env node"
    - path: "vitest.config.ts"
      provides: "Test runner config"
    - path: ".gitattributes"
      provides: "Cross-platform LF enforcement for shebang safety"
      contains: "eol=lf"
    - path: ".gitignore"
      provides: "node_modules, dist, coverage exclusion"
    - path: "src/cli.ts"
      provides: "Stub entry — prints 'smartc bootstrap ok' so build verifies end-to-end"
      min_lines: 3
  key_links:
    - from: "package.json"
      to: "dist/cli.js"
      via: "bin field"
      pattern: "\"smartc\":\\s*\"\\./dist/cli\\.js\""
    - from: "tsup.config.ts"
      to: "src/cli.ts"
      via: "entry"
      pattern: "entry.*cli.*src/cli\\.ts"
---

<objective>
Scaffold the TypeScript/Node ESM project from scratch. Install the standard stack identified in RESEARCH.md (commander, @clack/prompts, picocolors, cli-table3, vitest, tsup, tsx, typescript, @types/node). Wire `package.json` `bin.smartc` so `npm link` produces a working binary. Configure tsconfig, tsup, and vitest. Add a minimal `src/cli.ts` stub that prints a single line so the build is end-to-end verifiable on its own — later plans replace it.

Purpose: Every subsequent plan in Phase 1 (libs, registry, commander wiring) needs the project to exist. This plan is pure scaffolding — no business logic, no commander, no abstractions. Just the runway.

Output: Installable, buildable, testable TypeScript ESM project ready for code.
</objective>

<execution_context>
@C:\Users\chinm\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\chinm\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-cli-foundation/01-CONTEXT.md
@.planning/phases/01-cli-foundation/01-RESEARCH.md

# Codebase note
Repo currently contains only `.planning/`, `CLAUDE.md`, and a stub `beginning.py` (Python starter file, ignore — it is NOT a Python project signal). No package.json, no src/, no tests. You are scaffolding from zero.

# Stack pins (from RESEARCH.md - install latest minors of these majors)
- commander ^14.0.3
- @clack/prompts ^0.11
- picocolors ^1.1
- cli-table3 ^0.6.5
- typescript ^5.6
- tsup ^8
- tsx ^4
- vitest ^2 OR ^3 (use latest — both work)
- @types/node ^22

# Pitfalls to avoid (from RESEARCH.md)
- ESM `__dirname` undefined — use `fileURLToPath(import.meta.url)` or `import.meta.dirname` (Node 21.2+) when needed
- NodeNext `.js` extensions — import `./bar.js` even though file is `bar.ts`
- Windows CRLF + shebang — `.gitattributes` with `* text=auto eol=lf` is mandatory
- `dts: false` in tsup (no .d.ts emit needed — this is an app, not a lib)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize package.json and install dependencies</name>
  <files>package.json, package-lock.json, .gitignore, .gitattributes, .npmrc</files>
  <action>
    Create `package.json` with the exact shape from RESEARCH.md:

    ```json
    {
      "name": "smartc",
      "version": "0.1.0",
      "description": "Generate compile-verified smart contracts from a wizard.",
      "type": "module",
      "bin": { "smartc": "./dist/cli.js" },
      "files": ["dist"],
      "engines": { "node": ">=20" },
      "scripts": {
        "dev": "tsx src/cli.ts",
        "build": "tsup",
        "test": "vitest run",
        "test:watch": "vitest",
        "typecheck": "tsc --noEmit"
      }
    }
    ```

    Then install deps in two groups:

    Runtime: `npm install commander@^14 @clack/prompts@^0.11 picocolors@^1 cli-table3@^0.6.5`
    Dev: `npm install -D typescript@^5.6 tsup@^8 tsx@^4 vitest@latest @types/node@^22`

    Create `.gitignore` with:
    ```
    node_modules
    dist
    coverage
    *.log
    .DS_Store
    ```

    Create `.gitattributes` with (load-bearing — Windows CRLF would break the shebang in `dist/cli.js`):
    ```
    * text=auto eol=lf
    ```

    Create `.npmrc` with:
    ```
    save-exact=false
    engine-strict=true
    ```

    DO NOT delete or modify `beginning.py` — it's an unrelated user starter file per project memory.
  </action>
  <verify>
    Run `node --version` (must be >=20). Run `npm install` again — exits 0 with no warnings about peer deps. Run `cat package.json | node -e "const p=JSON.parse(require('fs').readFileSync(0,'utf8')); if(p.type!=='module'||!p.bin?.smartc) process.exit(1)"` — exits 0.
  </verify>
  <done>
    `package.json` exists with `type: module`, `bin.smartc`, all 9 scripts/deps listed. `node_modules/` populated. `.gitignore`, `.gitattributes`, `.npmrc` exist with the exact content above.
  </done>
</task>

<task type="auto">
  <name>Task 2: Configure TypeScript, tsup, vitest, and create stub entry</name>
  <files>tsconfig.json, tsup.config.ts, vitest.config.ts, src/cli.ts, README.md</files>
  <action>
    Create `tsconfig.json` using the RESEARCH.md recommendation verbatim:

    ```json
    {
      "compilerOptions": {
        "target": "ESNext",
        "module": "NodeNext",
        "moduleResolution": "NodeNext",
        "esModuleInterop": true,
        "verbatimModuleSyntax": true,
        "strict": true,
        "noUncheckedIndexedAccess": true,
        "skipLibCheck": true,
        "resolveJsonModule": true,
        "declaration": false,
        "outDir": "dist",
        "rootDir": "src",
        "lib": ["ES2023"]
      },
      "include": ["src/**/*"],
      "exclude": ["node_modules", "dist", "tests"]
    }
    ```

    Create `tsup.config.ts` (note: shebang via `banner.js`, NOT a literal shebang line in source — keeps dev-time `tsx src/cli.ts` clean):

    ```ts
    import { defineConfig } from "tsup";
    export default defineConfig({
      entry: { cli: "src/cli.ts" },
      format: ["esm"],
      target: "node20",
      outDir: "dist",
      clean: true,
      sourcemap: true,
      splitting: false,
      dts: false,
      shims: false,
      banner: { js: "#!/usr/bin/env node" },
    });
    ```

    Create `vitest.config.ts`:

    ```ts
    import { defineConfig } from "vitest/config";
    export default defineConfig({
      test: {
        include: ["tests/**/*.spec.ts"],
        environment: "node",
        clearMocks: true,
      },
    });
    ```

    Create `src/cli.ts` as a minimal stub so the build pipeline is end-to-end verifiable BEFORE we layer code on top (Plan 04 replaces this entirely):

    ```ts
    // Phase 1 Plan 01 stub. Replaced by Plan 04 with real commander dispatch.
    process.stdout.write("smartc bootstrap ok\n");
    ```

    Create `README.md` with minimal install/dev/test instructions (3 sections, ~20 lines):
    ```markdown
    # smartc

    Generate compile-verified smart contracts from a wizard.

    Status: in development (Phase 1 — CLI foundation).

    ## Develop

    ```bash
    npm install
    npm run dev -- --help     # run from source
    npm run typecheck
    npm test
    npm run build             # produces dist/cli.js
    npm link                  # makes `smartc` available globally for testing
    ```
    ```
  </action>
  <verify>
    Run all four in sequence — each must exit 0:
    1. `npx tsc --noEmit` — typecheck passes
    2. `npm run build` — produces `dist/cli.js`
    3. `node dist/cli.js` — prints `smartc bootstrap ok`
    4. `head -1 dist/cli.js` — first line is exactly `#!/usr/bin/env node`
    5. `npm test` — vitest runs (zero tests is OK — exit 0)
  </verify>
  <done>
    All five verification commands exit 0. `dist/cli.js` exists with shebang line. Running `node dist/cli.js` prints the bootstrap line. README.md exists.
  </done>
</task>

</tasks>

<verification>
1. `npm install` — clean install, no errors
2. `npm run typecheck` — exit 0
3. `npm run build` — exit 0, `dist/cli.js` created with shebang
4. `node dist/cli.js` — prints "smartc bootstrap ok"
5. `npm test` — exit 0 (vitest finds zero specs, that's fine)
6. `npm link` then `smartc` — prints "smartc bootstrap ok" (proves bin wiring works for CLI-01's local-link scenario)
</verification>

<success_criteria>
- Scaffold supports the next three plans without modification
- Build + typecheck + test all green on Node 20+
- `npm link` produces a working `smartc` binary
- No code beyond a 2-line stub entry — abstractions come later
</success_criteria>

<output>
After completion, create `.planning/phases/01-cli-foundation/01-01-SUMMARY.md`
</output>
