---
phase: 04
plan: 03
type: execute
wave: 1
depends_on:
  - 04-01
files_modified:
  - src/templates/erc1155/index.ts
  - src/templates/erc1155/wizard.ts
  - src/templates/erc1155/generate.ts
  - src/templates/erc1155/opts.ts
  - src/templates/erc1155/validators.ts
  - src/templates/erc1155/filename.ts
  - src/templates/erc1155/README.md
  - tests/templates/erc1155/wizard.spec.ts
  - tests/templates/erc1155/generate.spec.ts
  - tests/templates/erc1155/validators.spec.ts
files_modified_optional: []
autonomous: true
requirements:
  - ERC1155-01
  - ERC1155-02
  - ERC1155-03
  - ERC1155-04
  - ERC1155-05
tags:
  - phase-04
  - wave-1
  - erc1155
  - template
  - multi-token
  - parallel

must_haves:
  truths:
    - "User can call `runWizard({ output })` for ERC-1155 and get back an `Erc1155Opts` covering name + uri + mintable + burnable + supply + pausable + access (conditional)"
    - "URI template prompt accepts the literal `{id}` placeholder; default value is `https://example.com/api/token/{id}.json` (D-09)"
    - "Conditional access-control prompt fires when mintable OR pausable is true, offering Ownable vs AccessControl (ERC1155-05)"
    - "`generate(opts)` returns `{ filename, source }` where filename is contractName→.sol and source is `erc1155.print(...)` with NO post-process (ERC-1155 has no royalty in Phase 4 per CONTEXT D-08)"
    - "Two Ownable-related centralization warnings + one always-on updatableUri warning fire post-prompt (RESEARCH lines 697-715)"
    - "`tests/templates/erc1155/generate.spec.ts` matches both committed fixtures (bare-default, all-flags-on) via `toMatchFileSnapshot`"
    - "`tests/templates/erc1155/validators.spec.ts` covers boundary cases for `isNonEmptyUri` (empty, whitespace-only, whitespace-internal)"
  artifacts:
    - path: "src/templates/erc1155/index.ts"
      provides: "Template plugin barrel — `registerErc1155Template()` registers the template with id='erc1155', chain='evm', status='alpha'"
      exports: ["registerErc1155Template"]
    - path: "src/templates/erc1155/wizard.ts"
      provides: "Interactive wizard — 7 prompts (name, uri, mintable, burnable, supply, pausable, conditional access); 2 Ownable warnings + 1 always-on updatableUri warning"
      exports: ["runWizard"]
    - path: "src/templates/erc1155/generate.ts"
      provides: "Pure transform — `generate(opts)` calls `erc1155.print(mapped)`; NO post-process (D-08)"
      exports: ["generate"]
    - path: "src/templates/erc1155/opts.ts"
      provides: "Full Erc1155Opts type contract + WizardIo + GenerateResult + Erc1155Template"
      exports: ["Erc1155Opts", "WizardIo", "GenerateResult", "Erc1155Template"]
    - path: "src/templates/erc1155/validators.ts"
      provides: "isSolidityIdentifier (clone) + isNonEmptyUri (new)"
      exports: ["isSolidityIdentifier", "isNonEmptyUri"]
    - path: "src/templates/erc1155/filename.ts"
      provides: "Re-export of contractNameToFilename from erc20 (RESEARCH A5)"
      exports: ["contractNameToFilename"]
    - path: "src/templates/erc1155/README.md"
      provides: "Short page documenting the prompt set, opts mapping, updatableUri default rationale"
      min_lines: 20
    - path: "tests/templates/erc1155/wizard.spec.ts"
      provides: "Mocked @clack/prompts; happy paths + cancel-at-each-prompt cases + warning assertions"
      min_lines: 150
    - path: "tests/templates/erc1155/generate.spec.ts"
      provides: "Real wizard + toMatchFileSnapshot against both fixtures + per-flag assertions"
      min_lines: 60
    - path: "tests/templates/erc1155/validators.spec.ts"
      provides: "Boundary tests for isNonEmptyUri (empty, whitespace-only, internal whitespace, valid)"
      min_lines: 30
  key_links:
    - from: "src/templates/erc1155/wizard.ts"
      to: "src/templates/erc1155/validators.ts"
      via: "validate: isSolidityIdentifier / validate: isNonEmptyUri"
      pattern: "validate: is(SolidityIdentifier|NonEmptyUri)"
    - from: "src/templates/erc1155/index.ts"
      to: "src/registry/index.ts"
      via: "register(tpl as unknown as Template) with idempotency guard"
      pattern: "register\\(tpl as unknown as Template\\)"
    - from: "src/templates/erc1155/generate.ts"
      to: "@openzeppelin/wizard erc1155.print"
      via: "1:1 opts mapping; updatableUri:true wizard default passed verbatim"
      pattern: "erc1155\\.print\\("
    - from: "tests/templates/erc1155/generate.spec.ts"
      to: "tests/fixtures/erc1155/*.sol"
      via: "toMatchFileSnapshot relative paths"
      pattern: "toMatchFileSnapshot\\(\"\\.\\./\\.\\./fixtures/erc1155/"
---

<objective>
Ship the complete ERC-1155 multi-token template plugin under `src/templates/erc1155/`: barrel + wizard + generate + opts + validators + filename + README, plus the three test specs (wizard, generate, validators). Simpler than ERC-721 — no royalty post-process (CONTEXT D-08).

Purpose: Second proof of the additive-only plugin model — Phase 4's second new template lands with zero changes to `src/compiler/`, `src/registry/`, `src/lib/`, or `src/commands/create.ts`. Wires up requirements ERC1155-01 through ERC1155-05.

Runs in parallel with plan 04-02 (ERC-721) under separate worktree agents. The two plans touch DISJOINT file sets — no merge conflicts possible.

Output: Full ERC-1155 plugin with snapshot + per-flag + cancel coverage. The boot wiring in `src/cli.ts` and the integration test fixture rows land in plan 04-04.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-erc-721-and-erc-1155-templates/04-CONTEXT.md
@.planning/phases/04-erc-721-and-erc-1155-templates/04-RESEARCH.md
@.planning/phases/04-erc-721-and-erc-1155-templates/04-PATTERNS.md
@.planning/phases/04-erc-721-and-erc-1155-templates/04-01-PLAN.md
@src/templates/erc20/index.ts
@src/templates/erc20/wizard.ts
@src/templates/erc20/generate.ts
@src/templates/erc20/opts.ts
@src/templates/erc20/validators.ts
@src/templates/erc20/filename.ts
@src/registry/index.ts
@src/registry/types.ts
@src/lib/errors.ts
@src/lib/output.ts
@tests/templates/erc20/wizard.spec.ts
@tests/templates/erc20/generate.spec.ts
@tests/templates/erc20/validators.spec.ts

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/registry/index.ts:
- `export function register(tpl: Template): void;` — throws on duplicate id
- `export function get(id: string): Template | undefined;`

From src/registry/types.ts:
- `interface Template<TOpts = unknown> { id, name, chain, status, description, runWizard?, generate? }`

From src/templates/erc20/wizard.ts (clone analog — lines 1-161):
- `cancelGuard<T>(answer, promptName): T` inline helper — clone verbatim (swap fix-line template id to 'erc1155')
- The conditional access prompt block at lines 121-141 — clone verbatim per CONTEXT D-10

From @openzeppelin/wizard@0.10.8:
- `import { erc1155 } from "@openzeppelin/wizard";`
- `erc1155.print(opts: ERC1155Options): string` — name, uri, burnable, pausable, mintable, supply, updatableUri, access (NO royalty surface — D-08)
- Wizard defaults `updatableUri:true` (RESEARCH Pitfall 3) — pass through verbatim

From src/templates/erc20/filename.ts:
- `export function contractNameToFilename(contractName: string): string;` — pure; re-export per RESEARCH A5

From src/templates/erc20/validators.ts:
- `SOLIDITY_IDENTIFIER = /^[A-Za-z_][A-Za-z0-9_]{0,63}$/`
- `isSolidityIdentifier(v): undefined | "..."` — clone this regex + function body (not re-export — validators are template-owned)

From src/lib/output.ts:
- `output.warn(msg)` — always-on critical channel
- `output.explain(msg)`, `output.reference(label, url)` — newbie-only
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire ERC-1155 type contracts + validators + filename + barrel</name>
  <files>src/templates/erc1155/opts.ts, src/templates/erc1155/validators.ts, src/templates/erc1155/filename.ts, src/templates/erc1155/index.ts</files>
  <read_first>
    - .planning/phases/04-erc-721-and-erc-1155-templates/04-PATTERNS.md (§`src/templates/erc1155/index.ts`, §`src/templates/erc1155/opts.ts`, §`src/templates/erc1155/validators.ts`, §`src/templates/{erc721,erc1155}/filename.ts`)
    - .planning/phases/04-erc-721-and-erc-1155-templates/04-RESEARCH.md (§ERC-1155 opts.ts lines 1006-1022; §Validators ERC-1155 lines 759-768)
    - src/templates/erc20/index.ts (analog — clone shape; swap 4 identifiers)
    - src/templates/erc20/opts.ts (analog — clone shape; drop symbol/premint, add uri/supply)
    - src/templates/erc20/validators.ts (analog — clone isSolidityIdentifier; add isNonEmptyUri)
    - src/templates/erc20/filename.ts (source of re-export)
  </read_first>
  <behavior>
    - `src/templates/erc1155/opts.ts` exports `Erc1155Opts`, `WizardIo`, `GenerateResult`, `Erc1155Template`. Per RESEARCH lines 1006-1022.
    - `src/templates/erc1155/validators.ts` exports `isSolidityIdentifier` (cloned) and `isNonEmptyUri` (new). Each satisfies `(v: string | undefined) => string | undefined`.
    - `src/templates/erc1155/filename.ts` re-exports `contractNameToFilename` from `../erc20/filename.js`.
    - `src/templates/erc1155/index.ts` exports `registerErc1155Template()` which registers the template with id='erc1155', name='ERC-1155 Multi-Token', chain='evm', status='alpha', description per CONTEXT D-01/D-08. Idempotent via `if (get("erc1155")) return;`.
  </behavior>
  <action>
    Step A — `src/templates/erc1155/opts.ts`: New file. Per RESEARCH lines 1006-1022 + PATTERNS §opts.ts:
      - Imports: `import type { Output } from "../../lib/output.js"; import type { Template } from "../../registry/types.js";`
      - `Erc1155Opts` with readonly fields: `name: string`, `uri: string`, `mintable: boolean`, `burnable: boolean`, `supply: boolean`, `pausable: boolean`, `access: false | "ownable" | "roles"` (NO "managed", matching Phase 2 precedent).
      - `WizardIo { readonly output: Output }`
      - `GenerateResult { readonly filename: string; readonly source: string }`
      - `Erc1155Template extends Template<Erc1155Opts>` with required `runWizard` and `generate` narrowings.
      - Header comment references CONTEXT D-08 (no royalty in Phase 4 for ERC-1155) + D-09 (URI placeholder format).
      - NO `updatableUri` field — wizard default `true` is passed in generate.ts via a literal, not surfaced as user input (RESEARCH Pitfall 3 — wizard default matches wizard.openzeppelin.com).

    Step B — `src/templates/erc1155/validators.ts`: Clone `SOLIDITY_IDENTIFIER` regex + `isSolidityIdentifier` body from `src/templates/erc20/validators.ts:15+24-30` verbatim. Add `isNonEmptyUri` per RESEARCH lines 763-768:
      - Reject empty/undefined → return "URI template is required (e.g. https://example.com/api/token/{id}.json)."
      - Reject internal whitespace (use `/\s/.test(v)`) → return "URI must not contain whitespace."
      - Else return undefined.
      Error message strings are byte-locked — paste exactly per RESEARCH lines 763-768.

    Step C — `src/templates/erc1155/filename.ts`: Single line re-export:
      `export { contractNameToFilename } from "../erc20/filename.js";`
      Add a 3-line JSDoc / file header citing RESEARCH A5.

    Step D — `src/templates/erc1155/index.ts`: Clone `src/templates/erc20/index.ts` verbatim (analog). Swap four identifiers:
      - `registerErc20Template` → `registerErc1155Template`
      - `Erc20Opts` → `Erc1155Opts`
      - Imports of `./wizard.js`, `./generate.js`, `./opts.js` stay (correct relative paths).
      - The literal template metadata: `id: "erc1155"`, `name: "ERC-1155 Multi-Token"`, `chain: "evm"`, `status: "alpha"`, `description: "Multi-token (ERC-1155) on EVM chains. Opt-in Mintable/Burnable/Supply/Pausable."` (CONTEXT D-01 / D-08 — no royalty mention).
      Preserve the idempotency guard (`if (get("erc1155")) return;`) and the variance-cast comment.
  </action>
  <acceptance_criteria>
    - `src/templates/erc1155/opts.ts` exports all 4 type names (`Erc1155Opts`, `WizardIo`, `GenerateResult`, `Erc1155Template`).
    - `src/templates/erc1155/validators.ts` exports 2 functions; each error message matches the RESEARCH-locked string byte-for-byte.
    - `src/templates/erc1155/filename.ts` is exactly one re-export statement plus a header comment.
    - `src/templates/erc1155/index.ts` registers `id: "erc1155"`, `chain: "evm"`, `status: "alpha"` with the locked description string.
    - `npm run typecheck` clean.
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <done>Type contracts + validators + filename re-export + barrel exist; typecheck clean; downstream tasks can import.</done>
</task>

<task type="auto">
  <name>Task 2: Wizard + generate — 7 prompts, 3 warnings (2 conditional + 1 always-on)</name>
  <files>src/templates/erc1155/wizard.ts, src/templates/erc1155/generate.ts</files>
  <read_first>
    - .planning/phases/04-erc-721-and-erc-1155-templates/04-PATTERNS.md (§`src/templates/erc1155/wizard.ts`, §`src/templates/erc1155/generate.ts` (RESEARCH §Pattern 4))
    - .planning/phases/04-erc-721-and-erc-1155-templates/04-RESEARCH.md (§ERC-1155 (7 prompts) table lines 658-667; §Non-negotiable centralization warnings ERC-1155 block lines 697-715; §Pattern 4 lines 494-515)
    - src/templates/erc20/wizard.ts (lines 1-161 — analog; clone cancelGuard inline + conditional access prompt verbatim per CONTEXT D-10)
    - src/templates/erc20/generate.ts (lines 1-49 — analog)
    - src/templates/erc1155/opts.ts (type contract from Task 1)
    - src/templates/erc1155/validators.ts (validators from Task 1)
  </read_first>
  <behavior>
    - `runWizard(io: WizardIo)` returns `Promise<Erc1155Opts>`. Prompt order (CONTEXT Discretion + RESEARCH §ERC-1155): name → uri → mintable → burnable → supply → pausable → (if mintable OR pausable) access.
    - Newbie preamble: `output.explain("ERC-1155 is the multi-token standard...")` + `output.reference("EIP-1155", "https://eips.ethereum.org/EIPS/eip-1155")` + `output.reference("OpenZeppelin ERC1155 docs", "https://docs.openzeppelin.com/contracts/5.x/erc1155")`.
    - Per-prompt `output.explain` lines per RESEARCH §ERC-1155 table newbie-hook column.
    - `cancelGuard` is INLINE (per CONTEXT D-10).
    - Conditional access prompt: same UI copy as `src/templates/erc20/wizard.ts:121-141` (CONTEXT D-10 — clone verbatim).
    - Three centralization warnings post-prompt (RESEARCH lines 697-715):
      1. (conditional) `mintable && access === "ownable"` → "Mintable + Ownable: a single key can mint unlimited quantities of any token id. Consider AccessControl (multi-role) or transferring ownership to a multisig before deploy."
      2. (conditional) `pausable && access === "ownable"` → "Pausable + Ownable: a single key can halt all transfers across every token id. Consider AccessControl (multi-role) or a multisig owner."
      3. (always-on, fires every time the wizard completes) "ERC-1155 default-URI setter is owner-controlled (wizard default `updatableUri:true`). The contract owner can change the URI template at any time. Use a multisig owner or freeze ownership before launch if metadata must be immutable."
    - `generate(opts)` returns `{ filename: contractNameToFilename(opts.name), source: erc1155.print({name, uri, mintable, burnable, supply, pausable, updatableUri: true, access}) }`. NO `info` field (wizard defaults apply). NO post-process (D-08).
  </behavior>
  <action>
    Step A — `src/templates/erc1155/wizard.ts`: New file. Use RESEARCH §ERC-1155 (7 prompts) table verbatim. Adapt:
      - Imports: `text`, `select`, `confirm`, `isCancel` from "@clack/prompts"; `CliError`, `ERR_WIZARD_CANCEL` from `../../lib/errors.js`; `isSolidityIdentifier`, `isNonEmptyUri` from `./validators.js`; types `Erc1155Opts`, `WizardIo` from `./opts.js`.
      - Clone the `cancelGuard` inline helper from src/templates/erc20/wizard.ts:20-31; swap the fix-line template id to `'smartc create --template erc1155'`.
      - Preamble: 1 `output.explain` + 2 `output.reference` per RESEARCH lines 661.
      - Prompt 1 (name): text with placeholder "MyMulti", defaultValue "MyMulti", validate isSolidityIdentifier; explain "The on-chain contract name. Letters, digits, underscores; max 64 chars. NOT the URI."
      - Prompt 2 (uri): text with placeholder "https://example.com/api/token/{id}.json", defaultValue "https://example.com/api/token/{id}.json", validate isNonEmptyUri; explain "Metadata template. Use the literal `{id}` placeholder — clients substitute the hex-padded token id at lookup time."
      - Prompt 3 (mintable): confirm initialValue false; explain "Mintable means new token IDs / quantities can be minted post-deploy."
      - Prompt 4 (burnable): confirm initialValue false; explain "Burnable lets each holder destroy their own balances."
      - Prompt 5 (supply): confirm initialValue false; explain "Supply tracking adds totalSupply(id) + totalSupply() so clients can read circulating amounts per id. Costs gas on mint/burn."
      - Prompt 6 (pausable): confirm initialValue false; explain "Pausable lets an authorized account freeze all transfers."
      - Conditional access prompt: copy `src/templates/erc20/wizard.ts:121-141` verbatim per CONTEXT D-10.
      - Three centralization warnings per RESEARCH lines 697-715 (two conditional + one always-on). Byte-locked copy — paste exactly.
      - Return statement: `return { name, uri, mintable, burnable, supply, pausable, access };`

    Step B — `src/templates/erc1155/generate.ts`: New file. Use RESEARCH §Pattern 4 lines 494-515 verbatim. Imports:
      `import { erc1155 } from "@openzeppelin/wizard";`
      `import { contractNameToFilename } from "./filename.js";`
      `import type { Erc1155Opts, GenerateResult } from "./opts.js";`

      Body: call `erc1155.print({ name, uri, mintable, burnable, supply, pausable, updatableUri: true, access })` (literal `updatableUri: true` — wizard default; matches wizard.openzeppelin.com per RESEARCH Pitfall 3). Return `{ filename: contractNameToFilename(opts.name), source }`. No post-process. No conditional. No royalty.

    Header comment for generate.ts: cite CONTEXT D-01 + D-08 + D-09 (no royalty in Phase 4; URI template format; wizard pass-through).

    DO NOT pass `info` to `erc1155.print` — wizard defaults apply.
    DO NOT add a royalty branch — D-08 explicitly defers EIP-2981 on ERC-1155 to v2.

    Per CONTEXT D-10: conditional access prompt is duplicated verbatim from erc20/wizard.ts. No shared module extraction.

    Per CONTEXT D-12: this plan does NOT modify `src/commands/create.ts`, `src/compiler/*`, `src/registry/*`, or `src/lib/*`. All new files live under `src/templates/erc1155/`. (Boot wiring in `src/cli.ts` lands in plan 04-04.)
  </action>
  <acceptance_criteria>
    - `src/templates/erc1155/wizard.ts` exports `runWizard` and imports cancelGuard inline.
    - `src/templates/erc1155/generate.ts` exports `generate`; calls `erc1155.print(...)` with `updatableUri: true` as a literal.
    - Both files type-check under TS strict + noUncheckedIndexedAccess.
    - The wizard's prompt order matches RESEARCH §ERC-1155 table exactly: name → uri → mintable → burnable → supply → pausable → (if mintable||pausable) access.
    - No royalty references anywhere in `src/templates/erc1155/`.
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <done>wizard.ts + generate.ts exist and typecheck clean; ERC-1155 source generates via wizard with no post-process.</done>
</task>

<task type="auto">
  <name>Task 3: Tests — wizard.spec, generate.spec (2 golden snapshots), validators.spec; plus per-template README</name>
  <files>tests/templates/erc1155/wizard.spec.ts, tests/templates/erc1155/generate.spec.ts, tests/templates/erc1155/validators.spec.ts, src/templates/erc1155/README.md</files>
  <read_first>
    - .planning/phases/04-erc-721-and-erc-1155-templates/04-PATTERNS.md (§`tests/templates/erc1155/*.spec.ts` lines ~791-799; §Pattern S3 — mock pattern; §Pattern S4 — makeMockOutput; §Pattern S5 — toMatchFileSnapshot await)
    - .planning/phases/04-erc-721-and-erc-1155-templates/04-RESEARCH.md (§Wave 0 Gaps lines 1190-1197 — fixture filenames; §Non-negotiable centralization warnings ERC-1155 lines 697-715 — for warning assertions)
    - tests/templates/erc20/wizard.spec.ts (analog — clone mock setup, makeMockOutput, beforeEach resets, happy-path + cancel cases)
    - tests/templates/erc20/generate.spec.ts (analog — clone snapshot+per-flag pattern)
    - tests/templates/erc20/validators.spec.ts (analog — clone boundary-table shape)
    - tests/fixtures/erc1155/bare-default.sol (committed in plan 04-01 — toMatchFileSnapshot target)
    - tests/fixtures/erc1155/all-flags-on.sol (committed in plan 04-01)
  </read_first>
  <behavior>
    - `tests/templates/erc1155/wizard.spec.ts` covers ERC1155-01..05:
      - Happy path A (no flags): primes 2 text answers + 4 confirm:false answers; asserts return value is the bare-default Erc1155Opts (access: false); asserts updatableUri warning fires; asserts access prompt was NOT called.
      - Happy path B (mintable=true, access=ownable): primes name+uri text, confirm:true(mintable) + false x3; selects "ownable"; asserts return value; asserts TWO warn calls (Mintable+Ownable + updatableUri always-on).
      - Happy path C (all flags + roles): all true; access="roles"; asserts ONLY the updatableUri always-on warning fires (no Ownable triggers).
      - Cancel-at-each-prompt: 7 separate `it()` blocks (one per prompt index) where the relevant @clack mock returns a symbol AND `isCancelMock` returns true; assert `CliError` with code `E_WIZARD_CANCEL` is thrown.
    - `tests/templates/erc1155/generate.spec.ts` covers ERC1155-01..04:
      - Two snapshot tests against the committed fixtures via `await expect(source).toMatchFileSnapshot("../../fixtures/erc1155/<name>.sol")`.
      - Per-flag assertions (analog to erc20/generate.spec.ts:45+): burnable=true → `ERC1155Burnable`; mintable=true+ownable → `Ownable` + `mint(`; mintable=true+roles → `AccessControl` + `MINTER_ROLE`; supply=true → `ERC1155Supply`; pausable=true → `Pausable`.
    - `tests/templates/erc1155/validators.spec.ts` covers `isNonEmptyUri`:
      - "" → defined (required msg).
      - undefined → defined (required msg).
      - "   " (whitespace only) → defined.
      - "https://example.com/api/token/{id}.json" → undefined.
      - "https://x x/" (internal whitespace) → defined (URI must not contain whitespace).
  </behavior>
  <action>
    Step A — `tests/templates/erc1155/wizard.spec.ts`: Clone `tests/templates/erc20/wizard.spec.ts` lines 1-42 verbatim for the mock setup, dynamic SUT import, mock typing, makeMockOutput, beforeEach reset. Swap the SUT import: `const { runWizard } = await import("../../../src/templates/erc1155/wizard.js");`.

    Add happy-path tests A, B, C per §behavior. Each test:
      - Calls `textMock.mockResolvedValueOnce("...")` for the 2 text prompts.
      - Calls `confirmMock.mockResolvedValueOnce(true|false)` for the 4 confirm prompts (mintable, burnable, supply, pausable).
      - Calls `selectMock.mockResolvedValueOnce("...")` when access is expected.
      - Invokes `const opts = await runWizard({ output })` with `output = makeMockOutput()`.
      - Asserts the returned shape.
      - Asserts `output.warn` call count + copy (remember the always-on updatableUri warning fires in EVERY happy path).

    Add 7 cancel-at-each-prompt tests via `it.each(...)` parametrized on prompt index (0..6). For each index, prime the prior prompts with valid values, then mock the relevant @clack function to resolve a Symbol AND mock `isCancelMock.mockReturnValue(true)` for that one call; assert `await expect(runWizard(...)).rejects.toThrow(CliError)` and that the thrown error has `code: "E_WIZARD_CANCEL"`. promptName labels: "contract name", "uri", "mintable", "burnable", "supply", "pausable", "access control".

    Step B — `tests/templates/erc1155/generate.spec.ts`: Clone `tests/templates/erc20/generate.spec.ts` lines 1-43 verbatim. Swap the SUT import: `import { generate } from "../../../src/templates/erc1155/generate.js";`. Replace input objects with the two full Erc1155Opts shapes:
      - bare-default: `{ name: "MyMulti", uri: "https://example.com/api/token/{id}.json", mintable: false, burnable: false, supply: false, pausable: false, access: false }`.
      - all-flags-on: `{ name: "MyMulti", uri: "https://example.com/api/token/{id}.json", mintable: true, burnable: true, supply: true, pausable: true, access: "roles" }`.

    Snapshot paths: `../../fixtures/erc1155/bare-default.sol` and `../../fixtures/erc1155/all-flags-on.sol`. Use `await expect(source).toMatchFileSnapshot(path)`.

    After the two snapshot tests, add a `describe("erc1155 generate — per-flag assertions")` block with the per-flag tests from §behavior. Each test builds a minimal Erc1155Opts with one flag set, calls `generate(opts)`, asserts the source string contains the OZ-specific symbol (e.g., `ERC1155Burnable`, `ERC1155Supply`, `Pausable`).

    Step C — `tests/templates/erc1155/validators.spec.ts`: Clone the boundary-table shape from `tests/templates/erc20/validators.spec.ts`. Add one `describe("isNonEmptyUri", ...)` block covering the 5 cases in §behavior. Optionally include a slim `describe("isSolidityIdentifier (clone)", ...)` to confirm the cloned validator behaves identically to the erc20 analog on 2-3 sample cases — this catches regressions if the clone accidentally diverges.

    Step D — `src/templates/erc1155/README.md`: One short page documenting:
      - The wizard prompt set (list the 7 prompts with their copy).
      - The opts mapping (`Erc1155Opts` fields → `erc1155.print()` ERC1155Options fields, including the `updatableUri: true` literal pass).
      - The wizard's `updatableUri: true` default rationale (link to RESEARCH Pitfall 3); document why the bare-default fixture includes Ownable + setURI.
      - No royalty in Phase 4 — link to REQUIREMENTS.md §v2 ERC1155-V2-01.
      - Filename derivation reuses erc20's contractNameToFilename via re-export.
      Target ~30-50 lines. Terse.

    DO NOT add an integration test for compile-verify in this plan — that lives in plan 04-04.

    DO NOT update `tests/registry.spec.ts` here — the `it.skip` stub from plan 04-01 stays skipped; plan 04-04 flips it after this plan + 04-02 land.
  </action>
  <acceptance_criteria>
    - `tests/templates/erc1155/wizard.spec.ts` exists with at least 3 happy-path tests + 7 cancel tests; all green.
    - `tests/templates/erc1155/generate.spec.ts` exists with 2 snapshot tests (matching plan-04-01 fixtures byte-for-byte) + at least 5 per-flag tests; all green.
    - `tests/templates/erc1155/validators.spec.ts` exists with at least 1 describe block (`isNonEmptyUri`) covering all boundaries; all green.
    - `src/templates/erc1155/README.md` exists and is at least 20 lines.
    - `npx vitest run tests/templates/erc1155/` reports all green.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/templates/erc1155/</automated>
  </verify>
  <done>ERC-1155 template ships with full test coverage; snapshot match against plan-04-01 fixtures; README documents the plugin surface.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user input → wizard validators | Contract name (Solidity identifier), URI template (non-empty, no whitespace) enforced via `validate:` callbacks |
| wizard opts → erc1155.print | Pass-through 1:1; updatableUri:true is a hardcoded literal in generate.ts (not user input) |
| smartc → @openzeppelin/wizard | Trusted, pinned; no new install |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03-01 | Tampering | wizard `text` prompts (name, uri) | mitigate | `validate: isSolidityIdentifier / isNonEmptyUri` callbacks reject malformed input at prompt time; locked error copy per RESEARCH lines 763-768 |
| T-04-03-02 | Tampering | URI placeholder injection | accept | URI is opaque to the contract — emitted as a string literal into the `ERC1155(<uri>)` constructor arg; the `{id}` placeholder is semantic only (per ERC-1155 spec) and not executed |
| T-04-03-03 | Repudiation | Mintable+Ownable centralization | mitigate | `output.warn` fires the locked Mintable+Ownable copy when conditions hit; Phase 2 contract — always-on critical channel |
| T-04-03-04 | Repudiation | Pausable+Ownable centralization | mitigate | `output.warn` fires the locked Pausable+Ownable copy |
| T-04-03-05 | Elevation of Privilege | updatableUri (wizard default true) — owner can change URI | mitigate | Always-on warning fires every time the wizard completes: "ERC-1155 default-URI setter is owner-controlled..." (RESEARCH lines 710-715) |
| T-04-03-06 | Denial of Service | runWizard hang on Ctrl+C | mitigate | `cancelGuard` wraps every @clack call; throws `CliError(E_WIZARD_CANCEL, exitCode:130)` on isCancel |
| T-04-03-SC | Tampering | No new package installs | accept | Plan reuses @openzeppelin/wizard@0.10.8 — Approved in Phase 4 RESEARCH §Package Legitimacy Audit |
</threat_model>

<verification>
- `src/templates/erc1155/{index,wizard,generate,opts,validators,filename}.ts` exist and pass typecheck.
- `src/templates/erc1155/README.md` exists.
- `tests/templates/erc1155/{wizard,generate,validators}.spec.ts` exist; `npx vitest run tests/templates/erc1155/` is all green.
- Two golden snapshots in `tests/templates/erc1155/generate.spec.ts` match the committed fixtures from plan 04-01 byte-for-byte.
- Wizard happy paths return Erc1155Opts; cancel paths throw `CliError(E_WIZARD_CANCEL)`; the always-on updatableUri warning fires in every happy path; Ownable warnings fire conditionally.
- `src/commands/create.ts`, `src/compiler/*`, `src/registry/*`, `src/lib/*`, `src/cli.ts` are unmodified (additive-only check: `git diff --name-only` shows only `src/templates/erc1155/` + `tests/templates/erc1155/`).
- No royalty references in `src/templates/erc1155/` (D-08).
</verification>

<success_criteria>
- ERC1155-01 done: wizard collects name + uri; generate emits correct Solidity for each (validated by bare-default snapshot).
- ERC1155-02 done: per-flag assertions confirm Mintable / Burnable wire correctly into `erc1155.print()`.
- ERC1155-03 done: Supply opt-in propagates; per-flag assertion confirms `ERC1155Supply` parent.
- ERC1155-04 done: Pausable opt-in propagates.
- ERC1155-05 done: conditional access prompt fires when mintable OR pausable; wizard tests cover both `ownable` and `roles` branches.
- Plugin model additive-only check: zero modifications outside `src/templates/erc1155/` + `tests/templates/erc1155/`.
</success_criteria>

<output>
Create `.planning/phases/04-erc-721-and-erc-1155-templates/04-03-SUMMARY.md` when done.
Include: file count + line count per file, snapshot-match status for each of the 2 fixtures, warning-emission test results (especially the always-on updatableUri warning), the wizard prompt order locked in for plan 04-04's E2E test to clone, and a note confirming no files outside `src/templates/erc1155/` + `tests/templates/erc1155/` + `src/templates/erc1155/README.md` were modified.
</output>
