---
phase: 04
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - src/templates/erc721/royalty.ts
  - src/templates/erc721/opts.ts
  - tests/fixtures/erc721/bare-default.sol
  - tests/fixtures/erc721/all-flags-on.sol
  - tests/fixtures/erc721/all-flags-on-with-royalty.sol
  - tests/fixtures/erc1155/bare-default.sol
  - tests/fixtures/erc1155/all-flags-on.sol
  - src/commands/create.ts
  - tests/registry.spec.ts
  - tests/templates/erc721/royalty.spec.ts
files_modified_optional: []
autonomous: true
requirements:
  - ERC721-01
  - ERC721-02
  - ERC721-03
  - ERC721-04
  - ERC1155-01
  - ERC1155-02
  - ERC1155-03
  - ERC1155-04
tags:
  - phase-04
  - wave-0
  - royalty
  - fixtures
  - foundation

must_haves:
  truths:
    - "`injectRoyalty(source, opts)` lands the four EIP-2981 anchors correctly for bare, ownable+mintable, and all-flags+roles wizard outputs (CONTEXT D-04..D-06)"
    - "All five committed fixtures (`erc721/bare-default.sol`, `erc721/all-flags-on.sol`, `erc721/all-flags-on-with-royalty.sol`, `erc1155/bare-default.sol`, `erc1155/all-flags-on.sol`) compile clean under `compileVerify(source, 'evm')` (Phase 3 gate, evmVersion: 'cancun')"
    - "`src/commands/create.ts` E_USAGE fix-copy line lists all three templates: `Re-run with --template <erc20|erc721|erc1155>` (CONTEXT D-14)"
    - "`tests/registry.spec.ts` asserts all three templates register without collision when their three register*Template() functions are called sequentially"
    - "`tests/templates/erc721/royalty.spec.ts` proves each of the three royalty injection cases compiles under the Phase 3 gate (CONTEXT D-17)"
  artifacts:
    - path: "src/templates/erc721/royalty.ts"
      provides: "EIP-2981 royalty post-process — 4-anchor bracket-counting walker; exports injectRoyalty(source, Erc721RoyaltyOpts) -> string"
      exports: ["injectRoyalty"]
      min_lines: 60
    - path: "src/templates/erc721/opts.ts"
      provides: "Erc721RoyaltyOpts type contract — minimum surface needed for royalty.ts; full Erc721Opts arrives in Plan 04-02"
      exports: ["Erc721RoyaltyOpts"]
    - path: "tests/fixtures/erc721/bare-default.sol"
      provides: "Wizard output for erc721.print({name:'MyNFT', symbol:'MNFT'}) — committed LF-encoded"
      contains: "contract MyNFT"
    - path: "tests/fixtures/erc721/all-flags-on.sol"
      provides: "Wizard output for erc721.print with mintable+enumerable+burnable+pausable+uriStorage+access:roles"
      contains: "AccessControl"
    - path: "tests/fixtures/erc721/all-flags-on-with-royalty.sol"
      provides: "all-flags-on piped through injectRoyalty({enabled:true, feeNumerator:250, receiver:'0x0000…'})"
      contains: "ERC2981"
    - path: "tests/fixtures/erc1155/bare-default.sol"
      provides: "Wizard output for erc1155.print({name:'MyMulti', uri:'https://example.com/api/token/{id}.json'}) — includes Ownable+setURI per wizard default updatableUri:true"
      contains: "contract MyMulti"
    - path: "tests/fixtures/erc1155/all-flags-on.sol"
      provides: "Wizard output for erc1155.print with mintable+burnable+supply+pausable+updatableUri+access:roles"
      contains: "AccessControl"
    - path: "src/commands/create.ts"
      provides: "Updated E_USAGE fix-copy line listing all three templates (D-14)"
      contains: "<erc20|erc721|erc1155>"
    - path: "tests/registry.spec.ts"
      provides: "Three-template no-collision registration test"
      contains: "registerErc721Template"
    - path: "tests/templates/erc721/royalty.spec.ts"
      provides: "Unit test for injectRoyalty — 3 anchor scenarios + compile-gate verification per CONTEXT D-17"
      contains: "injectRoyalty"
  key_links:
    - from: "src/templates/erc721/royalty.ts"
      to: "@openzeppelin/contracts/token/common/ERC2981.sol"
      via: "anchor 1 import injection"
      pattern: "import \\{ERC2981\\} from \"@openzeppelin/contracts/token/common/ERC2981\\.sol\""
    - from: "tests/templates/erc721/royalty.spec.ts"
      to: "src/compiler/index.ts (compileVerify)"
      via: "injectRoyalty output piped through Phase 3 gate"
      pattern: "compileVerify\\("
    - from: "tests/fixtures/erc721/all-flags-on-with-royalty.sol"
      to: "supportsInterface override list"
      via: "anchor 4 ERC2981 appended"
      pattern: "override\\([^)]*ERC2981[^)]*\\)"
---

<objective>
Phase 4 Wave 0 — establish all foundation artifacts before the two parallel template plans (04-02 / 04-03) start. This plan ships: (1) the four-anchor `injectRoyalty` bracket-counting walker plus its `Erc721RoyaltyOpts` type contract, (2) all five committed golden fixtures generated from `@openzeppelin/wizard@0.10.8` and validated against the Phase 3 compile gate, (3) the one-line `E_USAGE` copy update in `src/commands/create.ts` (D-14), (4) the registry-spec extension confirming three-template no-collision registration, and (5) the `royalty.spec.ts` unit test proving the post-process is grammar-correct across bare / ownable+mintable / all-flags+roles permutations.

Purpose: De-risk the ONE deviation from Phase 2 D-02 (the royalty post-process) and lock the fixture canaries that downstream snapshot tests will validate against. Without this plan's outputs, plans 04-02 and 04-03 cannot land their generate.spec.ts snapshots.

Output: `src/templates/erc721/royalty.ts` + `src/templates/erc721/opts.ts` (royalty type only) + 5 fixtures + extended `src/commands/create.ts` E_USAGE copy + extended `tests/registry.spec.ts` + new `tests/templates/erc721/royalty.spec.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-erc-721-and-erc-1155-templates/04-CONTEXT.md
@.planning/phases/04-erc-721-and-erc-1155-templates/04-RESEARCH.md
@.planning/phases/04-erc-721-and-erc-1155-templates/04-PATTERNS.md
@src/templates/erc20/generate.ts
@src/templates/erc20/index.ts
@src/lib/errors.ts
@src/compiler/index.ts
@src/commands/create.ts
@tests/registry.spec.ts
@tests/compiler/compile.integration.spec.ts
@tests/fixtures/erc20/bare-default.sol
@tests/fixtures/erc20/all-flags-on.sol

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/compiler/index.ts (Phase 3 seam — DO NOT MODIFY):
- `export async function compileVerify(source: string, chain: "evm" | "solana"): Promise<{ warnings: CompileDiagnostic[] }>`
- evmVersion is set to "cancun" — required for OZ 5.6.1 mcopy opcode.

From src/lib/errors.ts:
- `export const ERR_USAGE = "E_USAGE" as const;`
- `export const ERR_INVALID_INPUT = "E_INVALID_INPUT" as const;` (already shipped per Phase 2)
- `export const ERR_COMPILE_FAILED = "E_COMPILE_FAILED" as const;`
- `class CliError extends Error { code, what, why, fix, exitCode }`

From @openzeppelin/wizard@0.10.8 namespaced API (DO NOT import via `printERC721`):
- `import { erc721, erc1155 } from "@openzeppelin/wizard";`
- `erc721.print(opts: ERC721Options): string` — name, symbol, baseUri, mintable, enumerable, uriStorage, burnable, pausable, incremental, votes, namespacePrefix, access, info
- `erc1155.print(opts: ERC1155Options): string` — name, uri, burnable, pausable, mintable, supply, updatableUri, access, info
- NEITHER has a `royalty` field — that is why this plan ships injectRoyalty.

From src/templates/erc20/index.ts (registry pattern to clone):
- `if (get("erc20")) return;` — idempotency guard
- `register(tpl as unknown as Template)` — boundary cast pattern

From tests/registry.spec.ts (line 88-96 — extend):
- Existing test calls `registerErc20Template()` then asserts list().length === 1 and runWizard/generate are functions.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create royalty.ts (4-anchor bracket-counting walker) + minimal Erc721RoyaltyOpts type</name>
  <files>src/templates/erc721/royalty.ts, src/templates/erc721/opts.ts</files>
  <read_first>
    - .planning/phases/04-erc-721-and-erc-1155-templates/04-RESEARCH.md (§Pattern 3 lines 363-473 — verbatim walker source; §Pitfall 1 lines 576-585 — anti-pattern; §Anchor stability table lines 478-482)
    - .planning/phases/04-erc-721-and-erc-1155-templates/04-PATTERNS.md (§`src/templates/erc721/royalty.ts` — pattern source)
    - .planning/phases/04-erc-721-and-erc-1155-templates/04-CONTEXT.md (D-04, D-05, D-06, D-07)
    - src/templates/erc20/opts.ts (analog for the type-only module shape — clone the comment header)
    - node_modules/@openzeppelin/contracts/token/common/ERC2981.sol (confirm `_setDefaultRoyalty(address receiver, uint96 feeNumerator)` signature exists; do not modify)
  </read_first>
  <behavior>
    - injectRoyalty({enabled:false, ...}) returns source unchanged byte-for-byte (D-06).
    - injectRoyalty({enabled:true, feeNumerator:250, receiver:"0x0…"}) inserts exactly 4 anchors:
      1. `import {ERC2981} from "@openzeppelin/contracts/token/common/ERC2981.sol";` immediately after the LAST `@openzeppelin/contracts/...` import line.
      2. `, ERC2981` appended to the `contract <Name> is <parents>` parent list (before the `{`).
      3. `    _setDefaultRoyalty(<receiver>, <feeNumerator>);\n` inserted before the matching closing brace of the constructor body, located by bracket-counting (NOT regex — Pitfall 1).
      4. `, ERC2981` appended to the existing `function supportsInterface(bytes4 interfaceId) ... override(...)` list IF such an override exists; no-op otherwise.
    - Helper `insertAtConstructorBodyEnd(source, insertion)` walks from `constructor(` past the signature parens, then to the first `{`, then bracket-counts to the matching `}`, returns source unchanged if no constructor is found (defensive — never throws).
    - When the constructor body is empty (`{}`), the insertion still lands inside the body (single newline added before the insertion so the result is readable).
    - Pure function: no I/O, no throws on happy path.
  </behavior>
  <action>
    Create `src/templates/erc721/opts.ts` as a type-only module exporting ONLY `Erc721RoyaltyOpts` (full Erc721Opts is added by plan 04-02). The interface has three readonly fields: `enabled: boolean`, `feeNumerator: number` (0-10000), `receiver: string` (`/^0x[0-9a-fA-F]{40}$/` form — runtime validator lives in 04-02). Header comment notes Plan 04-02 will extend with the full Erc721Opts surface and references CONTEXT D-04..D-07.

    Create `src/templates/erc721/royalty.ts` as the four-anchor walker. Use the implementation from RESEARCH §Pattern 3 lines 363-473 VERBATIM (planner has validated it via Wave 0 probe). The file exports exactly one function `injectRoyalty(source: string, opts: Erc721RoyaltyOpts): string` plus an internal helper `insertAtConstructorBodyEnd(source, insertion)`. Imports: `import type { Erc721RoyaltyOpts } from "./opts.js";`.

    Anchor regexes (use these EXACT patterns — RESEARCH §Pattern 3 validated them):
      - Anchor 1 (line-walker, NOT one big regex): iterate `source.split("\n")`, track `lastImport` index where `/^import \{[^}]+\} from "@openzeppelin\/contracts\/[^"]+\.sol";$/` matches; splice the ERC2981 import after `lastImport`.
      - Anchor 2: `/^(contract\s+\w+\s+is\s+)([^{]+?)(\s*\{)/m` with replacement `${prefix}${parents.trim()}, ERC2981${brace}` — IMPORTANT: use `parents.trim()` to drop trailing whitespace before appending the comma.
      - Anchor 3: call `insertAtConstructorBodyEnd(s, `    _setDefaultRoyalty(${opts.receiver}, ${opts.feeNumerator});\n`)`.
      - Anchor 4: `/(function\s+supportsInterface\(bytes4\s+interfaceId\)\s*[\s\S]*?override\()([^)]+)(\))/m` with replacement `${head}${list.trim()}, ERC2981${close}`.

    Bracket-counting walker (REQUIRED per Pitfall 1 — regex variant misplaces inserts on empty bodies): use `source.indexOf("constructor(")`, walk past constructor's parens via depth-counter (++ on `(`, -- on `)`), skip whitespace to the first `{` (the body open), then bracket-count from `bodyOpen + 1` (depth starts at 1, ++ on `{`, -- on `}`) until depth hits 0. The matching close is at `j - 1`. Insert with newline-prefix when the body was empty (i.e., `source[bodyClose - 1] === "{"`).

    Add JSDoc comments documenting each anchor's purpose, the four-anchor contract, the bracket-counting requirement (cite Pitfall 1 by name), and the no-op behavior of anchor 4. Reference 04-RESEARCH §Pattern 3 lines 363-473 in a header comment.

    DO NOT bundle this with the wizard.ts / generate.ts / index.ts files — those land in plan 04-02. This file is consumed only by royalty.spec.ts in this plan and by plan 04-02's generate.ts.

    Per D-04..D-07: this is the ONE place Phase 4 violates Phase 2 D-02 ("no string templating"). Document that in the file header.
  </action>
  <acceptance_criteria>
    - `src/templates/erc721/royalty.ts` exports `injectRoyalty(source, opts)` and the file compiles under TS strict + noUncheckedIndexedAccess (run `npm run typecheck`).
    - `src/templates/erc721/opts.ts` exports `Erc721RoyaltyOpts` with the three readonly fields.
    - Calling `injectRoyalty(anySource, {enabled:false, feeNumerator:0, receiver:"0x"+"0".repeat(40)})` returns the input source unchanged (`===` reference equality OR byte-for-byte string equality).
    - The bracket-counting walker handles `{}` (empty body) without misplacing the insertion into a sibling function.
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <done>injectRoyalty + Erc721RoyaltyOpts exist; typecheck clean; no production code consumes them yet (that arrives in 04-02).</done>
</task>

<task type="auto">
  <name>Task 2: Generate + commit 5 golden fixtures (3 erc721 + 2 erc1155); update E_USAGE copy; extend registry test</name>
  <files>tests/fixtures/erc721/bare-default.sol, tests/fixtures/erc721/all-flags-on.sol, tests/fixtures/erc721/all-flags-on-with-royalty.sol, tests/fixtures/erc1155/bare-default.sol, tests/fixtures/erc1155/all-flags-on.sol, src/commands/create.ts, tests/registry.spec.ts</files>
  <read_first>
    - .planning/phases/04-erc-721-and-erc-1155-templates/04-RESEARCH.md (§Wave 0 Gaps lines 1184-1201 — fixture generation calls; §Pitfall 3 lines 596-606 — ERC-1155 updatableUri:true rationale)
    - .planning/phases/04-erc-721-and-erc-1155-templates/04-PATTERNS.md (§Pattern S8 — SPDX header convention; §`src/cli.ts` MODIFY section — only context, not modified here)
    - .planning/phases/04-erc-721-and-erc-1155-templates/04-CONTEXT.md (D-13, D-14, D-15)
    - src/commands/create.ts lines 54-62 (current E_USAGE block — only the `why` + `fix` lines change)
    - tests/registry.spec.ts (whole file — current test patterns)
    - tests/fixtures/erc20/bare-default.sol + all-flags-on.sol (header / SPDX / pragma format to match LF-encoded)
  </read_first>
  <behavior>
    - `tests/fixtures/erc721/bare-default.sol` is the byte-for-byte output of `erc721.print({name:"MyNFT", symbol:"MNFT"})` — wizard defaults for everything else.
    - `tests/fixtures/erc721/all-flags-on.sol` is the byte-for-byte output of `erc721.print({name:"MyNFT", symbol:"MNFT", baseUri:"https://example.com/api/token/", mintable:true, enumerable:true, burnable:true, pausable:true, uriStorage:false, access:"roles"})`. NOTE: `uriStorage:false` matches the Open Question 3 RESEARCH recommendation (keep `uriStorage` reserved; do NOT surface in wizard).
    - `tests/fixtures/erc721/all-flags-on-with-royalty.sol` is the above all-flags-on piped through `injectRoyalty({enabled:true, feeNumerator:250, receiver:"0x0000000000000000000000000000000000000000"})`. Must include the ERC2981 import, ERC2981 in the parent list, `_setDefaultRoyalty(0x0000000000000000000000000000000000000000, 250);` in the constructor body, and ERC2981 in the supportsInterface override list.
    - `tests/fixtures/erc1155/bare-default.sol` is the byte-for-byte output of `erc1155.print({name:"MyMulti", uri:"https://example.com/api/token/{id}.json"})` — includes Ownable + setURI per the wizard default `updatableUri:true` (RESEARCH Pitfall 3).
    - `tests/fixtures/erc1155/all-flags-on.sol` is the output of `erc1155.print({name:"MyMulti", uri:"https://example.com/api/token/{id}.json", mintable:true, burnable:true, supply:true, pausable:true, updatableUri:true, access:"roles"})`.
    - All five fixtures are committed LF-encoded (Phase 1 .gitattributes `eol=lf` is load-bearing).
    - `src/commands/create.ts` E_USAGE branch (currently at lines 54-62) updates only its `why` and `fix` lines per CONTEXT D-14. The `what`, `code`, and `exitCode` are unchanged.
    - `tests/registry.spec.ts` gains one new test asserting all three templates register without collision and each exposes runWizard + generate as functions. Existing tests stay green.
  </behavior>
  <action>
    Step A — generate fixtures via a throwaway script: Write a temporary helper at `scripts/gen-phase4-fixtures.mjs` (NOT committed — delete after use). The script imports `erc721`, `erc1155` from `@openzeppelin/wizard` and `injectRoyalty` from `../src/templates/erc721/royalty.js` (via tsc build OR via `tsx` if available — if neither, hand-write the script in plain Node + dynamic import after `npm run build` so the dist/ files are available). The script calls the five generators per the §behavior section, writes each output to its target fixture path with LF line endings (use `writeFileSync(path, source.replace(/\r\n/g, "\n"), "utf8")`), and prints "WROTE 5 FIXTURES" on success. Run via `node scripts/gen-phase4-fixtures.mjs` (or `npm run build && node scripts/gen-phase4-fixtures.mjs` if build is required first).

    After fixtures are committed (`git status` shows the 5 new `.sol` files), delete `scripts/gen-phase4-fixtures.mjs`. The fixtures are committed; the script is not (per RESEARCH §Specifics line 135 — snapshot regeneration script deferred to v2).

    Step B — update `src/commands/create.ts` E_USAGE copy: locate the block at lines 54-62 (CliError throw when `!globalOpts.template`). Replace the `why` line with `"\`smartc create\` requires --template. Three templates ship in Phase 4: erc20, erc721, erc1155. The interactive multi-template picker is deferred."` and the `fix` line with `` "Re-run with `--template <erc20|erc721|erc1155>`. Run `smartc list-templates` to see available templates." ``. Do NOT touch ANY other line in `src/commands/create.ts` — per CONTEXT D-12, the dispatcher is otherwise untouched.

    Step C — extend `tests/registry.spec.ts`: Add two new imports at the top: `import { registerErc721Template } from "../src/templates/erc721/index.js";` and `import { registerErc1155Template } from "../src/templates/erc1155/index.js";`. NOTE: these template barrel files do NOT exist yet (they land in plans 04-02 and 04-03). To unblock this plan's CI without committing broken imports, GATE the new test with a conditional dynamic import OR add the test stub with a `it.skip` annotation that plan 04-04 will flip to `it`.

    Recommended path: Add the new test as `it.skip("registers all three Phase 4 templates without collision and exposes runWizard/generate")` with a TODO comment `// TODO(plan 04-04): flip to it() once erc721/index.ts + erc1155/index.ts ship`. Inside the skipped body, place the actual assertions per 04-PATTERNS.md §`tests/registry.spec.ts` (call registerErc20Template + registerErc721Template + registerErc1155Template, assert list().length === 3, iterate ["erc20", "erc721", "erc1155"] and assert .id, .chain="evm", typeof runWizard==="function", typeof generate==="function"). Plan 04-04 flips skip → run after the two template plans land.

    Per D-01..D-03: fixtures must be the literal wizard output. Do not hand-edit them. The royalty fixture is the ONE exception — it is `wizard output → injectRoyalty(…)` — but injectRoyalty itself is the deterministic post-process from Task 1.
  </action>
  <acceptance_criteria>
    - All 5 fixtures exist on disk with size > 0 and LF line endings (`file -b tests/fixtures/erc721/bare-default.sol` reports ASCII text, no CRLF).
    - `src/commands/create.ts` line ~58-60 shows the new D-14 copy. `grep -n "<erc20|erc721|erc1155>" src/commands/create.ts` returns exactly one match.
    - `tests/registry.spec.ts` contains a new `it.skip(...)` block with the three-template assertion body and a TODO comment naming plan 04-04.
    - `scripts/gen-phase4-fixtures.mjs` does NOT exist at the end of this task (`ls scripts/gen-phase4-fixtures.mjs` errors).
    - The existing test suite (excluding the new `it.skip`) still passes: `npx vitest run`.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/registry.spec.ts tests/commands/create.spec.ts</automated>
  </verify>
  <done>5 fixtures committed; E_USAGE copy updated; registry test extended with skip stub; existing tests green.</done>
</task>

<task type="auto">
  <name>Task 3: royalty.spec.ts — 3 anchor scenarios validated via Phase 3 compile gate</name>
  <files>tests/templates/erc721/royalty.spec.ts</files>
  <read_first>
    - .planning/phases/04-erc-721-and-erc-1155-templates/04-RESEARCH.md (§Probe C lines 794-803; §Validation Architecture lines 1186-1188)
    - .planning/phases/04-erc-721-and-erc-1155-templates/04-PATTERNS.md (§`tests/templates/erc721/royalty.spec.ts` lines ~720-754; §Pattern S6 — real-solc integration test)
    - .planning/phases/04-erc-721-and-erc-1155-templates/04-CONTEXT.md (D-17)
    - src/compiler/index.ts (compileVerify signature)
    - tests/compiler/compile.integration.spec.ts (real-solc pattern — no vi.mock)
    - src/templates/erc721/royalty.ts (the SUT — from Task 1)
  </read_first>
  <behavior>
    - Test 1 — bare ERC-721 + royalty 250: assert `injectRoyalty(erc721.print({name:"T", symbol:"T"}), {enabled:true, feeNumerator:250, receiver:"0x"+"0".repeat(40)})` includes `import {ERC2981}`, the parent-list pattern matches `/contract\s+\w+\s+is\s+[^{]+ERC2981/`, the constructor body contains `_setDefaultRoyalty(0x0000000000000000000000000000000000000000, 250);` (anchor 3 fired into the empty body), and anchor 4 is a no-op (the result does NOT contain `override(`). Compile via `compileVerify(result, "evm")` and assert it does not throw and returns `{ warnings: [] | [...] }`.
    - Test 2 — ownable + mintable + royalty 500: input is `erc721.print({name:"T", symbol:"T", mintable:true, access:"ownable"})`; expected — anchors 1, 2, 3 fire; anchor 4 is a no-op (wizard does not emit supportsInterface for Ownable-only); `_setDefaultRoyalty(0x..., 500);` lands inside the constructor body AFTER the Ownable initializer; result compiles clean.
    - Test 3 — all-flags + roles + royalty 250: input is `erc721.print({name:"T", symbol:"T", baseUri:"https://x/", mintable:true, enumerable:true, burnable:true, pausable:true, uriStorage:false, access:"roles"})`; expected — all 4 anchors fire. Result contains `ERC2981` in the `is` list AND in the `supportsInterface(...)` override list. Result compiles clean.
    - Test 4 — opts.enabled === false: input = arbitrary wizard output; result must equal the input string byte-for-byte (D-06 invariant).
  </behavior>
  <action>
    Create `tests/templates/erc721/royalty.spec.ts`. Use the exact import shape from 04-PATTERNS.md §`tests/templates/erc721/royalty.spec.ts` lines 730-734:
      `import { describe, it, expect } from "vitest";`
      `import { erc721 } from "@openzeppelin/wizard";`
      `import { injectRoyalty } from "../../../src/templates/erc721/royalty.js";`
      `import { compileVerify } from "../../../src/compiler/index.js";`

    Wrap in a single `describe("injectRoyalty — 4-anchor post-process", () => { ... })` block with the four `it()` cases above. Each test:
      1. Build the source via `erc721.print(...)`.
      2. Call `const injected = injectRoyalty(wizardSrc, royaltyOpts)`.
      3. Assert the string-level contract per §behavior (use `expect(injected).toContain(...)` / `expect(injected).toMatch(/regex/)` / `expect(injected).not.toContain(...)`).
      4. Pipe through `await compileVerify(injected, "evm")` and assert `Array.isArray(result.warnings)` is `true`.

    For Test 4 (opt-out invariant): call `injectRoyalty(srcA, {enabled:false, feeNumerator:0, receiver:"0x"+"0".repeat(40)})` and `expect(result).toBe(srcA)` (strict identity OR byte-for-byte). DO NOT compile the result — that path is exercised by the existing erc721/erc1155 integration tests in plan 04-04.

    Crucially: this test file uses NO `vi.mock`. It runs real solc + real OZ via the Phase 3 gate. The compile path is the production code path — this is the "wave-0 royalty probe in test form" per CONTEXT D-17.

    DO NOT add Test 5 / Test 6 covering wizard outputs that this plan does not generate — keep the spec focused on the three Wave 0 scenarios (probe-validated in RESEARCH §Probe C lines 794-803).
  </action>
  <acceptance_criteria>
    - `tests/templates/erc721/royalty.spec.ts` exists with 4 `it()` blocks under one `describe`.
    - `npx vitest run tests/templates/erc721/royalty.spec.ts` reports 4 passed.
    - No `vi.mock` calls in the file (grep returns zero).
    - Each compile-gate call routes through `compileVerify(injected, "evm")` — no direct `solc.compile` invocations.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/templates/erc721/royalty.spec.ts</automated>
  </verify>
  <done>royalty.spec.ts green; the 4-anchor walker is validated against real wizard outputs and real solc compile.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user input → wizard validators | User-typed strings (royalty receiver, basis points) cross into the smartc process — full validation deferred to plan 04-02 where validators land; this plan ships only the post-process |
| smartc → @openzeppelin/wizard | Trusted dependency; pinned at 0.10.8; legitimacy audited in Phase 2 + Phase 4 (no new install) |
| smartc → @openzeppelin/contracts (`ERC2981.sol`) | Trusted dependency; pinned at 5.6.1; only inserted via the royalty post-process |
| royalty post-process → generated Solidity source | Anchor-located insertions modify the wizard's output before it reaches the Phase 3 compile gate. The compile gate is the final tamper-detection — malformed insertions surface as `E_COMPILE_FAILED` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01-01 | Tampering | royalty.ts anchor 3 (constructor body) | mitigate | Bracket-counting walker (NOT regex) — RESEARCH Pitfall 1 reproduces the regex-only failure; bracket-counting is grammar-exact and tolerates empty `{}` |
| T-04-01-02 | Tampering | royalty.ts anchor 4 (supportsInterface override) | mitigate | Anchor 4 is a no-op when the wizard does not emit a supportsInterface override; the all-flags-on-with-royalty fixture exercises the override-extension path; the bare-with-royalty case exercises the no-op path |
| T-04-01-03 | Tampering | Fixture generation | mitigate | Fixtures are generated by calling `erc721.print` / `erc1155.print` directly + (royalty case) `injectRoyalty`; LF-encoded; committed as-emitted; downstream snapshot tests catch any drift |
| T-04-01-04 | Tampering | E_USAGE copy update | accept | One-line copy edit; the dispatcher logic is untouched (D-12) |
| T-04-01-SC | Tampering | No new package installs in this plan | accept | Phase 4 ships zero new packages (RESEARCH §Package Legitimacy Audit). All three pinned packages (`@openzeppelin/wizard`, `@openzeppelin/contracts`, `solc`) were audited in Phase 2 / Phase 3 and remain Approved. No `[ASSUMED]`/`[SUS]`/`[SLOP]` items in Phase 4 |
</threat_model>

<verification>
- All 5 fixtures exist and are LF-encoded (`tests/fixtures/erc721/*.sol`, `tests/fixtures/erc1155/*.sol`).
- `src/templates/erc721/royalty.ts` exports `injectRoyalty`; `src/templates/erc721/opts.ts` exports `Erc721RoyaltyOpts`.
- `src/commands/create.ts` lines 54-62 show the D-14 copy; `grep -n "<erc20|erc721|erc1155>" src/commands/create.ts` returns one match; no other line in `src/commands/create.ts` is modified.
- `tests/registry.spec.ts` contains the `it.skip` three-template registration test with a TODO comment naming plan 04-04.
- `tests/templates/erc721/royalty.spec.ts` exists with 4 `it()` cases under one `describe`.
- `npm run typecheck` clean.
- `npx vitest run tests/templates/erc721/royalty.spec.ts tests/registry.spec.ts tests/commands/create.spec.ts tests/compiler/compile.integration.spec.ts` — all green.
- `scripts/gen-phase4-fixtures.mjs` is NOT present.
</verification>

<success_criteria>
- SC-5 partial: 5 royalty + ERC-721 + ERC-1155 fixtures compile clean under the Phase 3 gate, proving the gate accepts new EVM Solidity sources without modification.
- ERC721-03 foundation: royalty post-process is implemented + tested in isolation; plan 04-02 consumes it from `generate.ts`.
- D-12 verified: only one line of `src/commands/create.ts` changed (the E_USAGE copy); no other dispatcher code is modified.
- Plans 04-02, 04-03 are unblocked.
</success_criteria>

<output>
Create `.planning/phases/04-erc-721-and-erc-1155-templates/04-01-SUMMARY.md` when done.
Include: list of artifacts shipped, anchor-by-anchor validation table from the royalty.spec.ts runs, fixture byte-counts, the literal new E_USAGE copy lines for downstream Plan 04-04 reference.
</output>
