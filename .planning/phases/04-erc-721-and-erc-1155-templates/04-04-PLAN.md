---
phase: 04
plan: 04
type: execute
wave: 2
depends_on:
  - 04-01
  - 04-02
  - 04-03
files_modified:
  - src/cli.ts
  - tests/registry.spec.ts
  - tests/compiler/compile.integration.spec.ts
  - tests/commands/create.compile.spec.ts
files_modified_optional: []
autonomous: true
requirements:
  - ERC721-01
  - ERC721-02
  - ERC721-03
  - ERC721-04
  - ERC721-05
  - ERC1155-01
  - ERC1155-02
  - ERC1155-03
  - ERC1155-04
  - ERC1155-05
tags:
  - phase-04
  - wave-2
  - boot
  - integration
  - e2e

must_haves:
  truths:
    - "`smartc list-templates` shows three rows (erc20, erc721, erc1155) after boot wiring (CONTEXT D-15)"
    - "Both new templates register without duplicate-id collision (`tests/registry.spec.ts` it.skip from plan 04-01 is flipped to it() and passes)"
    - "All 5 new EVM fixtures (3 erc721 + 2 erc1155) compile clean under `compileVerify(source, 'evm')` via the parametrized `describe.each` in `tests/compiler/compile.integration.spec.ts` (SC-5)"
    - "End-to-end happy-path E2E tests for ERC-721 and ERC-1155 in `tests/commands/create.compile.spec.ts` write a real .sol file via the full dispatcher pipeline (registry → wizard → generate → compileVerify → fs.writeFile)"
    - "`src/cli.ts` adds exactly 4 lines (2 imports + 2 register calls); the rest of `src/cli.ts` is unchanged (RESEARCH §Probe E)"
  artifacts:
    - path: "src/cli.ts"
      provides: "Boot wires registerErc721Template + registerErc1155Template next to the existing registerErc20Template (D-15)"
      contains: "registerErc721Template"
    - path: "tests/registry.spec.ts"
      provides: "Three-template no-collision registration test (it.skip from plan 04-01 flipped to it())"
      contains: "registerErc1155Template"
    - path: "tests/compiler/compile.integration.spec.ts"
      provides: "Refactored to describe.each(FIXTURES) — happy-path corpus extended with 3 erc721 + 2 erc1155 fixture rows; broken/warns error-path it blocks preserved"
      contains: "describe.each"
    - path: "tests/commands/create.compile.spec.ts"
      provides: "Extended with primeErc721HappyPathMocks + primeErc1155HappyPathMocks helpers + 2 new happy-path it() blocks paralleling the ERC-20 test"
      contains: "primeErc721HappyPathMocks"
  key_links:
    - from: "src/cli.ts boot"
      to: "src/templates/erc721/index.ts + src/templates/erc1155/index.ts"
      via: "registerErc721Template() + registerErc1155Template()"
      pattern: "registerErc(721|1155)Template\\(\\);"
    - from: "tests/compiler/compile.integration.spec.ts"
      to: "tests/fixtures/erc721/*.sol + tests/fixtures/erc1155/*.sol"
      via: "describe.each fixture-row loop"
      pattern: "fixtures/erc(721|1155)/"
    - from: "tests/commands/create.compile.spec.ts"
      to: "src/templates/erc721/index.ts + src/templates/erc1155/index.ts"
      via: "beforeEach registers all three templates"
      pattern: "registerErc(20|721|1155)Template\\(\\);"
---

<objective>
Wave 2 — Wire everything together. After plans 04-01 (foundation + fixtures + royalty + E_USAGE), 04-02 (ERC-721 plugin), and 04-03 (ERC-1155 plugin) ship, this plan adds the missing connective tissue:

1. **Boot wiring** in `src/cli.ts` — 2 imports + 2 register calls so `smartc list-templates` shows three rows on a fresh install.
2. **Registry test flip** — `tests/registry.spec.ts` `it.skip(...)` stub from plan 04-01 flips to live `it(...)` asserting all three templates register without collision.
3. **Compile-integration corpus extension** — `tests/compiler/compile.integration.spec.ts` refactored to `describe.each(FIXTURES)` parametrization with 5 new fixture rows (2 erc20 existing + 3 erc721 + 2 erc1155 = 7 happy-path rows).
4. **E2E happy-path coverage** — `tests/commands/create.compile.spec.ts` extended with `primeErc721HappyPathMocks` + `primeErc1155HappyPathMocks` helpers + 2 new `it()` blocks paralleling the existing ERC-20 E2E test.

Purpose: Final assembly. Until this plan ships, `smartc create --template erc721` exits with "Template 'erc721' was not found in the registry" because the boot path never registers it. After this plan ships, the full SC-1..SC-5 success criteria from ROADMAP §Phase 4 are satisfied.

Output: 4 modified files; 1 flipped test; 5 new compile-integration fixture rows; 2 new E2E happy-path cases.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-erc-721-and-erc-1155-templates/04-CONTEXT.md
@.planning/phases/04-erc-721-and-erc-1155-templates/04-RESEARCH.md
@.planning/phases/04-erc-721-and-erc-1155-templates/04-PATTERNS.md
@.planning/phases/04-erc-721-and-erc-1155-templates/04-01-PLAN.md
@.planning/phases/04-erc-721-and-erc-1155-templates/04-02-PLAN.md
@.planning/phases/04-erc-721-and-erc-1155-templates/04-03-PLAN.md
@src/cli.ts
@tests/registry.spec.ts
@tests/compiler/compile.integration.spec.ts
@tests/commands/create.compile.spec.ts

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/cli.ts (CURRENT — lines 1-9):
- Imports `buildProgram` from "./program.js"
- Imports `registerErc20Template` from "./templates/erc20/index.js"
- Calls `registerErc20Template()` in `main()`
- THIS plan adds 2 more imports + 2 more register calls between these existing lines.

From src/templates/erc721/index.ts (PLAN 04-02 — DO NOT MODIFY):
- `export function registerErc721Template(): void;`

From src/templates/erc1155/index.ts (PLAN 04-03 — DO NOT MODIFY):
- `export function registerErc1155Template(): void;`

From tests/registry.spec.ts (CURRENT — extended by plan 04-01):
- Plan 04-01 added an `it.skip(...)` block with the three-template assertion body.
- This plan flips `it.skip` → `it` and ensures the imports + assertions are correct.

From tests/compiler/compile.integration.spec.ts (CURRENT — lines 1-67):
- Three hardcoded `it()` blocks for ERC-20 bare-default, ERC-20 all-flags-on, broken.sol, warns-no-error.sol.
- This plan refactors the two happy-path it() blocks into a `describe.each(FIXTURES)` block; the two error-path blocks stay unchanged.

From tests/commands/create.compile.spec.ts (CURRENT — lines 1-176):
- Mocks @clack/prompts at the top.
- `primeHappyPathMocks()` primes 3 text + 3 confirm for ERC-20.
- `beforeEach` calls `registerErc20Template()`.
- ONE happy-path it() block for ERC-20.
- This plan adds: 2 new prime helpers, beforeEach registers all 3 templates, 2 new it() blocks.

From plan 04-02 SUMMARY (the wizard prompt order for ERC-721):
- name → symbol → baseUri → mintable → enumerable → burnable → pausable → royalty → (if royalty) bps + receiver → (if mintable||pausable) access
- Happy path with no flags primes: text("MyNFT") + text("MNFT") + text("") + confirm(false)×5 (mintable, enumerable, burnable, pausable, royalty) — 3 text + 5 confirm; no select.

From plan 04-03 SUMMARY (the wizard prompt order for ERC-1155):
- name → uri → mintable → burnable → supply → pausable → (if mintable||pausable) access
- Happy path with no flags primes: text("MyMulti") + text("https://example.com/api/token/{id}.json") + confirm(false)×4 — 2 text + 4 confirm; no select.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire src/cli.ts boot + flip tests/registry.spec.ts it.skip → it</name>
  <files>src/cli.ts, tests/registry.spec.ts</files>
  <read_first>
    - src/cli.ts (CURRENT — full file; the 2 new imports + 2 new register calls land in specific positions)
    - tests/registry.spec.ts (CURRENT — the it.skip stub added by plan 04-01)
    - src/templates/erc721/index.ts (plan 04-02 output — exports registerErc721Template)
    - src/templates/erc1155/index.ts (plan 04-03 output — exports registerErc1155Template)
    - .planning/phases/04-erc-721-and-erc-1155-templates/04-CONTEXT.md (D-15)
    - .planning/phases/04-erc-721-and-erc-1155-templates/04-PATTERNS.md (§`src/cli.ts (MODIFY — 4 lines added)` lines 822-848)
  </read_first>
  <behavior>
    - `src/cli.ts` boots `registerErc20Template() + registerErc721Template() + registerErc1155Template()` in that order before `buildProgram()`.
    - Registration order is not semantically load-bearing (the registry rejects duplicate ids on insert, not order — Phase 1 D-08), but PATTERNS recommends keeping the order matching the historical sequence (erc20 → erc721 → erc1155) for diff-readability.
    - `tests/registry.spec.ts` `it(...)` (formerly `it.skip(...)`) registers all three templates sequentially, asserts `list().length === 3`, iterates [`"erc20", "erc721", "erc1155"`], and asserts each template's `.id`, `.chain === "evm"`, `typeof runWizard === "function"`, `typeof generate === "function"`.
    - All other tests in `tests/registry.spec.ts` stay green (the new test runs in the `beforeEach(clear())` isolation).
  </behavior>
  <action>
    Step A — `src/cli.ts`: Locate the existing import line `import { registerErc20Template } from "./templates/erc20/index.js";` (line 3). IMMEDIATELY AFTER it, add two new lines:
      `import { registerErc721Template } from "./templates/erc721/index.js";`
      `import { registerErc1155Template } from "./templates/erc1155/index.js";`

    Locate the existing `registerErc20Template();` call inside `main()` (line 9). IMMEDIATELY AFTER it, add two new lines:
      `  registerErc721Template();`
      `  registerErc1155Template();`

    DO NOT modify any other line in `src/cli.ts`. The error-handling block, SIGINT handler, color/newbie resolution, and `void main()` invocation all stay unchanged. The diff for this file is exactly 4 added lines (2 imports + 2 calls).

    Step B — `tests/registry.spec.ts`: Plan 04-01 added an `it.skip(...)` block with a TODO comment referencing plan 04-04. Locate that block. Verify the imports at the top of the file include `registerErc721Template` from `"../src/templates/erc721/index.js"` and `registerErc1155Template` from `"../src/templates/erc1155/index.js"` — if missing or commented out, add them.

    Flip `it.skip(...)` → `it(...)`. Remove the TODO comment referencing plan 04-04 (it's no longer pending). The test body should:
      ```
      registerErc20Template();
      registerErc721Template();
      registerErc1155Template();
      expect(list()).toHaveLength(3);
      for (const id of ["erc20", "erc721", "erc1155"]) {
        const tpl = get(id);
        expect(tpl).toBeDefined();
        expect(tpl!.id).toBe(id);
        expect(tpl!.chain).toBe("evm");
        expect(typeof (tpl as Record<string, unknown>).runWizard).toBe("function");
        expect(typeof (tpl as Record<string, unknown>).generate).toBe("function");
      }
      ```

    Optionally add a second `it("is idempotent when registerErc721Template() is called twice", ...)` mirroring the existing erc20 idempotency test pattern.

    DO NOT touch any other test in `tests/registry.spec.ts`. The existing 8 tests stay green.
  </action>
  <acceptance_criteria>
    - `src/cli.ts` shows exactly 4 new lines vs. the prior commit (`git diff src/cli.ts | grep '^+' | grep -v '^+++' | wc -l` returns 4).
    - `node dist/cli.js list-templates` after `npm run build` outputs 3 rows (erc20, erc721, erc1155) — MANUAL VERIFICATION step recorded in 04-VALIDATION.md.
    - `tests/registry.spec.ts` contains the live three-template registration test (no `it.skip` for this test).
    - `npx vitest run tests/registry.spec.ts` reports all tests green; the three-template test runs and passes.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/registry.spec.ts &amp;&amp; npm run build</automated>
  </verify>
  <done>Boot wires both new templates; registry test confirms three-template no-collision registration; build succeeds.</done>
</task>

<task type="auto">
  <name>Task 2: Extend compile.integration.spec.ts — refactor happy-path to describe.each with 5 new fixture rows</name>
  <files>tests/compiler/compile.integration.spec.ts</files>
  <read_first>
    - tests/compiler/compile.integration.spec.ts (CURRENT full file; refactor target)
    - .planning/phases/04-erc-721-and-erc-1155-templates/04-RESEARCH.md (§Extending tests/compiler/compile.integration.spec.ts lines 1024-1056)
    - .planning/phases/04-erc-721-and-erc-1155-templates/04-PATTERNS.md (§`tests/compiler/compile.integration.spec.ts (MODIFY — add 5 fixture rows)` lines 912-936)
    - .planning/phases/04-erc-721-and-erc-1155-templates/04-CONTEXT.md (D-13, D-16 — SC-5 satisfied via integration tests)
    - tests/fixtures/erc721/*.sol (committed in plan 04-01)
    - tests/fixtures/erc1155/*.sol (committed in plan 04-01)
  </read_first>
  <behavior>
    - The current two happy-path `it()` blocks (lines 23-35: ERC-20 bare-default and all-flags-on) are replaced with a `describe.each(FIXTURES)("compileVerify — $label fixture", ...)` block that iterates over 7 entries.
    - The FIXTURES array contains 7 rows: `erc20 bare-default`, `erc20 all-flags-on`, `erc721 bare-default`, `erc721 all-flags-on`, `erc721 all-flags-on-with-royalty`, `erc1155 bare-default`, `erc1155 all-flags-on`. Paths are relative to `tests/compiler/`: `../fixtures/erc20/bare-default.sol`, etc.
    - The two error-path `it()` blocks (lines 37-66: broken.sol throw + warns-no-error.sol) STAY UNCHANGED.
    - Each row's `it()` reads the fixture from disk, calls `await compileVerify(source, "evm")`, and asserts `Array.isArray(result.warnings)` is true. No throws expected. (Wave 0 probe in plan 04-01 already validated all 5 new fixtures compile clean; this test locks the canary.)
    - Total test count after refactor: 7 happy-path (was 2) + 2 error-path (unchanged) = 9.
  </behavior>
  <action>
    Replace lines 15-35 of `tests/compiler/compile.integration.spec.ts` (the BARE_DEFAULT / ALL_FLAGS_ON path consts + the two happy-path it blocks) with a fixtures array + a `describe.each` block per RESEARCH lines 1027-1055:

    ```
    const FIXTURES: ReadonlyArray<{ label: string; path: string }> = [
      { label: "erc20 bare-default",                  path: "../fixtures/erc20/bare-default.sol" },
      { label: "erc20 all-flags-on",                  path: "../fixtures/erc20/all-flags-on.sol" },
      { label: "erc721 bare-default",                 path: "../fixtures/erc721/bare-default.sol" },
      { label: "erc721 all-flags-on",                 path: "../fixtures/erc721/all-flags-on.sol" },
      { label: "erc721 all-flags-on-with-royalty",    path: "../fixtures/erc721/all-flags-on-with-royalty.sol" },
      { label: "erc1155 bare-default",                path: "../fixtures/erc1155/bare-default.sol" },
      { label: "erc1155 all-flags-on",                path: "../fixtures/erc1155/all-flags-on.sol" },
    ];

    describe.each(FIXTURES)("compileVerify — $label fixture", ({ path }) => {
      it("compiles clean (zero errors)", async () => {
        const source = readFileSync(join(__dirname, path), "utf8");
        const result = await compileVerify(source, "evm");
        expect(Array.isArray(result.warnings)).toBe(true);
      });
    });
    ```

    Place this block where lines 23-35 (the two original happy-path `it`s inside the existing `describe(...)`) currently live. Two options:
      (a) Keep the existing outer `describe("compileVerify — integration with real solc + real OZ", ...)` block and add the describe.each INSIDE it; OR
      (b) Hoist the describe.each to top-level outside the outer describe, leaving the outer describe to wrap only the error-path it blocks.

    Recommendation: option (a) — keep one outer describe for cohesion. Vitest 4 nested describe.each is supported.

    Preserve the two error-path it blocks (lines 37-66) verbatim — `broken.sol throws E_COMPILE_FAILED ...` and `warns-no-error.sol returns warnings.length >= 1 ...`. Delete the now-unused `BARE_DEFAULT` and `ALL_FLAGS_ON` path consts (lines 15-19), keep `BROKEN` and `WARNS` (lines 20-21).

    Verify import statements at the top: keep `readFileSync`, `fileURLToPath`, `dirname`, `join`, and `compileVerify`. Add nothing new — vitest exports are unchanged.

    DO NOT modify `tests/fixtures/erc721/*.sol`, `tests/fixtures/erc1155/*.sol` — they shipped in plan 04-01 and are immutable golden artifacts.

    DO NOT touch `src/compiler/*` — D-12 forbids changes to the compile module in Phase 4.
  </action>
  <acceptance_criteria>
    - `tests/compiler/compile.integration.spec.ts` contains exactly one `describe.each(FIXTURES)` block iterating 7 fixtures and 2 error-path `it()` blocks (broken + warns).
    - Test count when run: 7 happy-path passes + 2 error-path passes = 9 total.
    - `npx vitest run tests/compiler/compile.integration.spec.ts` reports all 9 tests green.
    - The 5 new fixture compile-verifies all succeed (no throw, warnings array exists), confirming SC-5.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/compiler/compile.integration.spec.ts</automated>
  </verify>
  <done>Integration corpus extended; all 5 new fixtures compile clean under the unchanged Phase 3 gate; SC-5 satisfied.</done>
</task>

<task type="auto">
  <name>Task 3: Extend create.compile.spec.ts — 2 prime helpers + 2 happy-path E2E cases for ERC-721 + ERC-1155</name>
  <files>tests/commands/create.compile.spec.ts</files>
  <read_first>
    - tests/commands/create.compile.spec.ts (CURRENT full file)
    - .planning/phases/04-erc-721-and-erc-1155-templates/04-RESEARCH.md (§Extending tests/commands/create.compile.spec.ts lines 1058-1085)
    - .planning/phases/04-erc-721-and-erc-1155-templates/04-PATTERNS.md (§`tests/commands/create.compile.spec.ts (MODIFY — add 2 happy-path E2E cases)` lines 940-973)
    - src/templates/erc721/wizard.ts (plan 04-02 output — confirms ERC-721 prompt order)
    - src/templates/erc1155/wizard.ts (plan 04-03 output — confirms ERC-1155 prompt order)
    - .planning/phases/04-erc-721-and-erc-1155-templates/04-02-SUMMARY.md (plan 04-02 SUMMARY — confirms happy-path prompt-by-prompt sequence for E2E priming)
    - .planning/phases/04-erc-721-and-erc-1155-templates/04-03-SUMMARY.md (plan 04-03 SUMMARY — same for ERC-1155)
  </read_first>
  <behavior>
    - `primeErc721HappyPathMocks()` primes 3 text answers + 5 confirm answers for the ERC-721 wizard's happy "no flags + no royalty" path: text("MyNFT"), text("MNFT"), text(""), confirm(false)×5 (mintable, enumerable, burnable, pausable, royalty). No select call expected.
    - `primeErc1155HappyPathMocks()` primes 2 text + 4 confirm: text("MyMulti"), text("https://example.com/api/token/{id}.json"), confirm(false)×4 (mintable, burnable, supply, pausable).
    - The `beforeEach` block extends to call `registerErc20Template() + registerErc721Template() + registerErc1155Template()` (after `clear()`).
    - Two new `it()` blocks each:
      1. Prime mocks via the new helper.
      2. Call `program.parseAsync(["create", "--template", <id>, "--newbie", "--out", outPath], { from: "user" })`.
      3. Assert `existsSync(outPath)` is true.
      4. Read the written file, assert it contains `contract MyNFT` (ERC-721) or `contract MyMulti` (ERC-1155).
      5. Assert captured stdio contains `Wrote ${outPath}`, `Compile-verified against solc`, and the OZ contracts version.
    - Existing tests stay green (the existing ERC-20 happy-path test, warning pass-through test, version-line test).
  </behavior>
  <action>
    Step A — add imports: at the top of `tests/commands/create.compile.spec.ts` (after the existing `const { registerErc20Template } = await import("../../src/templates/erc20/index.js");` line), add:
      ```
      const { registerErc721Template } = await import(
        "../../src/templates/erc721/index.js"
      );
      const { registerErc1155Template } = await import(
        "../../src/templates/erc1155/index.js"
      );
      ```

    Step B — add prime helpers AFTER the existing `primeHappyPathMocks()` function (around line 71):
      ```
      function primeErc721HappyPathMocks(): void {
        textMock.mockResolvedValueOnce("MyNFT");
        textMock.mockResolvedValueOnce("MNFT");
        textMock.mockResolvedValueOnce(""); // baseUri (empty)
        confirmMock.mockResolvedValueOnce(false); // mintable
        confirmMock.mockResolvedValueOnce(false); // enumerable
        confirmMock.mockResolvedValueOnce(false); // burnable
        confirmMock.mockResolvedValueOnce(false); // pausable
        confirmMock.mockResolvedValueOnce(false); // royalty
      }

      function primeErc1155HappyPathMocks(): void {
        textMock.mockResolvedValueOnce("MyMulti");
        textMock.mockResolvedValueOnce("https://example.com/api/token/{id}.json");
        confirmMock.mockResolvedValueOnce(false); // mintable
        confirmMock.mockResolvedValueOnce(false); // burnable
        confirmMock.mockResolvedValueOnce(false); // supply
        confirmMock.mockResolvedValueOnce(false); // pausable
      }
      ```

    Step C — extend `beforeEach` (currently lines 95-104). Replace the lone `registerErc20Template();` call with:
      ```
      registerErc20Template();
      registerErc721Template();
      registerErc1155Template();
      ```

    Step D — add two new it() blocks after the existing ERC-20 happy-path block (line 140), BEFORE the warning-pass-through block. Each block mirrors the existing happy-path test shape (lines 111-140):

      Test 1 — ERC-721 happy path:
      ```
      it("happy path ERC-721 (no flags, no royalty) — file is compile-verified, written to disk, contains contract MyNFT, footer shows version line", async () => {
        primeErc721HappyPathMocks();
        const outPath = join(tmpDir, "MyNFT.sol");
        const program = buildProgram();
        const captured = await captureStdio(async () => {
          await program
            .exitOverride()
            .parseAsync(
              ["create", "--template", "erc721", "--newbie", "--out", outPath],
              { from: "user" },
            );
        });
        expect(existsSync(outPath)).toBe(true);
        const written = readFileSync(outPath, "utf8");
        expect(written).toContain("contract MyNFT");
        expect(captured).toContain(`Wrote ${outPath}`);
        expect(captured).toContain("Compile-verified against solc");
        expect(captured).toContain("@openzeppelin/contracts");
      });
      ```

      Test 2 — ERC-1155 happy path: same shape but `primeErc1155HappyPathMocks()`, `--template erc1155`, `MyMulti.sol`, `contract MyMulti`.

    DO NOT remove the existing ERC-20 happy-path test, warning-pass-through test, or version-line test — they stay verbatim.

    DO NOT add a test for the royalty branch here — that's covered by plan 04-01's royalty.spec.ts at the unit-test layer + plan 04-02's generate.spec.ts at the snapshot layer + plan 04-04 Task 2's integration spec at the compile-verify layer.

    DO NOT touch `src/commands/create.ts` — the dispatcher is unchanged (D-12); plan 04-01 already shipped the one E_USAGE copy edit.
  </action>
  <acceptance_criteria>
    - `tests/commands/create.compile.spec.ts` contains two new prime helpers, an extended `beforeEach` registering all three templates, and two new `it()` blocks.
    - The two new tests write `MyNFT.sol` and `MyMulti.sol` to the per-test tmpDir, verify compile-verify ran (no throw), and confirm the version-line footer is present.
    - `npx vitest run tests/commands/create.compile.spec.ts` reports all tests green (existing + 2 new = total count increased by 2).
    - No changes to existing tests in this file.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/commands/create.compile.spec.ts</automated>
  </verify>
  <done>E2E happy-path coverage extended to ERC-721 + ERC-1155; the dispatcher pipeline is exercised end-to-end through the full pipeline (registry → wizard → generate → compileVerify → fs.writeFile).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| smartc boot → registry | Three `register*Template()` calls before `buildProgram()`; the registry's `register()` throws on duplicate id (Phase 1 D-08), so a programming error (e.g., accidentally re-registering the same id) surfaces at boot |
| E2E test → full dispatcher pipeline | The new tests prime @clack mocks then drive the entire create command via `program.parseAsync` — the same path a real user takes |
| Integration test → real solc + real OZ | No mocks; the 5 new fixtures compile through the production Phase 3 gate |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04-01 | Tampering | src/cli.ts boot wiring | accept | 4-line change; registry rejects duplicate ids; tests/registry.spec.ts three-template test catches mis-wiring |
| T-04-04-02 | Tampering | Fixture file paths in compile.integration.spec.ts | mitigate | Paths are relative to `tests/compiler/`; readFileSync throws ENOENT if any fixture is missing — surfaces in CI before merge |
| T-04-04-03 | DoS | Integration corpus growth | accept | 5 new fixtures × ~1-2s solc compile per fixture ≈ 7-10s added to the suite. Suite still under the 18s target from 04-VALIDATION.md (Phase 3 baseline ~10s) |
| T-04-04-04 | Repudiation | E2E test reliability under @clack mock pattern | mitigate | Phase 2 + 3 already exercise this pattern in the same file; the two new tests reuse the existing `captureStdio` + `exitOverride` + `parseAsync` scaffolding without modification |
| T-04-04-SC | Tampering | No new package installs | accept | This plan modifies only test wiring + boot wiring; zero new packages |
</threat_model>

<verification>
- `src/cli.ts` shows exactly +4 lines, -0 lines, modified-0 lines vs. the prior commit (`git diff --stat src/cli.ts`).
- `tests/registry.spec.ts` three-template registration test runs (no `it.skip` for this case); `npx vitest run tests/registry.spec.ts` all green.
- `tests/compiler/compile.integration.spec.ts` runs the 7-row `describe.each` block + 2 error-path tests; `npx vitest run tests/compiler/compile.integration.spec.ts` all green.
- `tests/commands/create.compile.spec.ts` runs the existing 3 tests + 2 new tests; `npx vitest run tests/commands/create.compile.spec.ts` all green.
- After `npm run build`: `node dist/cli.js list-templates` outputs 3 rows (erc20, erc721, erc1155) — captured for the manual-verification table in 04-VALIDATION.md.
- Full suite: `npx vitest run` and `npm run typecheck` clean.
- `git diff --name-only` shows only the four files in `files_modified` were touched.
</verification>

<success_criteria>
- SC-1 (ROADMAP §Phase 4): User can generate ERC-721 with configurable name+symbol+baseUri+opt-ins — verified by the ERC-721 happy-path E2E test writing a real .sol file.
- SC-2: User can opt in to EIP-2981 royalties — wired through plans 04-01 (royalty.ts) + 04-02 (wizard prompt pair + generate conditional) + 04-04 (compile-integration covers the with-royalty fixture).
- SC-3: User can generate ERC-1155 with configurable URI + opt-ins — verified by the ERC-1155 happy-path E2E test.
- SC-4: Conditional access-control prompts fire under Mintable OR Pausable for both templates — covered by plans 04-02 + 04-03 wizard.spec.ts unit tests; not re-tested here (would require complex mock priming).
- SC-5: All three Solidity templates pass compile-verify with their full option matrices — verified by the extended compile.integration.spec.ts (7 fixture rows: 2 erc20 + 3 erc721 + 2 erc1155).
- Plugin model verified additive-only: file-set check shows only `src/cli.ts` (4 lines) + `src/commands/create.ts` (1 line, in plan 04-01) + the entirely-new `src/templates/erc721/` + `src/templates/erc1155/` + test additions were modified. NO changes to `src/compiler/`, `src/registry/`, `src/lib/`, `src/templates/erc20/`, `src/program.ts`, `src/commands/list-templates.ts`.
</success_criteria>

<output>
Create `.planning/phases/04-erc-721-and-erc-1155-templates/04-04-SUMMARY.md` when done.
Include: final phase-wide file-set diff (which directories were touched, which were NOT — the additive-only model verification); full-suite test count after Phase 4 vs. before (Phase 3 baseline); `node dist/cli.js list-templates` output snapshot; a checklist mapping ERC721-01..05 + ERC1155-01..05 + SC-1..SC-5 to the plan that satisfied each.
</output>
