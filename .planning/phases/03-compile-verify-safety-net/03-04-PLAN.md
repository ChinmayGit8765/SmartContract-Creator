---
phase: 03
plan: 04
type: execute
wave: 3
depends_on:
  - 03-03
files_modified:
  - src/compiler/README.md
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
files_modified_optional: []
autonomous: true
requirements:
  - COMP-01
  - COMP-03
  - COMP-04
  - COMP-05
tags:
  - docs
  - roadmap
  - finalization
  - wave-3

must_haves:
  truths:
    - "Future maintainer can read `src/compiler/README.md` and understand: why solc-js + standard JSON, where the import callback lives, why the broken/warns fixtures exist, how to bump pinned versions and refresh tests"
    - "ROADMAP.md Phase 3 entry shows the final plan count (4) and lists all four PLAN.md files with brief objectives"
    - "REQUIREMENTS.md traceability table marks COMP-01, COMP-03, COMP-04, COMP-05 as Complete (Phase 3 finalized)"
  artifacts:
    - path: "src/compiler/README.md"
      provides: "One-page docs covering architecture, where things live, how to bump versions"
      min_lines: 30
    - path: ".planning/ROADMAP.md"
      provides: "Finalized Phase 3 plan listing"
      contains: "03-01-PLAN.md"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Updated traceability for Phase 3 requirements"
      contains: "Complete"
  key_links:
    - from: ".planning/ROADMAP.md Phase 3 entry"
      to: ".planning/phases/03-compile-verify-safety-net/"
      via: "plan-list shows all four PLAN.md files"
      pattern: "03-04-PLAN\\.md"
---

<objective>
Wrap Phase 3 with the discretionary `src/compiler/README.md` (CONTEXT recommends yes — see the
Claude's Discretion section), finalize ROADMAP.md Phase 3 metadata (plan count, plan list), and
mark Phase 3 requirements as complete in REQUIREMENTS.md traceability table.

Purpose: The README lowers the cost of every future phase touching this module (Phase 4 adds
ERC-721/1155 against the same gate; Phase 7 adds SPL via the same `compileVerify` seam; Phase 8
adds the AI sandbox-compile flow). Without it, every future plan re-researches "why solc-js,
why standard JSON, why this fixture, where to bump versions". The ROADMAP/REQUIREMENTS edits
unlock `/gsd:execute-phase` → `/gsd:verify-work` advancement.

Output: One README, a finalized ROADMAP entry for Phase 3, four COMP-* requirements marked
complete in the traceability table.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-compile-verify-safety-net/03-CONTEXT.md
@.planning/phases/03-compile-verify-safety-net/03-RESEARCH.md
@.planning/phases/03-compile-verify-safety-net/03-01-PLAN.md
@.planning/phases/03-compile-verify-safety-net/03-02-PLAN.md
@.planning/phases/03-compile-verify-safety-net/03-03-PLAN.md
@src/compiler/index.ts
@src/compiler/imports.ts
@src/compiler/types.ts

<interfaces>
ROADMAP.md Phase 3 entry currently reads (per ROADMAP.md lines 63-77):
```
### Phase 3: Compile-Verify Safety Net
**Goal**: User never receives a file that doesn't compile — the Solidity compile gate runs in-process before any source touches disk, against pinned OpenZeppelin and `solc` versions.
**Depends on**: Phase 2
**Requirements**: COMP-01, COMP-02, COMP-03, COMP-04, COMP-05
**Success Criteria** (what must be TRUE):
  1. User runs `smartc create` for ERC-20 and the generated file is compile-verified before being written
  ...
**Plans**: TBD

Plans:
- [ ] 03-01: TBD
```

REQUIREMENTS.md traceability rows for COMP-01..05 currently read (lines 181-185):
```
| COMP-01 | Phase 3 | Pending |
| COMP-02 | Phase 7 | Pending |
| COMP-03 | Phase 3 | Pending |
| COMP-04 | Phase 3 | Pending |
| COMP-05 | Phase 3 | Pending |
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author src/compiler/README.md — one-page architecture + maintenance doc</name>
  <files>src/compiler/README.md</files>
  <read_first>
    - .planning/phases/03-compile-verify-safety-net/03-CONTEXT.md Claude's Discretion section (CONTEXT recommends yes: "one short page covering why solc-js + standard JSON, where the import callback lives, why the deliberate-fail fixture exists, how to bump pinned versions and refresh snapshots")
    - .planning/phases/03-compile-verify-safety-net/03-RESEARCH.md §Pitfall 2 lines 438-448 (evmVersion pinning rationale — must be documented for future bumps)
    - .planning/phases/03-compile-verify-safety-net/03-RESEARCH.md §Pitfall 4 lines 462-469 (ESM↔CJS interop rationale — important for future contributors)
    - .planning/phases/03-compile-verify-safety-net/03-RESEARCH.md §Don't Hand-Roll lines 411-422 (anti-patterns the codebase already avoids)
    - .planning/phases/03-compile-verify-safety-net/03-PATTERNS.md §No Analog Found lines 643-646 (README is greenfield; no template to copy)
    - src/compiler/index.ts (final state — Plan 02 implemented)
    - src/compiler/imports.ts (final state — Plan 02 implemented)
    - src/compiler/types.ts (Plan 01 type definitions)
    - .planning/phases/03-compile-verify-safety-net/03-01-SUMMARY.md, 03-02-SUMMARY.md, 03-03-SUMMARY.md (Plan 01/02/03 deliverables — informs the "Maintenance" section's "what to refresh when bumping" list)
  </read_first>
  <behavior>
    - File `src/compiler/README.md` exists, between 30 and 200 lines, with these required sections (use `## H2` headings):
      1. `## Overview` — One paragraph naming the module's purpose: "In-process Solidity compile gate. Throws `CliError(E_COMPILE_FAILED)` on errors, surfaces warnings via `output.warn`, never blocks writing on warnings (COMP-04)." Names the COMP-01..COMP-05 requirements covered.
      2. `## Architecture` — Bullet list naming each file in `src/compiler/` and what it owns: `index.ts` owns `compileVerify(source, chain)`, `imports.ts` owns the bundled-OZ resolver + path-traversal guard, `types.ts` owns `CompileDiagnostic` + `StandardJsonInput` + `SolcOutput`. One paragraph explaining the seam shape (`compileVerify(source, chain)` is chain-agnostic; Phase 7 plugs in the SPL anchor branch).
      3. `## Why solc-js + Standard JSON` — Explain the choice: official Ethereum Foundation package, pure-JS (emscripten libsolc), no native binary distribution, no system-solc requirement (COMP-05). Reference RESEARCH §Standard Stack for verified provenance.
      4. `## Why we pin evmVersion: "paris"` — Explain Pitfall 2: solc 0.8.30+ defaults shift across releases (prague → osaka); pinning paris keeps bytecode targeting broad-chain-compatible EVM. Reference the Hardhat/Foundry precedent.
      5. `## Why the import callback is synchronous` — Explain Pitfall 1: solc-js callbacks MUST be sync; returning a Promise breaks silently with "File not found" diagnostics. Type signature `(path: string) => { contents: string } | { error: string }` enforces this.
      6. `## Why cwd-independence matters` — Explain Pitfall 3: `require.resolve("@openzeppelin/contracts/package.json")` finds smartc's bundled OZ regardless of where the user runs `smartc`. Reference `scripts/probe-compile.mjs` as the Wave 0 cwd-independence canary.
      7. `## Test fixtures` — Explain each fixture's role: `tests/fixtures/erc20/bare-default.sol` + `all-flags-on.sol` (Phase 2 goldens — OZ-drift canary across version bumps), `tests/fixtures/broken.sol` (D-14 deliberate-fail — verifies the error path), `tests/fixtures/warns-no-error.sol` (Pitfall 6 — verifies warnings surface without blocking).
      8. `## Bumping pinned versions` — Step-by-step: (a) update `package.json` exact pins, (b) run `node scripts/probe-compile.mjs` and confirm PROBE PASSED, (c) run `npx vitest run` and confirm `tests/compiler/compile.integration.spec.ts` is still green against the new pins (the goldens are the canary), (d) update `tests/version.spec.ts` exact-pin assertions, (e) commit with `chore(03): bump solc to X.Y.Z` style message.
      9. `## Phase forward-looking notes` — Brief: Phase 4 (ERC-721/1155) reuses this gate without modification — the import callback's `@openzeppelin/contracts/` prefix match covers all OZ namespaces. Phase 7 plugs in the SPL anchor branch (Solana). Phase 8 (AI-patch) reuses the sandbox-compile semantic — same gate, same error code.
    - File uses Markdown headings, lists, fenced code blocks (only short snippets — full implementations are NOT duplicated here)
    - File mentions specific pinned versions (`solc@0.8.35`, `@openzeppelin/contracts@5.6.1`) and the stable error code (`E_COMPILE_FAILED`) for find-ability
    - File has no broken internal links (any references to other files use relative paths that exist on disk)
  </behavior>
  <action>
    Write `src/compiler/README.md` per the behavior section. Open with an H1 `# src/compiler — Solidity compile-verify gate` followed by the eight H2 sections above.

    Each section should be 1-3 short paragraphs or a focused list. Avoid restating the implementation — link to the source files instead (relative paths: `./index.ts`, `./imports.ts`, `./types.ts`).

    Include one short fenced code block under §Architecture showing the public seam signature (the `compileVerify` declaration from `types.ts` perspective — no body), so a future reader sees the contract at a glance. No full implementations in fenced blocks — that's what the source files are for.

    Cite specific RESEARCH/CONTEXT decisions where they unlock understanding (e.g., "(see CONTEXT D-08 for the WHAT/WHY/FIX rendering rule)"). Keep the doc terse — target ≤150 lines.

    Sanity-check: `node -e "const c=require('node:fs').readFileSync('src/compiler/README.md','utf8'); ['Overview','Architecture','Why solc-js','Why we pin evmVersion','Why the import callback','Why cwd-independence','Test fixtures','Bumping pinned versions','Phase forward'].forEach(h=>{if(!c.includes(h)){console.error('missing section:',h); process.exit(1)}}); console.log('OK')"` — exits 0.
  </action>
  <acceptance_criteria>
    - File `src/compiler/README.md` exists
    - File is between 30 and 200 lines (`wc -l` or equivalent)
    - File contains the literal section headings (case-sensitive substring match): `Overview`, `Architecture`, `Why solc-js`, `Why we pin evmVersion`, `Why the import callback`, `Why cwd-independence`, `Test fixtures`, `Bumping pinned versions`, `Phase forward`
    - File mentions the specific pins: `0.8.35` for solc and `5.6.1` for `@openzeppelin/contracts`
    - File mentions the stable error code `E_COMPILE_FAILED`
    - File references at least three of the four sibling files via relative paths: `./index.ts`, `./imports.ts`, `./types.ts`, or `../../scripts/probe-compile.mjs`
  </acceptance_criteria>
  <verify>
    <automated>node -e "const fs=require('node:fs'); if(!fs.existsSync('src/compiler/README.md')) process.exit(1); const c=fs.readFileSync('src/compiler/README.md','utf8'); const lines=c.split(/\\r?\\n/).length; if(lines < 30 || lines > 200){console.error('line count out of range:',lines); process.exit(1)}; ['Overview','Architecture','Why solc-js','Why we pin evmVersion','Why the import callback','Why cwd-independence','Test fixtures','Bumping pinned versions','Phase forward'].forEach(h=>{if(!c.includes(h)){console.error('missing section:',h); process.exit(1)}}); if(!c.includes('0.8.35')) process.exit(1); if(!c.includes('5.6.1')) process.exit(1); if(!c.includes('E_COMPILE_FAILED')) process.exit(1); console.log('OK')"</automated>
  </verify>
  <done>One-page README documents the architecture, key decisions (evmVersion, sync callback, cwd-independence), test fixtures, and version-bump procedure; future phases inherit context without re-research.</done>
</task>

<task type="auto">
  <name>Task 2: Finalize ROADMAP.md Phase 3 plan listing and update REQUIREMENTS.md traceability</name>
  <files>.planning/ROADMAP.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/ROADMAP.md lines 63-77 (Phase 3 entry with `**Plans**: TBD` and the placeholder `- [ ] 03-01: TBD` line)
    - .planning/REQUIREMENTS.md lines 181-185 (COMP-01..05 traceability rows; COMP-01/03/04/05 currently marked Pending, COMP-02 maps to Phase 7)
    - .planning/REQUIREMENTS.md lines 208-216 (the cross-phase notes section explaining COMP-02's Phase 7 mapping)
    - .planning/phases/03-compile-verify-safety-net/03-01-PLAN.md, 03-02-PLAN.md, 03-03-PLAN.md (all three plans' objectives — extract one-line summaries for the ROADMAP plan list)
    - .planning/ROADMAP.md lines 169-179 (the Progress table; Phase 3 row currently shows `0/TBD` and `Not started`)
  </read_first>
  <behavior>
    - `.planning/ROADMAP.md` Phase 3 entry is updated:
      - `**Plans**: TBD` becomes `**Plans**: 4 plans in 4 waves`
      - The placeholder `Plans:\n- [ ] 03-01: TBD` block is replaced with a four-item list, each prefixed `- [ ]` (still unchecked — execution hasn't happened yet), naming each PLAN file and a brief objective:
        - `- [ ] 03-01-PLAN.md — Wave 0: install pinned deps, run probe, scaffold compiler skeleton + broken/warns fixtures`
        - `- [ ] 03-02-PLAN.md — Wave 1: implement compileVerify + bundled-OZ resolver + unit/integration tests`
        - `- [ ] 03-03-PLAN.md — Wave 2: splice into dispatcher + E2E happy/fail tests + version-line flip`
        - `- [ ] 03-04-PLAN.md — Wave 3: src/compiler/README.md + ROADMAP/REQUIREMENTS finalization`
    - The Phase 3 row in the Progress table (lines 169-179) is updated: `0/TBD` → `0/4`. Status stays `Not started` (this PLAN is part of planning, not execution — the executor flips status after each plan completes).
    - `.planning/REQUIREMENTS.md` traceability rows for COMP-01, COMP-03, COMP-04, COMP-05 stay `Pending` (execution hasn't shipped yet; the verifier marks them Complete after `/gsd:verify-work`). HOWEVER, the planner-time action is to ensure the rows are correctly mapped to Phase 3 — review and confirm each row's Phase column reads `Phase 3`. COMP-02's row stays `Phase 7` (the cross-phase note at line 211 explains why; do not move it).
    - No other lines in ROADMAP.md or REQUIREMENTS.md are modified.
  </behavior>
  <action>
    **Edit `.planning/ROADMAP.md`:**

    1. Locate the Phase 3 entry (line 63: `### Phase 3: Compile-Verify Safety Net`)
    2. Replace `**Plans**: TBD` (line 73) with `**Plans**: 4 plans in 4 waves`
    3. Replace the placeholder `Plans:` block (lines 75-77) with a markdown `Plans:` heading followed by four unchecked list items (`- [ ]` prefix). Write the four list items in this exact order to keep the wave structure visible: (a) `03-01-PLAN.md — Wave 0: install pinned deps, run probe, scaffold compiler skeleton + broken/warns fixtures`; (b) `03-02-PLAN.md — Wave 1: implement compileVerify + bundled-OZ resolver + unit/integration tests`; (c) `03-03-PLAN.md — Wave 2: splice into dispatcher + E2E happy/fail tests + version-line flip`; (d) `03-04-PLAN.md — Wave 3: src/compiler/README.md + ROADMAP/REQUIREMENTS finalization`.
    4. Locate the Progress table (around line 170-179). Update the Phase 3 row's "Plans Complete" column from `0/TBD` to `0/4`. Leave the Status as `Not started` and Completed as `-`. (Executor updates these as each plan ships.)

    **Edit `.planning/REQUIREMENTS.md`:**

    1. Scan lines 181-185 (the COMP-01..05 rows). Confirm:
       - `COMP-01 | Phase 3 | Pending` — keep as-is
       - `COMP-02 | Phase 7 | Pending` — keep as-is (cross-phase note line 211)
       - `COMP-03 | Phase 3 | Pending` — keep as-is
       - `COMP-04 | Phase 3 | Pending` — keep as-is
       - `COMP-05 | Phase 3 | Pending` — keep as-is
    2. No row edits needed at planning time. (The verifier flips Pending → Complete after `/gsd:verify-work` runs.)
    3. If REQUIREMENTS.md has a "Last updated" line at the bottom (line ~216), append a note: `*Phase 3 planning: 4 plans created; COMP-01..05 mapped (COMP-02 cross-phase to Phase 7 per existing note).*`

    Verify the edits with two greps:
    - `grep -c "03-04-PLAN.md" .planning/ROADMAP.md` returns ≥1
    - `grep -c "4 plans in 4 waves" .planning/ROADMAP.md` returns ≥1

    Do not touch any other phase entries.
  </action>
  <acceptance_criteria>
    - `.planning/ROADMAP.md` Phase 3 entry contains the literal `**Plans**: 4 plans in 4 waves`
    - `.planning/ROADMAP.md` lists all four PLAN.md filenames under Phase 3: `03-01-PLAN.md`, `03-02-PLAN.md`, `03-03-PLAN.md`, `03-04-PLAN.md`
    - `.planning/ROADMAP.md` Phase 3 row in the Progress table shows `0/4` (not `0/TBD`)
    - `.planning/ROADMAP.md` has no entries showing `03-01: TBD` (the placeholder is gone)
    - `.planning/REQUIREMENTS.md` traceability rows for COMP-01, COMP-03, COMP-04, COMP-05 still read `Phase 3` (mapping unchanged)
    - `.planning/REQUIREMENTS.md` COMP-02 row still reads `Phase 7` (cross-phase note preserved)
    - No phases other than Phase 3 are modified in ROADMAP.md (only the Phase 3 entry block + the Phase 3 row in the Progress table)
  </acceptance_criteria>
  <verify>
    <automated>node -e "const fs=require('node:fs'); const r=fs.readFileSync('.planning/ROADMAP.md','utf8'); if(!r.includes('4 plans in 4 waves')) process.exit(1); ['03-01-PLAN.md','03-02-PLAN.md','03-03-PLAN.md','03-04-PLAN.md'].forEach(f=>{if(!r.includes(f)){console.error('missing:',f); process.exit(1)}}); if(r.includes('03-01: TBD')) process.exit(1); if(!r.includes('| 3. Compile-Verify Safety Net | 0/4 |')) process.exit(1); const q=fs.readFileSync('.planning/REQUIREMENTS.md','utf8'); ['| COMP-01 | Phase 3','| COMP-03 | Phase 3','| COMP-04 | Phase 3','| COMP-05 | Phase 3','| COMP-02 | Phase 7'].forEach(t=>{if(!q.includes(t)){console.error('missing trace row:',t); process.exit(1)}}); console.log('OK')"</automated>
  </verify>
  <done>ROADMAP Phase 3 finalized: 4 plans named in 4 waves; REQUIREMENTS traceability rows confirmed; execution can begin via /gsd:execute-phase 03.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| documentation → future maintainers | README content shapes future Phase 4/7/8 plans; misleading copy compounds across the project |
| ROADMAP/REQUIREMENTS state → orchestrator tools | `/gsd:execute-phase` and `/gsd:verify-work` parse these files; malformed entries break tooling |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-13 | Tampering | README.md content drift | accept | README is intentionally light (≤200 lines); drift is bounded; "Bumping pinned versions" section explicitly tells future contributors what to refresh when pins change. CONTEXT D-discretion recommends one-page scope. |
| T-03-14 | Repudiation | ROADMAP/REQUIREMENTS untracked drift | mitigate | Plan 04 Task 2 acceptance criteria includes specific literal-string checks for the Phase 3 row state; the verifier in `/gsd:verify-work` re-validates after execution. |
| T-03-15 | Information Disclosure | README accidentally embeds secrets | accept | README contains no auth-related content (CLI tool, no secret handling); no risk surface. |
</threat_model>

<verification>
**Phase requirements addressed:**

- COMP-01, COMP-03, COMP-04, COMP-05 — all four are mapped to Phase 3 plans in REQUIREMENTS.md traceability (mapping locked in this task; verifier marks Complete after `/gsd:verify-work`)
- COMP-02 — stays mapped to Phase 7 per the long-standing cross-phase note (REQUIREMENTS.md line 211); Phase 3 ships the SEAM SHAPE (compileVerify chain-agnostic interface) but the SPL anchor implementation belongs to Phase 7.

**Final phase-level checks (after Plan 04 + Plan 03 execution):**
```
npx vitest run                                   # full suite green
npm run typecheck && npm run build               # clean
node dist/cli.js --version                       # contains "solc 0.8.35" + "@openzeppelin/contracts 5.6.1"
node scripts/probe-compile.mjs                   # PROBE PASSED, errors=0 (still works)
grep -c "0/4" .planning/ROADMAP.md               # Phase 3 row updated
```

**Note:** No production source code changes in this plan — Task 1 adds docs only; Task 2 edits planning artifacts only. No new test cases. The verifier (`/gsd:verify-work`) reads ROADMAP/REQUIREMENTS to validate the phase landed all five SC items.
</verification>

<success_criteria>
- [ ] `src/compiler/README.md` exists with all 8 required H2 sections, mentions both pinned versions (`0.8.35`, `5.6.1`) and the stable error code (`E_COMPILE_FAILED`), and is 30-200 lines
- [ ] `.planning/ROADMAP.md` Phase 3 entry shows `**Plans**: 4 plans in 4 waves` with the four PLAN.md files listed (unchecked `- [ ]` since execution hasn't happened yet)
- [ ] `.planning/ROADMAP.md` Progress table Phase 3 row shows `0/4` (replaces `0/TBD`)
- [ ] `.planning/REQUIREMENTS.md` traceability rows for COMP-01, COMP-03, COMP-04, COMP-05 read `Phase 3` (mapping preserved); COMP-02 row reads `Phase 7` (cross-phase mapping preserved)
- [ ] No phases other than Phase 3 are modified in ROADMAP.md or REQUIREMENTS.md
</success_criteria>

<output>
Create `.planning/phases/03-compile-verify-safety-net/03-04-SUMMARY.md` when done, including:

- Files created/modified (paths + brief)
- Confirmation that ROADMAP/REQUIREMENTS edits are scoped to Phase 3 only (no churn on other phases)
- Note that `/gsd:execute-phase 03` is now ready to run (all four PLAN.md files exist, ROADMAP finalized)
- Reminder that CLAUDE.md's phase-completion push contract applies: orchestrator runs `git push` to origin AFTER Phase 3 is marked complete in ROADMAP.md/STATE.md (this plan does NOT include a push task)
</output>
