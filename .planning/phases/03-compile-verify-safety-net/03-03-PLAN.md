---
phase: 03
plan: 03
type: execute
wave: 2
depends_on:
  - 03-02
files_modified:
  - src/commands/create.ts
  - tests/commands/create.spec.ts
  - tests/commands/create.compile.spec.ts
  - tests/commands/create.compile-fail.spec.ts
  - tests/version.spec.ts
files_modified_optional: []
autonomous: true
requirements:
  - COMP-01
  - COMP-03
  - COMP-04
  - COMP-05
tags:
  - dispatcher
  - splice
  - e2e
  - tdd
  - wave-2

must_haves:
  truths:
    - "User running `smartc create --template erc20` triggers compileVerify before the file is written to disk (SC-1)"
    - "When compileVerify throws, the file is NOT written to disk (SC-2 / COMP-03 / D-15 load-bearing)"
    - "When compileVerify returns warnings, the warnings appear on stderr AND the file IS written (SC-3 / COMP-04)"
    - "The post-write footer mentions the real pinned solc + @openzeppelin/contracts versions (D-12)"
    - "`node dist/cli.js --version` output contains `solc 0.8.35` and `@openzeppelin/contracts 5.6.1` (SC-5 / COMP-05 user-facing surface)"
  artifacts:
    - path: "src/commands/create.ts"
      provides: "Dispatcher with compileVerify spliced at line 95 + D-12 footer update"
      contains: "compileVerify(source, tpl.chain)"
    - path: "tests/commands/create.compile.spec.ts"
      provides: "E2E happy path — real solc, real fixture, file written, footer shows real versions, warnings pass through"
      min_lines: 80
    - path: "tests/commands/create.compile-fail.spec.ts"
      provides: "D-15 load-bearing E2E — broken template injected, dispatcher throws E_COMPILE_FAILED, NO file written, WHY block contains solc diagnostic"
      min_lines: 60
    - path: "tests/version.spec.ts"
      provides: "Flipped from `not bundled` to real-version assertions for solc + @oz/contracts"
      contains: "solc 0.8.35"
  key_links:
    - from: "src/commands/create.ts (line ~95)"
      to: "src/compiler/index.ts"
      via: "import { compileVerify } from '../compiler/index.js'; const { warnings } = await compileVerify(source, tpl.chain);"
      pattern: "compileVerify\\(source,\\s*tpl\\.chain\\)"
    - from: "src/commands/create.ts (footer)"
      to: "src/lib/version.ts"
      via: "safeReadVersion('solc') and safeReadVersion('@openzeppelin/contracts')"
      pattern: "Compile-verified against solc"
    - from: "tests/commands/create.compile-fail.spec.ts"
      to: "src/registry/index.ts"
      via: "clear() then register(brokenTemplate)"
      pattern: "register\\(brokenTemplate"
---

<objective>
Splice `compileVerify(source, tpl.chain)` into the `src/commands/create.ts` dispatcher at the
Phase 2 marker (line 95), update the post-write footer to surface the real pinned versions
(D-12), and land the two E2E tests that prove the gate works end-to-end through the dispatcher:
- `create.compile.spec.ts` — happy path with real solc + real OZ; file written; warnings pass through; footer mentions versions
- `create.compile-fail.spec.ts` — D-15 load-bearing: broken template injected via registry; dispatcher throws E_COMPILE_FAILED; NO file written; WHY contains solc diagnostic

Also flip `tests/version.spec.ts` from the Phase 2 "not bundled" assertions to the Phase 3 real-version
assertions (the user-facing SC-5 surface — Plan 01 installed the deps so `safeReadVersion`
already returns the real values; we just need to update the assertions to match).

Purpose: This plan delivers SC-1, SC-2 (COMP-03), SC-3 (COMP-04), and SC-5 (COMP-05 user-facing
surface). Plan 02 proved the compile pipeline works in isolation; this plan proves it works
through the actual user-facing command surface.

Output: A `smartc create --template erc20` invocation now writes a compile-verified file, with
diagnostics on failure and warnings on partial success; the `--version` line surfaces the pinned
versions for SC-5.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-compile-verify-safety-net/03-CONTEXT.md
@.planning/phases/03-compile-verify-safety-net/03-RESEARCH.md
@.planning/phases/03-compile-verify-safety-net/03-PATTERNS.md
@.planning/phases/03-compile-verify-safety-net/03-01-PLAN.md
@.planning/phases/03-compile-verify-safety-net/03-02-PLAN.md
@src/commands/create.ts
@src/compiler/index.ts
@src/lib/errors.ts
@src/lib/version.ts
@src/registry/types.ts
@src/registry/index.ts
@tests/commands/create.spec.ts
@tests/version.spec.ts

<interfaces>
Contracts Plan 02 produced that this plan consumes:

- `compileVerify(source: string, chain: "evm" | "solana"): Promise<{ warnings: CompileDiagnostic[] }>` exported from `src/compiler/index.ts`
  - `chain="solana"` throws CliError(E_NOT_IMPLEMENTED)
  - `chain="evm"` + compile errors throws CliError(E_COMPILE_FAILED) with multi-line WHY (formattedMessages + version tail)
  - `chain="evm"` + compile clean returns `{ warnings: [] }`
  - `chain="evm"` + compile warnings only returns `{ warnings: [...] }`, no throw

- From `src/registry/index.ts`: `register(template)` (throws on duplicate id), `clear()` (test-only isolation), `get(id)`
- From `src/registry/types.ts`: `Template<TOpts>` with `runWizard?`, `generate?`, `chain: "evm"|"solana"|"any"`
- From `src/lib/version.ts`: `safeReadVersion("solc")` returns `"0.8.35"`, `safeReadVersion("@openzeppelin/contracts")` returns `"5.6.1"` after Plan 01 install

Existing splice marker at `src/commands/create.ts:95` reads `// ◄─── PHASE 3 SPLICE POINT: compileVerify(source, tpl.chain) inserts HERE per UI-SPEC §Coordination Seams ───►`.

Existing footer at `src/commands/create.ts:110-111`: a `nextStep` for list-templates plus a "Phase 3 will add automatic compile-verify before write..." placeholder line.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Splice compileVerify into src/commands/create.ts at line 95 and update D-12 post-write footer</name>
  <files>src/commands/create.ts, tests/commands/create.spec.ts</files>
  <read_first>
    - src/commands/create.ts (current state has splice marker at line 95 and Phase 2 footer at lines 110-111)
    - .planning/phases/03-compile-verify-safety-net/03-PATTERNS.md §`src/commands/create.ts` lines 184-219 (exact splice content + new imports + footer replacement)
    - .planning/phases/03-compile-verify-safety-net/03-RESEARCH.md §Code Examples lines 711-733 (verbatim splice body + footer)
    - .planning/phases/03-compile-verify-safety-net/03-CONTEXT.md D-06, D-07, D-10, D-12 (signature, order, warning channel, footer wording)
    - .planning/phases/02-erc-20-canary-template/02-VERIFICATION.md lines 49, 67 (confirms one and only one splice marker present at create.ts:95)
    - tests/commands/create.spec.ts lines 221-225 (the "Phase 3 splice point comment present" test — currently expects exactly ONE match; after Plan 03 this becomes ZERO matches because the splice replaces the marker)
  </read_first>
  <behavior>
    - The line at `src/commands/create.ts:95` containing the literal `PHASE 3 SPLICE POINT` comment is REPLACED by the compileVerify call + warning forEach + newbie explain. The phrase `PHASE 3 SPLICE POINT` no longer appears anywhere in the file.
    - Two new imports added near the top of the file:
      - `import { compileVerify } from "../compiler/index.js";`
      - `import { safeReadVersion } from "../lib/version.js";` (needed for the new footer)
    - The splice body runs `const { warnings } = await compileVerify(source, tpl.chain);` IMMEDIATELY after `const { filename, source } = tpl.generate(opts);` (current line 93). This puts compile BEFORE `outPath` resolution and BEFORE `confirmOverwrite` (D-07 order: runWizard → generate → compileVerify → confirmOverwrite → writeFile).
    - After the compileVerify call: a `for (const w of warnings) { output.warn(w.formattedMessage); }` loop surfaces all warnings via the locked `output.warn` channel (D-10)
    - After the warning loop: a `newbie`-gated `output.explain("Warnings don't block deployment but often point at latent bugs. Review each before shipping.")` call. The explicit `newbie &&` gate matches CONTEXT D-10 wording; functionally `output.explain` is already a newbie-only no-op (output.ts:53) but the explicit gate makes intent obvious to future readers.
    - The post-write footer at lines 110-111 is REPLACED with the D-12 wording: a `nextStep` line reading `Compile-verified against solc <X.Y.Z> + @openzeppelin/contracts <A.B.C>.` (interpolated from `safeReadVersion` calls), followed by the existing `Run 'smartc list-templates' to see other templates.` line. The old "Phase 3 will add automatic compile-verify..." line is REMOVED entirely.
    - `npm run typecheck` and `npm run build` both clean after the edit.
    - The "Phase 3 splice point comment present" test at `tests/commands/create.spec.ts:221-225` is updated to assert ZERO marker occurrences AND at least one `compileVerify(source, tpl.chain)` occurrence. Other tests in the file continue to pass (the happy path now exercises real solc against the bare-default fixture which Plan 02 verified compiles clean; overwrite-refused and --force tests still hit their assertions because compile passes for bare-default).
  </behavior>
  <action>
    Open `src/commands/create.ts`. Make the following targeted edits:

    **1. Imports — add two lines** after the existing imports block (after line 10):
    - `import { compileVerify } from "../compiler/index.js";`
    - `import { safeReadVersion } from "../lib/version.js";`

    **2. Splice at line 95 — replace the marker comment** with active code per RESEARCH lines 717-724:
    - Delete the line containing `// ◄─── PHASE 3 SPLICE POINT: compileVerify(source, tpl.chain) inserts HERE per UI-SPEC §Coordination Seams ───►`
    - Insert in its place (preserving the 4-space indent that puts these statements inside the `.action()` body):
      - A one-line traceability comment: `// Phase 3 — compile-verify gate (D-06, D-07, D-10): throws CliError(E_COMPILE_FAILED) on errors; warnings surface via output.warn.`
      - `const { warnings } = await compileVerify(source, tpl.chain);`
      - `for (const w of warnings) { output.warn(w.formattedMessage); }`
      - `if (warnings.length > 0 && newbie) { output.explain("Warnings don't block deployment but often point at latent bugs. Review each before shipping."); }`

    **3. Footer replacement at lines 110-111** — per RESEARCH lines 727-730 and CONTEXT D-12:
    - Locate the two `output.nextStep(...)` lines after `output.result(\`Wrote ${outPath}\`)`
    - REPLACE BOTH lines with the D-12 wording. The new sequence is:
      - `const solcVer = safeReadVersion("solc") ?? "unknown";`
      - `const ozVer = safeReadVersion("@openzeppelin/contracts") ?? "unknown";`
      - `output.nextStep(\`Compile-verified against solc ${solcVer} + @openzeppelin/contracts ${ozVer}.\`);`
      - `output.nextStep("Run 'smartc list-templates' to see other templates.");`
    - The original "Phase 3 will add automatic compile-verify..." line is DELETED entirely.

    **4. Update the existing splice-marker test** at `tests/commands/create.spec.ts:221-225`:
    - Rewrite the test body to assert (a) ZERO occurrences of the literal `PHASE 3 SPLICE POINT` in `src/commands/create.ts`, AND (b) AT LEAST ONE occurrence of `compileVerify(source, tpl.chain)` in the same file
    - Update the test name to reflect the new contract, e.g., `"Phase 3 splice landed in src/commands/create.ts (marker consumed, compileVerify wired)"`

    **5. Verify the file still type-checks and builds:** Run `npm run typecheck` (expect clean) and `npm run build` (expect clean). Then run `npx vitest run tests/commands/create.spec.ts` — the happy-path test now exercises real solc-js (must still pass since the fixture compiles clean per Plan 02 Task 3); the splice-marker test was just updated. The wizard-cancel test still passes (the wizard throws before generate runs, so compile is never reached). The overwrite-refused and --force tests still pass because compile of the bare-default ERC-20 succeeds, and the overwrite gate runs after compile per D-07.

    The Phase 1/2 test files outside `tests/commands/create.spec.ts` are not touched by this task.
  </action>
  <acceptance_criteria>
    - `src/commands/create.ts` contains the literal `import { compileVerify } from "../compiler/index.js";`
    - `src/commands/create.ts` contains the literal `import { safeReadVersion } from "../lib/version.js";`
    - `src/commands/create.ts` contains the literal `await compileVerify(source, tpl.chain)`
    - `src/commands/create.ts` does NOT contain the string `PHASE 3 SPLICE POINT` (case-sensitive search)
    - `src/commands/create.ts` contains the literal `Compile-verified against solc`
    - `src/commands/create.ts` does NOT contain the string `Phase 3 will add automatic compile-verify`
    - The `await compileVerify` line appears at a SOURCE-FILE INDEX earlier than the `existsSync(outPath)` line (D-07 ordering)
    - `tests/commands/create.spec.ts` splice-marker test is updated: zero marker occurrences asserted, ≥1 `compileVerify(source, tpl.chain)` asserted
    - `npm run typecheck` exits 0
    - `npm run build` exits 0
    - `npx vitest run tests/commands/create.spec.ts` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck &amp;&amp; npm run build &amp;&amp; npx vitest run tests/commands/create.spec.ts</automated>
  </verify>
  <done>compileVerify call wired into the dispatcher at line 95 in the correct order per D-07, footer updated to D-12 wording, splice-marker test updated, full typecheck+build+create.spec.ts green.</done>
</task>

<task type="auto">
  <name>Task 2: Author tests/commands/create.compile.spec.ts — E2E happy path with real solc + warning pass-through</name>
  <files>tests/commands/create.compile.spec.ts</files>
  <read_first>
    - tests/commands/create.spec.ts (the exact analog — copy structure, helpers, mock setup; this file becomes the second e2e suite alongside it)
    - .planning/phases/03-compile-verify-safety-net/03-PATTERNS.md §`tests/commands/create.compile.spec.ts` lines 351-381 (test pattern + footer assertion + warning fixture seam)
    - .planning/phases/03-compile-verify-safety-net/03-RESEARCH.md §Validation Architecture lines 519-531 (e2e test commands per requirement)
    - .planning/phases/03-compile-verify-safety-net/03-CONTEXT.md D-12 (footer wording), D-10 (warning channel)
    - .planning/phases/03-compile-verify-safety-net/03-02-SUMMARY.md (Plan 02's compile-time measurement and warns-no-error fixture validation — if Plan 02 confirmed the fixture surfaces warnings under solc 0.8.35, this task asserts they propagate through the dispatcher)
    - src/commands/create.ts (the now-spliced dispatcher — confirm the splice is in place before writing this test)
    - tests/fixtures/erc20/bare-default.sol (the happy-path input — wizard mocks will be primed to match this fixture)
    - tests/fixtures/warns-no-error.sol (Plan 01 fixture — feeds the warning-pass-through test via a test-only template)
  </read_first>
  <behavior>
    - File `tests/commands/create.compile.spec.ts` exists with a `describe("create dispatcher — compile-verify E2E")` block
    - The file copies the helper structure from `tests/commands/create.spec.ts:1-67`: `vi.mock("@clack/prompts", ...)`, `const { buildProgram } = await import("../../src/program.js")`, `const { clear, register } = await import("../../src/registry/index.js")`, `const { registerErc20Template } = await import("../../src/templates/erc20/index.js")`, the `captureStdout` helper, the `primeHappyPathMocks` helper, and the `beforeEach/afterEach` `clear()/registerErc20Template()/mkdtempSync/rmSync` skeleton
    - Three `it()` blocks:
      1. `"happy path with real solc — file is compile-verified, written to disk, and footer shows real solc + @oz/contracts versions"` — primes happy-path mocks; runs `program.exitOverride().parseAsync(["create", "--template", "erc20", "--newbie", "--out", outPath], { from: "user" })`; asserts (a) `existsSync(outPath) === true`, (b) written content contains `contract MyToken`, (c) captured stdout contains `Wrote ${outPath}`, (d) captured stdout contains `Compile-verified against solc` and `@openzeppelin/contracts` (the D-12 footer with REAL versions). The `--newbie` flag is required for `nextStep` lines to surface (newbie-gated channel per output.ts:53).
      2. `"warning pass-through — register test-only template returning warns-no-error source; dispatcher writes file AND emits warning on stderr"` — registers a test-only template (similar to D-15's broken template but returning the warns-no-error fixture content), parses the create command pointed at it, asserts (a) file written, (b) `output.warn` was invoked (captured stderr contains a `warn:` prefix and the warning's formattedMessage substring like `Warning`), (c) exit code 0. The test-only template uses the `clear()` + `register()` pattern from `tests/commands/create.spec.ts:58-72`.
      3. `"--version line surfaces real pinned versions (SC-5 user-facing surface)"` — calls `safeReadVersion("solc")` and `safeReadVersion("@openzeppelin/contracts")` directly (no need to spawn dist); asserts both return the exact strings `"0.8.35"` and `"5.6.1"`. Then constructs `formatVersionLine()` and asserts it contains `solc 0.8.35` and `@openzeppelin/contracts 5.6.1` and does NOT contain `solc not bundled` / `@openzeppelin/contracts not bundled`. (Task 4 also updates the version.spec.ts file to lock the same assertions; this test is the E2E mirror — both can coexist.)
    - All three tests run real solc under the hood — runtime per Plan 02's measurement: budget ~1-3 seconds for the suite
  </behavior>
  <action>
    Create `tests/commands/create.compile.spec.ts` by copying the helper preamble from `tests/commands/create.spec.ts:1-67` (the `vi.mock("@clack/prompts")` block, the top-level await imports of `buildProgram`/`clear`/`registerErc20Template`/`clack`, the typed mock handles, the `captureStdout` helper, the `primeHappyPathMocks` helper, the `beforeEach/afterEach` clean-up scaffolding). This is intentional duplication — both e2e suites need the same primitives.

    Add the three `it()` blocks per the behavior section. Specifically:

    **Test 1 (happy path with footer):**
    1. `primeHappyPathMocks();`
    2. `const outPath = join(tmpDir, "MyToken.sol");`
    3. `const program = buildProgram();`
    4. `const captured = await captureStdout(async () => { await program.exitOverride().parseAsync(["create", "--template", "erc20", "--newbie", "--out", outPath], { from: "user" }); });`
    5. `expect(existsSync(outPath)).toBe(true);`
    6. `const written = readFileSync(outPath, "utf8"); expect(written).toContain("contract MyToken");`
    7. `expect(captured).toContain(\`Wrote ${outPath}\`);`
    8. `expect(captured).toContain("Compile-verified against solc");`
    9. `expect(captured).toContain("@openzeppelin/contracts");`
    10. `expect(captured).not.toContain("solc unknown");` (sanity: safeReadVersion succeeded)

    **Test 2 (warning pass-through via test-only template):** Declare an object literal `warnsTemplate` at test-file scope with these fields: `id: "warns-test-only"`, `name: "Warns (test only)"`, `chain: "evm" as const`, `status: "stub" as const`, `description: "DO NOT USE — surfaces a deliberate solc warning"`, `async runWizard() { return {}; }`, and a `generate()` method that calls `readFileSync(join(__dirname, "../fixtures/warns-no-error.sol"), "utf8")` and returns `{ filename: "Warns.sol", source: <read-content> }`. Same shape pattern as the broken template defined in Task 3 of this plan, just pointed at the warns fixture instead.
    In the test body: `clear(); register(warnsTemplate as never);` (clears the erc20 registration from beforeEach and replaces with the warns template), then `await program.exitOverride().parseAsync(["create", "--template", "warns-test-only", "--out", outPath], { from: "user" });`, then assert `existsSync(outPath) === true`, `captured.toLowerCase().includes("warn:")` (the locked output.warn prefix from output.ts:51). If Plan 02 SUMMARY confirmed the warns fixture surfaces a specific warning type (e.g., "Unused local variable"), assert that substring too.

    **Test 3 (--version surface):** `import { safeReadVersion, formatVersionLine } from "../../src/lib/version.js";` at top of file (alongside existing imports), then `expect(safeReadVersion("solc")).toBe("0.8.35"); expect(safeReadVersion("@openzeppelin/contracts")).toBe("5.6.1"); const line = formatVersionLine(); expect(line).toContain("solc 0.8.35"); expect(line).toContain("@openzeppelin/contracts 5.6.1"); expect(line).not.toContain("solc not bundled"); expect(line).not.toContain("@openzeppelin/contracts not bundled");`

    For ESM `__dirname` resolution in the file, mirror `tests/compiler/compile.integration.spec.ts` from Plan 02: `const __filename = fileURLToPath(import.meta.url); const __dirname = dirname(__filename);`

    Run `npx vitest run tests/commands/create.compile.spec.ts` — all 3 tests must pass.
  </action>
  <acceptance_criteria>
    - File `tests/commands/create.compile.spec.ts` exists
    - File contains a `describe("create dispatcher — compile-verify E2E")` block with exactly 3 `it()` blocks
    - File imports `compileVerify` is NOT necessary (this is an E2E test that exercises the dispatcher; compileVerify is consumed transitively via create.ts)
    - File imports `safeReadVersion` and `formatVersionLine` from `../../src/lib/version.js` for test 3
    - File uses the `@clack/prompts` mock pattern from `tests/commands/create.spec.ts:8-17`
    - Test 1 includes assertions: `expect(captured).toContain("Compile-verified against solc")` AND `expect(captured).toContain("@openzeppelin/contracts")` AND `expect(existsSync(outPath)).toBe(true)` AND `expect(written).toContain("contract MyToken")`
    - Test 2 registers a test-only template returning the warns-no-error fixture and asserts both `existsSync(outPath) === true` AND `captured` contains the `warn:` prefix (output.warn locked prefix per output.ts:51)
    - Test 3 asserts `safeReadVersion("solc")` is exactly `"0.8.35"` and `safeReadVersion("@openzeppelin/contracts")` is exactly `"5.6.1"`
    - Running `npx vitest run tests/commands/create.compile.spec.ts` exits 0 with 3 tests passing
    - Runtime under 5 seconds for the file alone
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/commands/create.compile.spec.ts</automated>
  </verify>
  <done>E2E happy-path coverage green: compile-verified file written, D-12 footer surfaced, warning pass-through proven, SC-5 version surface locked.</done>
</task>

<task type="auto">
  <name>Task 3: Author tests/commands/create.compile-fail.spec.ts — D-15 load-bearing: broken template, no file written, CliError structure</name>
  <files>tests/commands/create.compile-fail.spec.ts</files>
  <read_first>
    - tests/commands/create.spec.ts (overall structure + wizard-cancel test at lines 159-176 as the closest analog for "throws + file not written")
    - .planning/phases/03-compile-verify-safety-net/03-PATTERNS.md §`tests/commands/create.compile-fail.spec.ts` lines 385-472 (full test seam + assertions)
    - .planning/phases/03-compile-verify-safety-net/03-RESEARCH.md §Test-only broken-source seam lines 737-779 (the registry-injected template approach — CONTEXT specifics path (a), cleanest seam)
    - .planning/phases/03-compile-verify-safety-net/03-CONTEXT.md D-14 (broken fixture), D-15 (load-bearing assertions: rejects with E_COMPILE_FAILED, file absent, WHY shape)
    - .planning/phases/03-compile-verify-safety-net/03-RESEARCH.md §Open Question 1 lines 811-815 (registry-injection rationale — cleaner than entangling @clack mocks)
    - tests/fixtures/broken.sol (Plan 01 fixture — the source the test-only template returns)
    - src/registry/index.ts (the clear() / register() / get() surface used by the test-only seam)
    - src/lib/errors.ts (CliError structure assertions hit in this test)
  </read_first>
  <behavior>
    - File `tests/commands/create.compile-fail.spec.ts` exists with a `describe("create dispatcher — compile-fail path (D-15)")` block
    - The file uses the same `@clack/prompts` mock + top-level await import pattern as `create.spec.ts` and `create.compile.spec.ts`
    - The file defines an inline test-only template `brokenTemplate` whose `generate()` returns the broken.sol content (read from `tests/fixtures/broken.sol`). The template has `id: "broken-test-only"`, `chain: "evm"`, `status: "stub"`, `runWizard: async () => ({})`, `generate: () => ({ filename: "Broken.sol", source: <broken-content> })`.
    - The `beforeEach` clears the registry and registers ONLY the broken template (do not also register erc20 — keeps the test surface minimal and avoids ambiguity)
    - Two `it()` blocks (D-15 splits into the structural assertions and the user-message assertions):
      1. `"D-15 (a)+(b): rejects with E_COMPILE_FAILED and writes NO file"` — runs `await expect(program.exitOverride().parseAsync(["create", "--template", "broken-test-only", "--out", outPath], { from: "user" })).rejects.toMatchObject({ code: "E_COMPILE_FAILED", exitCode: 1 });` then `expect(existsSync(outPath)).toBe(false);` (load-bearing — the file MUST NOT exist on disk)
      2. `"D-15 (c): rendered error contains WHAT/WHY/FIX shape with solc ParserError in WHY and version tail"` — wraps the call in try/catch to inspect fields: `expect(err.what).toBe("Generated source failed to compile."); expect(err.why).toContain("ParserError"); expect(err.why).toContain("Compile errors come from solc"); expect(err.fix).toContain("please report this");` (the structural assertions from CONTEXT D-08's locked WHAT/WHY/FIX wording)
    - The test never spawns `dist/cli.js` — it runs `buildProgram().exitOverride().parseAsync(...)` in-process per RESEARCH Open Question 1's recommendation (line 813: "in-process command spec; no dist spawn")
  </behavior>
  <action>
    Create `tests/commands/create.compile-fail.spec.ts` following the structure from PATTERNS lines 387-471:

    1. Mirror the imports + mocks block from `tests/commands/create.spec.ts:1-22`:
       - `import { describe, it, expect, beforeEach, afterEach, vi } from "vitest";`
       - `import { existsSync, mkdtempSync, rmSync, readFileSync } from "node:fs";`
       - `import { join, dirname } from "node:path";`
       - `import { tmpdir } from "node:os";`
       - `import { fileURLToPath } from "node:url";`
       - `vi.mock("@clack/prompts", () => ({ text: vi.fn(), select: vi.fn(), multiselect: vi.fn(), confirm: vi.fn(), isCancel: vi.fn(() => false), cancel: vi.fn() }));`
       - `const { buildProgram } = await import("../../src/program.js");`
       - `const { clear, register } = await import("../../src/registry/index.js");`
       - `const __filename = fileURLToPath(import.meta.url); const __dirname = dirname(__filename);`

    2. Define the test-only template inline (declared at module scope, BEFORE the `describe`):
       - `id: "broken-test-only"`, `name: "Broken (test only)"`, `chain: "evm" as const`, `status: "stub" as const`, `description: "DO NOT USE — deliberately broken for compile-fail tests"`
       - `runWizard: async () => ({})`
       - `generate: () => ({ filename: "Broken.sol", source: readFileSync(join(__dirname, "../fixtures/broken.sol"), "utf8") })`
       - Cast as needed to satisfy `Template<unknown>` at the `register()` call site (per PATTERNS line 430: `register(brokenTemplate as never)`)

    3. `describe("create dispatcher — compile-fail path (D-15)")` block:
       - `let tmpDir: string;`
       - `beforeEach(() => { clear(); register(brokenTemplate as never); tmpDir = mkdtempSync(join(tmpdir(), "smartc-compile-fail-")); });`
       - `afterEach(() => { rmSync(tmpDir, { recursive: true, force: true }); clear(); });`

    4. Test 1 — `"D-15 (a)+(b): rejects with E_COMPILE_FAILED and writes NO file"`:
       - `const outPath = join(tmpDir, "Broken.sol");`
       - `const program = buildProgram();`
       - `await expect(program.exitOverride().parseAsync(["create", "--template", "broken-test-only", "--out", outPath], { from: "user" })).rejects.toMatchObject({ code: "E_COMPILE_FAILED", exitCode: 1 });`
       - `expect(existsSync(outPath)).toBe(false);` — **this is the load-bearing assertion**

    5. Test 2 — `"D-15 (c): rendered error contains WHAT/WHY/FIX shape with solc ParserError in WHY and version tail"`: Mirror the try/catch field-inspection pattern from `tests/commands/create.spec.ts:104-113`. Concretely: wrap the same `buildProgram().exitOverride().parseAsync(...)` call in a try; if the call returns (no throw), explicitly `throw new Error("expected throw")` to fail the test. In the catch block, type-narrow the caught value as `{ code: string; what: string; why: string; fix: string }` and assert five `expect` checks: `err.code === "E_COMPILE_FAILED"`, `err.what === "Generated source failed to compile."` (one-line WHAT per D-08), `err.why` contains the substring `"ParserError"` (solc emits this for the `uint256 x = ;` syntax error), `err.why` contains the substring `"Compile errors come from solc"` (the locked version-tail line per D-08), `err.fix` contains the substring `"please report this"` (the locked FIX wording per RESEARCH lines 701-702).

    Run `npx vitest run tests/commands/create.compile-fail.spec.ts` — both tests must pass. Runtime ~1-2 seconds (one real solc compile of a small broken source).
  </action>
  <acceptance_criteria>
    - File `tests/commands/create.compile-fail.spec.ts` exists
    - File contains a `describe("create dispatcher — compile-fail path (D-15)")` block with exactly 2 `it()` blocks
    - File defines a `brokenTemplate` object literal with `id: "broken-test-only"`, `chain: "evm"`, `status: "stub"`, and `generate` reading from `../fixtures/broken.sol`
    - File uses `vi.mock("@clack/prompts", ...)` per the locked Vitest 4 ESM pattern
    - File does NOT spawn `dist/cli.js` (no `spawn` / `execFile` / `execSync` calls)
    - Test 1 includes both `rejects.toMatchObject({ code: "E_COMPILE_FAILED", exitCode: 1 })` AND `expect(existsSync(outPath)).toBe(false)` — both load-bearing per D-15
    - Test 2 asserts at least four field-shape checks: `err.code === "E_COMPILE_FAILED"`, `err.what === "Generated source failed to compile."`, `err.why.includes("ParserError")`, `err.why.includes("Compile errors come from solc")`, `err.fix.includes("please report this")`
    - Running `npx vitest run tests/commands/create.compile-fail.spec.ts` exits 0 with 2 tests passing
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/commands/create.compile-fail.spec.ts</automated>
  </verify>
  <done>D-15 load-bearing E2E green: broken source → dispatcher throws E_COMPILE_FAILED → no file on disk → CliError WHY block contains solc diagnostic with version tail and locked FIX wording.</done>
</task>

<task type="auto">
  <name>Task 4: Flip tests/version.spec.ts from "not bundled" assertions to real-version assertions (SC-5 automated lock)</name>
  <files>tests/version.spec.ts</files>
  <read_first>
    - tests/version.spec.ts (current Phase 2 state — has the "returns null for solc" tests at lines 15-21 and the "reports the two Phase-3-gated deps as 'not bundled'" test at lines 40-44 that have been failing since Plan 01 installed the deps)
    - .planning/phases/03-compile-verify-safety-net/03-PATTERNS.md §`tests/version.spec.ts` lines 552-578 (exact replacement assertions)
    - .planning/phases/03-compile-verify-safety-net/03-RESEARCH.md §Code Examples lines 555-577 (analog: how the @openzeppelin/wizard segment test pattern at lines 23-27 already locks a real version with an exact-pin assertion)
    - .planning/phases/03-compile-verify-safety-net/03-CONTEXT.md D-11 (formatVersionLine auto-rolls forward; no version.ts code edit needed, just spec edit)
    - .planning/phases/03-compile-verify-safety-net/03-01-PLAN.md `<verification>` block (lists the expected version.spec.ts failures this task resolves)
    - src/lib/version.ts (the formatVersionLine output the spec asserts against — confirm the format is unchanged so the per-segment toContain checks remain valid)
  </read_first>
  <behavior>
    - Lines 15-21 of `tests/version.spec.ts` (two `it` cases asserting `safeReadVersion("solc") === null` and `safeReadVersion("@openzeppelin/contracts") === null`) are REPLACED with two cases asserting the deps now return non-null semver strings, mirroring the existing @oz/wizard pattern at lines 23-27
    - Lines 40-44 (the `formatVersionLine` "not bundled" case) is REPLACED with a case asserting the line contains `solc 0.8.35` and `@openzeppelin/contracts 5.6.1` AND does NOT contain the "not bundled" sentinels
    - The remaining tests are unchanged (the `commander` test at 9-13, the `smartc 0.0.0` test at 54-60, the `@openzeppelin/wizard` exact-pin tests at 23-27 and 46-52, the regex-shape test at 35-38)
    - The exact pin assertions reference the same string literals as Plan 01's package.json install: `0.8.35` for solc and `5.6.1` for `@openzeppelin/contracts`. Note PATTERNS line 578: drift = deliberate commit, not churn (same convention as the wizard `0.10.8` lock at line 50)
    - Running `npx vitest run tests/version.spec.ts` exits 0 — the spec is now consistent with Plan 01's install state
  </behavior>
  <action>
    Edit `tests/version.spec.ts`:

    **Replace lines 15-21** (the two "returns null" cases) with:
    - `it("returns the installed solc version in Phase 3 (now a dependency)", () => { const v = safeReadVersion("solc"); expect(v).not.toBeNull(); expect(v).toMatch(/^\d+\.\d+\.\d+/); });`
    - `it("returns the installed @openzeppelin/contracts version in Phase 3 (now a dependency)", () => { const v = safeReadVersion("@openzeppelin/contracts"); expect(v).not.toBeNull(); expect(v).toMatch(/^\d+\.\d+\.\d+/); });`

    **Replace lines 40-44** (the `formatVersionLine` "not bundled" case) with:
    - `it("includes solc + @openzeppelin/contracts segments with their exact pinned versions in Phase 3 (UI-16 + SC-5)", () => {`
    - `  const line = formatVersionLine();`
    - `  // Plan 01 pinned solc@0.8.35 and @openzeppelin/contracts@5.6.1 (both exact). Drift = deliberate commit (same convention as wizard@0.10.8 lock).`
    - `  expect(line).toContain("solc 0.8.35");`
    - `  expect(line).toContain("@openzeppelin/contracts 5.6.1");`
    - `  expect(line).not.toContain("solc not bundled");`
    - `  expect(line).not.toContain("@openzeppelin/contracts not bundled");`
    - `});`

    Do NOT touch the other tests in the file (the `commander` semver-shape test, the @openzeppelin/wizard tests, the regex-format test, the "smartc 0.0.0" assertion).

    Run `npx vitest run tests/version.spec.ts` — all 7 (or so) tests in the file must pass. Then run `npx vitest run` (full suite) — every test in the project must be green at this point.

    Finally, run `npm run build && node dist/cli.js --version` and capture the output. Expected: `smartc 0.1.0 (solc 0.8.35, @openzeppelin/contracts 5.6.1, @openzeppelin/wizard 0.10.8)`. This is the user-facing SC-5 surface — record the exact output in the task SUMMARY.
  </action>
  <acceptance_criteria>
    - `tests/version.spec.ts` no longer contains the literal string `returns null for solc` (the Phase 2 assertion is replaced)
    - `tests/version.spec.ts` no longer contains the literal string `returns null for @openzeppelin/contracts`
    - `tests/version.spec.ts` no longer contains the literal string `solc not bundled` in any test body assertion (it appears at most inside `.not.toContain("solc not bundled")` calls — that's fine, but the old positive assertion is gone)
    - `tests/version.spec.ts` contains the literal `expect(line).toContain("solc 0.8.35")`
    - `tests/version.spec.ts` contains the literal `expect(line).toContain("@openzeppelin/contracts 5.6.1")`
    - `npx vitest run tests/version.spec.ts` exits 0
    - `npx vitest run` (full suite) exits 0
    - `npm run build && node dist/cli.js --version` prints a line containing `solc 0.8.35` and `@openzeppelin/contracts 5.6.1`
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/version.spec.ts &amp;&amp; npx vitest run &amp;&amp; npm run build</automated>
  </verify>
  <done>tests/version.spec.ts assertions reflect Plan 01's install state; full vitest suite green; --version output surfaces real pinned versions (SC-5 user-facing surface locked).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user CLI args → registry lookup | `--template <id>` resolves a registry entry; registry has only the test-only template in compile-fail tests |
| template.generate → compileVerify | Generated source string crosses into solc.compile (Plan 02's resolver + traversal guard apply) |
| compileVerify CliError → user stderr | The locked WHAT/WHY/FIX block is the user-facing error surface; WHY contains solc-emitted formattedMessage verbatim |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-09 | Tampering | Registry overwrite via duplicate `register()` | accept | `register()` throws on duplicate id per Phase 1 contract (STATE.md line 75); the test-only `brokenTemplate` registration occurs ONLY after `clear()` per the locked test-isolation seam (registry.spec.ts pattern). No cross-test leakage possible. |
| T-03-10 | Information Disclosure | Compile-failure WHY contains source file:line:col | accept | The WHY block exposes the user's own generated source location (Contract.sol:N:M) — this IS the user-facing diagnostic surface (CONTEXT D-08). No PII risk for a code-scaffolding tool. |
| T-03-11 | DoS | Real-solc E2E tests slow down CI | accept | Plan 02's measurements (recorded in 03-02-SUMMARY.md) inform whether E2E suite stays under the 15s sampling-rate budget per VALIDATION.md. The 3+2 new E2E tests + 4 integration tests + 12 unit tests collectively budget ~5-8 seconds; well under the 15s budget. |
| T-03-12 | Repudiation | Compile-error reports omit version provenance | mitigate | Task 1's footer + WHY-tail line both interpolate `safeReadVersion("solc")` and `safeReadVersion("@openzeppelin/contracts")` — so reproducible bug reports include the exact pins. Task 4 locks the user-facing --version line to the same versions. |
</threat_model>

<verification>
**Phase requirements addressed (E2E + user-facing surface):**

- COMP-01 (in-process solc compile) — Task 1 wires it through the dispatcher; Task 2 happy-path proves end-to-end
- COMP-03 (no file on compile failure) — Task 3 D-15 load-bearing test asserts both `rejects.toMatchObject({ code: "E_COMPILE_FAILED" })` AND `existsSync(outPath) === false`
- COMP-04 (warnings surface but do not block) — Task 2's warning pass-through test asserts file IS written AND `output.warn` was invoked
- COMP-05 (user-facing visibility) — Task 4 locks the `--version` line to the exact pinned versions; Task 1's footer also surfaces them

**SC-1..SC-5 coverage:**
- SC-1 ("file is compile-verified before being written") — Task 1 source-order assertion (compile BEFORE existsSync) + Task 2 happy-path end-to-end
- SC-2 ("compile fail → user sees diagnostics → no file on disk") — Task 3 D-15 load-bearing
- SC-3 ("compile warns → user sees warnings → file still written") — Task 2 warning pass-through
- SC-4 ("imports resolve from bundled deps, no user install") — Plan 02's integration tests prove this; Task 2's E2E happy path exercises the full pipeline with bundled deps in play
- SC-5 ("pinned versions visible somewhere user-facing") — Task 1 footer + Task 4 --version line both surface them

**Run after this plan completes:**
```
npx vitest run                  # full suite, must be 100% green
npm run typecheck               # clean
npm run build                   # clean
node dist/cli.js --version      # contains "solc 0.8.35" and "@openzeppelin/contracts 5.6.1"
```
</verification>

<success_criteria>
- [ ] `src/commands/create.ts` has `compileVerify` spliced at the former marker location; source-order puts compile BEFORE `existsSync(outPath)` (D-07)
- [ ] `src/commands/create.ts` post-write footer reads "Compile-verified against solc X.Y.Z + @openzeppelin/contracts A.B.C" (D-12)
- [ ] `tests/commands/create.spec.ts` splice-marker test updated to assert zero marker occurrences + ≥1 compileVerify call
- [ ] `tests/commands/create.compile.spec.ts` exists with 3 passing E2E tests (happy + warning + version-surface)
- [ ] `tests/commands/create.compile-fail.spec.ts` exists with 2 passing E2E tests (D-15 (a)+(b) and D-15 (c))
- [ ] `tests/version.spec.ts` flipped to assert real `0.8.35` + `5.6.1` versions; full vitest suite green
- [ ] `npm run build && node dist/cli.js --version` output contains both `solc 0.8.35` and `@openzeppelin/contracts 5.6.1`
</success_criteria>

<output>
Create `.planning/phases/03-compile-verify-safety-net/03-03-SUMMARY.md` when done, including:

- Files modified (paths + brief)
- Test counts before/after: Phase 2 baseline was 136 passing + 1 skipped; after Plan 03 expect 136 + ~9 new (3 + 2 + minor adjustment for splice-marker test rewrite + Task 4 net delta) — confirm actual numbers
- Captured output of `node dist/cli.js --version` (the user-facing SC-5 surface — pasted verbatim into the SUMMARY for evidence)
- Total suite runtime (`time npx vitest run`) — confirms we're within the 15s sampling budget
- Any test flakes observed (none expected; flag for Plan 04 if so)
</output>
