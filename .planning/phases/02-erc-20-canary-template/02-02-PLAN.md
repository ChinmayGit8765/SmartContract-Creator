---
phase: 02-erc-20-canary-template
plan: 02
type: execute
wave: 1
depends_on: [02-01]
files_modified:
  - src/templates/erc20/validators.ts
  - src/templates/erc20/filename.ts
  - src/templates/erc20/generate.ts
  - tests/templates/erc20/validators.spec.ts
  - tests/templates/erc20/filename.spec.ts
  - tests/templates/erc20/generate.spec.ts
  - tests/fixtures/erc20/bare-default.sol
  - tests/fixtures/erc20/all-flags-on.sol
autonomous: true
requirements: [ERC20-01, ERC20-02, ERC20-03, ERC20-04, ERC20-05]
tags: [erc20, validators, openzeppelin, snapshots, pure-functions]

must_haves:
  truths:
    - "isSolidityIdentifier / isAsciiSymbol / isNonNegativeDecimal return undefined for valid inputs and the UI-SPEC-locked error strings for invalid inputs"
    - "contractNameToFilename derives a `<PascalCase>.sol` filename from any user input, with fallback `Token.sol` for empty/all-symbol input"
    - "generate(opts) returns { filename, source } where source is the byte-exact `@openzeppelin/wizard@0.10.8` `erc20.print()` output for opts mapped 1:1 (with the premint-0 rule from Wave 0 Probe B applied)"
    - "Two golden snapshot fixtures (bare-default.sol, all-flags-on.sol) are committed and lock the wizard output byte-for-byte"
    - "Per-flag assertions cover burnable / mintable+ownable / mintable+roles / pausable+roles / premint>0 / SPDX header (axis-by-axis without 2^N snapshots, per D-09/D-10)"
    - "All seven validator test-case-table rows from RESEARCH §Validators are green"
    - "All eight filename test-case-table rows from RESEARCH §Filename Derivation are green"
  artifacts:
    - path: "src/templates/erc20/validators.ts"
      provides: "Three @clack/prompts validator callbacks with UI-SPEC-locked error strings"
      contains: "isSolidityIdentifier"
    - path: "src/templates/erc20/filename.ts"
      provides: "contractNameToFilename pure function"
      contains: "contractNameToFilename"
    - path: "src/templates/erc20/generate.ts"
      provides: "generate(opts) thin wrapper over erc20.print"
      contains: "erc20.print"
    - path: "tests/fixtures/erc20/bare-default.sol"
      provides: "Golden snapshot — bare ERC-20 (name+symbol+supply, no flags)"
      contains: "contract MyToken"
    - path: "tests/fixtures/erc20/all-flags-on.sol"
      provides: "Golden snapshot — all flags + access:roles"
      contains: "AccessControl"
  key_links:
    - from: "src/templates/erc20/generate.ts"
      to: "@openzeppelin/wizard"
      via: "erc20.print (import form from Wave 0 Probe A)"
      pattern: "erc20\\.print"
    - from: "src/templates/erc20/generate.ts"
      to: "src/templates/erc20/filename.ts"
      via: "contractNameToFilename(opts.name)"
      pattern: "contractNameToFilename"
    - from: "tests/templates/erc20/generate.spec.ts"
      to: "tests/fixtures/erc20/bare-default.sol"
      via: "toMatchFileSnapshot"
      pattern: "toMatchFileSnapshot"
---

<objective>
Build the pure-function half of the ERC-20 template plugin: validators, filename derivation, and the thin `generate()` wrapper around `@openzeppelin/wizard.erc20.print()`. Lock the output against two committed golden snapshots and per-flag axis assertions.

Purpose: Delivers Requirements ERC20-01..05 at the SOURCE-CODE-GENERATION layer (the wizard plugin in Plan 03 connects user input to these functions). Per D-01 / D-02, generate is a thin wrapper — we own the validators, filename, and snapshot lock; OZ owns the actual Solidity source.

Output:
- `src/templates/erc20/validators.ts` — three @clack-shaped validator callbacks with UI-SPEC-locked error strings.
- `src/templates/erc20/filename.ts` — `contractNameToFilename` pure function with the locked algorithm.
- `src/templates/erc20/generate.ts` — thin wrapper around `erc20.print()`, applying the premint-"0" rule from Wave 0 Probe B, returning `{ filename, source }` per D-04.
- Two committed `.sol` snapshot fixtures (D-09) that lock byte-for-byte against the wizard output.
- Filled-in spec files (Wave 0 todos → real assertions) for validators, filename, generate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-erc-20-canary-template/02-CONTEXT.md
@.planning/phases/02-erc-20-canary-template/02-RESEARCH.md
@.planning/phases/02-erc-20-canary-template/02-PATTERNS.md
@.planning/phases/02-erc-20-canary-template/02-UI-SPEC.md
@.planning/phases/02-erc-20-canary-template/02-VALIDATION.md
@.planning/phases/02-erc-20-canary-template/02-01-PLAN.md
@.planning/phases/02-erc-20-canary-template/02-WAVE0-PROBES.md
@src/templates/erc20/opts.ts
@tests/env.spec.ts

<interfaces>
<!-- Types created by Plan 01. -->

From src/templates/erc20/opts.ts (Plan 01):
```
export interface Erc20Opts {
  readonly name: string;
  readonly symbol: string;
  readonly premint: string;
  readonly mintable: boolean;
  readonly burnable: boolean;
  readonly pausable: boolean;
  readonly access: false | "ownable" | "roles";
}
export interface GenerateResult { readonly filename: string; readonly source: string; }
```

From @openzeppelin/wizard (consumed by generate.ts; exact import form chosen in Wave 0 Probe A):
```
erc20.print(opts: ERC20Options): string
// ERC20Options accepts: name, symbol, premint?, mintable?, burnable?, pausable?, access?, info?
// access values: false | "ownable" | "roles" | "managed" — we never pass "managed"
```

@clack/prompts validator contract (consumed by validators.ts callers in Plan 03):
```
type ClackValidator = (value: string | undefined) => string | undefined;
// Returns undefined on valid; returns the error message string on invalid.
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement validators.ts with three @clack-shaped validators and fill validators.spec.ts</name>
  <files>src/templates/erc20/validators.ts, tests/templates/erc20/validators.spec.ts</files>
  <read_first>
    - .planning/phases/02-erc-20-canary-template/02-RESEARCH.md (§Validators ~lines 451-509 — locked regexes and locked error strings)
    - .planning/phases/02-erc-20-canary-template/02-UI-SPEC.md (Prompts 1, 2, 3 tables ~lines 84-119 — validator failure strings are LOCKED; paste byte-exact)
    - .planning/phases/02-erc-20-canary-template/02-PATTERNS.md (§src/templates/erc20/validators.ts — locked regexes and signature; §tests/templates/erc20/validators.spec.ts — table-driven shape)
    - tests/env.spec.ts (canonical pure-function table-driven test style)
  </read_first>
  <behavior>
    - isSolidityIdentifier(undefined | "") → "Contract name is required."
    - isSolidityIdentifier("MyToken") → undefined
    - isSolidityIdentifier("3Token") → "Must start with a letter or underscore, contain only letters/digits/underscores, max 64 chars."
    - isSolidityIdentifier("a".repeat(65)) → same error message (over length)
    - isAsciiSymbol(undefined | "") → "Token symbol is required."
    - isAsciiSymbol("MTK") → undefined
    - isAsciiSymbol("MT K") → "Must be 1-11 ASCII letters/digits, no spaces or punctuation."
    - isNonNegativeDecimal(undefined | "") → "Initial supply is required (use 0 for no premint)."
    - isNonNegativeDecimal("0") → undefined
    - isNonNegativeDecimal("1000000") → undefined
    - isNonNegativeDecimal("1.5") → undefined
    - isNonNegativeDecimal("-1") → "Must be a non-negative decimal number, e.g. 1000000 or 1.5."
    - All three return-undefined-on-valid contract is preserved (matches @clack `validate` callback shape).
  </behavior>
  <action>
    Create `src/templates/erc20/validators.ts` with three exports matching the @clack `validate` callback signature `(v: string | undefined) => string | undefined`.

    Regexes (LOCKED — paste verbatim from RESEARCH §Validators lines ~458-491):
    - `SOLIDITY_IDENTIFIER = /^[A-Za-z_][A-Za-z0-9_]{0,63}$/` — Solidity grammar minus `$` for cross-language hygiene (RESEARCH Assumption A8).
    - `ASCII_SYMBOL = /^[A-Za-z0-9]{1,11}$/` — ERC-20 practical 1-11 char convention (Assumption A9).
    - `DECIMAL_STRING = /^(?:0|[1-9]\d*)(?:\.\d+)?$/` — non-negative decimal, no exponent.

    Error strings (LOCKED — UI-SPEC §Prompts 1/2/3 tables):
    - Solidity identifier empty: "Contract name is required."
    - Solidity identifier malformed: "Must start with a letter or underscore, contain only letters/digits/underscores, max 64 chars."
    - Symbol empty: "Token symbol is required."
    - Symbol malformed: "Must be 1-11 ASCII letters/digits, no spaces or punctuation."
    - Premint empty: "Initial supply is required (use 0 for no premint)."
    - Premint malformed: "Must be a non-negative decimal number, e.g. 1000000 or 1.5."

    No throws — these are pure predicates. JSDoc each export with a one-line description per PATTERNS §validators.ts.

    Then REPLACE the `it.todo` placeholders in `tests/templates/erc20/validators.spec.ts` with real assertions matching the RESEARCH §Validators test-case table (lines ~496-508). Use three `describe` blocks (one per validator) with table-driven `it.each` or one `it` per input class. Every row of the table is a test row.
  </action>
  <verify>
    <automated>npx vitest run tests/templates/erc20/validators.spec.ts</automated>
    <automated>node -e "const t=require('fs').readFileSync('src/templates/erc20/validators.ts','utf8'); for (const k of ['SOLIDITY_IDENTIFIER','ASCII_SYMBOL','DECIMAL_STRING','isSolidityIdentifier','isAsciiSymbol','isNonNegativeDecimal','Contract name is required','Token symbol is required','Initial supply is required','Must start with a letter','Must be 1-11 ASCII','Must be a non-negative decimal']) if (!t.includes(k)) {console.error('missing:',k); process.exit(1)} console.log('ok')"</automated>
  </verify>
  <done>
    All validators.spec.ts cases pass (zero `it.todo` remaining for validators). All six UI-SPEC-locked error strings are present byte-exact in src/templates/erc20/validators.ts. `npm run typecheck` clean.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement filename.ts (contractNameToFilename) and fill filename.spec.ts</name>
  <files>src/templates/erc20/filename.ts, tests/templates/erc20/filename.spec.ts</files>
  <read_first>
    - .planning/phases/02-erc-20-canary-template/02-RESEARCH.md (§Filename Derivation ~lines 510-542 — locked algorithm + test-case table)
    - .planning/phases/02-erc-20-canary-template/02-PATTERNS.md (§src/templates/erc20/filename.ts — pure-function module shape; §tests/templates/erc20/filename.spec.ts — table-driven shape)
    - .planning/phases/02-erc-20-canary-template/02-CONTEXT.md (D-04 — filename derivation lives in the template, not the dispatcher; "My Token" → "MyToken.sol")
    - src/lib/env.ts (canonical pure-function module shape)
  </read_first>
  <behavior>
    - contractNameToFilename("MyToken") → "MyToken.sol"
    - contractNameToFilename("My Token") → "MyToken.sol"
    - contractNameToFilename("my_token") → "MyToken.sol"
    - contractNameToFilename("MyToken123") → "MyToken123.sol"
    - contractNameToFilename("123Token") → "Token.sol" (leading digits stripped per Solidity identifier rule)
    - contractNameToFilename("my-cool-token") → "MyCoolToken.sol"
    - contractNameToFilename("   ") → "Token.sol" (fallback)
    - contractNameToFilename("$$$") → "Token.sol" (fallback)
    - No throws.
  </behavior>
  <action>
    Create `src/templates/erc20/filename.ts` with the verbatim algorithm from RESEARCH §Filename Derivation:
    - Split input on `/[^A-Za-z0-9_]+/`, filter empty, PascalCase each word (uppercase first char + slice(1)), join.
    - Strip leading digits with `/^[0-9]+/`.
    - Fall back to `"Token"` if the result is empty.
    - Return `${base}.sol`.

    Single `export function contractNameToFilename(contractName: string): string`. JSDoc per PATTERNS §filename.ts — defensive on empty/symbol-only input, no throws, single exported function (analog: src/lib/env.ts).

    Then REPLACE `it.todo` placeholders in `tests/templates/erc20/filename.spec.ts` with real assertions, one row per RESEARCH §Filename Derivation test-case table (lines ~530-541). Use one `describe` block with `it` per row.
  </action>
  <verify>
    <automated>npx vitest run tests/templates/erc20/filename.spec.ts</automated>
    <automated>node -e "const t=require('fs').readFileSync('src/templates/erc20/filename.ts','utf8'); for (const k of ['contractNameToFilename','.sol']) if(!t.includes(k)){console.error('missing:',k);process.exit(1)} console.log('ok')"</automated>
  </verify>
  <done>
    All filename.spec.ts cases pass (zero `it.todo` for filename). The eight RESEARCH table rows are all covered. The function handles defensive input (empty, whitespace-only, symbol-only) without throwing. `npm run typecheck` clean.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement generate.ts (thin erc20.print wrapper) and lock golden snapshots + per-flag assertions</name>
  <files>src/templates/erc20/generate.ts, tests/templates/erc20/generate.spec.ts, tests/fixtures/erc20/bare-default.sol, tests/fixtures/erc20/all-flags-on.sol</files>
  <read_first>
    - .planning/phases/02-erc-20-canary-template/02-WAVE0-PROBES.md (Probe A: chosen import form; Probe B: premint-"0" mapping rule — apply both per Wave 0 decisions)
    - .planning/phases/02-erc-20-canary-template/02-RESEARCH.md (§Pattern 1 ~lines 249-270 — defensive default-then-destructure import; §Code Examples generate.ts body ~lines 1075-1093; §Snapshot Test Mechanics ~lines 609-695 — locked test cases; §Don't Hand-Roll ~lines 401-403; §Anti-Patterns ~lines 385-389)
    - .planning/phases/02-erc-20-canary-template/02-PATTERNS.md (§src/templates/erc20/generate.ts — pure-transform shape; §tests/templates/erc20/generate.spec.ts — toMatchFileSnapshot + per-flag describe blocks)
    - .planning/phases/02-erc-20-canary-template/02-CONTEXT.md (D-01: thin wrapper over erc20.print; D-02: no string templating with sentinels; D-04: return { filename, source }; D-09: hybrid golden + per-flag; D-10: no exhaustive snapshots)
    - .planning/phases/02-erc-20-canary-template/02-UI-SPEC.md (UI-09: status="alpha"; ERC20Permit is default-on per A2; UI-12 fixture inputs)
    - src/lib/version.ts (analog: thin synchronous wrapper around external resolution; lines ~79-86 for shape)
    - src/templates/erc20/opts.ts (Erc20Opts + GenerateResult — Plan 01)
    - src/templates/erc20/filename.ts (Task 2 of this plan — contractNameToFilename)
  </read_first>
  <behavior>
    - generate({ name:"MyToken", symbol:"MTK", premint:"1000000", mintable:false, burnable:false, pausable:false, access:false }) → { filename:"MyToken.sol", source: <wizard output starting with "// SPDX-License-Identifier: MIT"> }
    - generate({ ..., burnable:true }) → source contains "ERC20Burnable"
    - generate({ ..., mintable:true, access:"ownable" }) → source contains "Ownable" + "mint(" + "onlyOwner"
    - generate({ ..., mintable:true, access:"roles" }) → source contains "AccessControl" + "MINTER_ROLE" + "onlyRole(MINTER_ROLE)"
    - generate({ ..., pausable:true, access:"roles" }) → source contains "ERC20Pausable" + "PAUSER_ROLE"
    - generate({ ..., premint:"1234" }) → source matches /_mint\(recipient,\s*1234\s*\*\s*10\s*\*\*\s*decimals\(\)\)/
    - generate({ ..., premint:"0" }) → applies Wave 0 Probe B mapping (either passes through OR maps to undefined per the recorded decision)
    - Source matches /^\/\/ SPDX-License-Identifier: MIT/
    - Source contains "Compatible with OpenZeppelin Contracts"
    - bare-default.sol fixture matches generate output byte-for-byte (toMatchFileSnapshot)
    - all-flags-on.sol fixture matches generate output byte-for-byte (toMatchFileSnapshot)
    - No throws on happy path; CliError if any surfaces at the dispatcher level (Plan 04), not from this function.
  </behavior>
  <action>
    Create `src/templates/erc20/generate.ts`. Pure synchronous transform.

    Premint-value note: the locked fixture inputs use `premint: "1000000"` per CONTEXT D-09 / UI-SPEC UI-12 / Phase 2 wizard defaults. RESEARCH.md §Snapshot Test Mechanics shows a worked example using `premint: "1000"` for compactness — ignore that documentation-only number and follow CONTEXT D-09 / UI-12 (1000000) when populating the fixtures.

    Import: use the form determined by Wave 0 Probe A (recorded in 02-WAVE0-PROBES.md). If naive form works: `import { erc20 } from "@openzeppelin/wizard"; import type { ERC20Options } from "@openzeppelin/wizard";`. If defensive form required: `import wizard from "@openzeppelin/wizard"; const { erc20 } = wizard; import type { ERC20Options } from "@openzeppelin/wizard";`. Add a one-line comment above the import: `// Import form chosen per .planning/phases/02-erc-20-canary-template/02-WAVE0-PROBES.md Probe A`.

    Also import `contractNameToFilename` from `./filename.js` and types `Erc20Opts`, `GenerateResult` from `./opts.js`.

    Implementation (verbatim from RESEARCH §Code Examples generate.ts body, ~lines 1081-1092):
    - Map `Erc20Opts` → wizard's `ERC20Options` one-way (never expose ERC20Options outside this module per RESEARCH Anti-Patterns ~line 386).
    - Apply Wave 0 Probe B rule: if Probe B returned "REMAP REQUIRED" then `premint: opts.premint === "0" ? undefined : opts.premint`; if "PASSTHROUGH SAFE" then `premint: opts.premint`. Comment which rule applied with a reference to Probe B.
    - Pass through `name`, `symbol`, `mintable`, `burnable`, `pausable`, `access` unchanged. Do NOT pass `info` (the wizard's default `{ license: "MIT", securityContact: "" }` is what we want — matches wizard.openzeppelin.com per A2).
    - Call `erc20.print(mapped)`; the return is the source string.
    - Return `{ filename: contractNameToFilename(opts.name), source }`.

    No post-processing of the source string — D-02 forbids string templating with sentinels.

    Then REPLACE `it.todo` placeholders in `tests/templates/erc20/generate.spec.ts` with the test bodies from RESEARCH §Snapshot Test Mechanics (lines ~615-694) verbatim. Two describe blocks:
    1. "erc20 generate — golden snapshots (D-09)": two `it` cases with `await expect(source).toMatchFileSnapshot("../../fixtures/erc20/bare-default.sol")` and likewise `all-flags-on.sol`. MUST use `await` per Pitfall 2 / Vitest 4 async-matcher contract.
    2. "erc20 generate — per-flag assertions (D-09)": six `it` cases covering burnable / mintable+ownable / mintable+roles / pausable+roles / premint>0 / SPDX header. Use `toContain` / `toMatch` per the RESEARCH locked bodies.

    Snapshot fixture population: the first `npx vitest run tests/templates/erc20/generate.spec.ts` will fail with "snapshot file does not exist" for both snapshot cases. Run `npx vitest run tests/templates/erc20/generate.spec.ts -u` ONCE to populate `tests/fixtures/erc20/bare-default.sol` and `tests/fixtures/erc20/all-flags-on.sol`. Then OPEN each fixture, verify it matches the expected default wizard.openzeppelin.com output (SPDX-MIT, pragma ^0.8.27, ERC20Permit by default per A2, ERC20Burnable/ERC20Pausable parents only when their flag is on, AccessControl + MINTER_ROLE/PAUSER_ROLE when access=roles). Commit both fixtures to git — they are the byte-for-byte lock per D-09.

    The two fixtures and the inputs that produce them (per CONTEXT D-09 / UI-12 — premint=1000000):
    - `bare-default.sol`: `{ name:"MyToken", symbol:"MTK", premint:"1000000", mintable:false, burnable:false, pausable:false, access:false }`
    - `all-flags-on.sol`: `{ name:"MyToken", symbol:"MTK", premint:"1000000", mintable:true, burnable:true, pausable:true, access:"roles" }`

    Confirm `.gitattributes eol=lf` (Phase 1 setting) is in effect so the fixtures commit with LF endings — Vitest 4 PR #3164 already normalizes EOL in `toMatchFileSnapshot` but the committed file must also be LF for CI parity (RESEARCH Pitfall 3).
  </action>
  <verify>
    <automated>npx vitest run tests/templates/erc20/generate.spec.ts</automated>
    <automated>node -e "require('fs').statSync('tests/fixtures/erc20/bare-default.sol'); require('fs').statSync('tests/fixtures/erc20/all-flags-on.sol')"</automated>
    <automated>node -e "const fs=require('fs'); const b=fs.readFileSync('tests/fixtures/erc20/bare-default.sol','utf8'); for(const k of ['SPDX-License-Identifier: MIT','contract MyToken']) if(!b.includes(k)){console.error('bare-default missing:',k);process.exit(1)} console.log('ok')"</automated>
    <automated>node -e "const fs=require('fs'); const a=fs.readFileSync('tests/fixtures/erc20/all-flags-on.sol','utf8'); for(const k of ['AccessControl','MINTER_ROLE','PAUSER_ROLE','ERC20Burnable','ERC20Pausable']) if(!a.includes(k)){console.error('all-flags missing:',k);process.exit(1)} console.log('ok')"</automated>
    <automated>node -e "const t=require('fs').readFileSync('src/templates/erc20/generate.ts','utf8'); for (const k of ['erc20.print','contractNameToFilename','GenerateResult','02-WAVE0-PROBES']) if (!t.includes(k)) {console.error('missing:',k); process.exit(1)} console.log('ok')"</automated>
    <automated>node -e "const cr='\\r'.charCodeAt(0); const b1=require('fs').readFileSync('tests/fixtures/erc20/bare-default.sol'); const b2=require('fs').readFileSync('tests/fixtures/erc20/all-flags-on.sol'); for (const [n,b] of [['bare',b1],['all',b2]]) for (const x of b) if (x===cr) {console.error(n,'has CRLF'); process.exit(1)} console.log('LF ok')"</automated>
  </verify>
  <done>
    `npx vitest run tests/templates/erc20/generate.spec.ts` is green (snapshots + 6 per-flag assertions). Both fixtures exist, committed with LF endings, and contain the expected OZ identifiers (SPDX-MIT, ERC20Permit, contract MyToken; the all-flags fixture additionally contains AccessControl + MINTER_ROLE + PAUSER_ROLE + ERC20Burnable + ERC20Pausable). `src/templates/erc20/generate.ts` cites the Wave 0 probes file in its import-form comment. No string templating with sentinels (D-02 honored).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user input → wizard SDK | Validator-time check in wizard.ts (Plan 03); generate.ts trusts the Erc20Opts it receives but the wizard SDK itself escapes name/symbol when building Solidity-string contexts. |
| committed snapshots ↔ wizard output | Snapshots are the byte-for-byte lock; intentional regeneration via `vitest -u` produces a deliberate diff (the audit trail per D-09). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03 | Tampering | Wizard input → command injection (special chars in `name`/`symbol`) | mitigate | Validators in validators.ts reject anything outside `^[A-Za-z_][A-Za-z0-9_]{0,63}$` for `name` and `^[A-Za-z0-9]{1,11}$` for `symbol` at wizard time (Plan 03); generate.ts additionally passes name/symbol to `erc20.print()` which constructs the Solidity source programmatically (not via concatenation) per RESEARCH §Security Domain. Two layers. |
| T-02-04 | Tampering (snapshot) | Snapshot file tampering | mitigate | Fixtures committed to git; `vitest -u` updates only on intentional regen; `.gitattributes eol=lf` + Vitest 4 PR #3164 normalize EOL across platforms. CI runs the snapshot tests on `ubuntu-latest` (VALIDATION.md Manual-Only Verifications) to catch CRLF drift. |
| T-02-05 | Information Disclosure (on-chain, in the eventual deployed contract) | Mintable+Ownable centralization risk | accept (this plan) / mitigate (Plan 03) | This plan does not surface warnings — `output.warn` for Mintable+Ownable is in Plan 03's wizard. generate.ts only emits what the user asked for. |
</threat_model>

<verification>
- `npx vitest run tests/templates/erc20` — all spec files green (validators, filename, generate); zero `it.todo` remaining for these three.
- `npm run typecheck` — clean.
- `npm run build` — clean.
- Both fixtures exist under `tests/fixtures/erc20/` and commit with LF endings.
- Per-flag assertion coverage: burnable, mintable+ownable, mintable+roles, pausable+roles, premint>0, SPDX header (six axes per D-09 / RESEARCH §Snapshot Test Mechanics).
</verification>

<success_criteria>
1. ERC20-01: name/symbol/premint flow through `generate()` to wizard output unchanged (bare-default fixture proves it).
2. ERC20-02: `mintable:true` adds `mint()` plus the chosen access-control parent (per-flag assertion + all-flags-on fixture).
3. ERC20-03: `burnable:true` adds `ERC20Burnable` parent (per-flag assertion).
4. ERC20-04: `pausable:true` adds `ERC20Pausable` parent + `pause/unpause` (per-flag assertion + all-flags-on fixture).
5. ERC20-05: `access` field accepts `"ownable"` or `"roles"` and surfaces the matching constructs in the generated source (per-flag assertions for both).
6. ROADMAP SC-4: Two golden snapshots lock byte-for-byte against `@openzeppelin/wizard` output; intentional regen via `vitest -u` is the audit trail (D-09).
7. All three validators expose the @clack callback shape `(v: string | undefined) => string | undefined` with UI-SPEC-locked failure strings.
8. `contractNameToFilename` handles the eight RESEARCH-table input cases including the empty/all-symbol fallback.
</success_criteria>

<output>
Create `.planning/phases/02-erc-20-canary-template/02-02-SUMMARY.md` when done. Include: the import form chosen (cite Probe A), the premint-"0" rule chosen (cite Probe B), the two fixture filepaths + their byte counts, and the six per-flag assertion identifiers.
</output>
</content>
</invoke>