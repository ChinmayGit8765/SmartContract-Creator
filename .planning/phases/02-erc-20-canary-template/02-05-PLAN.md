---
phase: 02-erc-20-canary-template
plan: 05
type: execute
wave: 3
depends_on: [02-04]
files_modified:
  - tests/cli.spec.ts
  - tests/cli.sc4.spec.ts
  - tests/version.spec.ts
  - src/lib/version.ts
  - .planning/phases/02-erc-20-canary-template/02-VALIDATION.md
files_modified_optional:
  - .planning/ROADMAP.md
autonomous: true
requirements: [ERC20-01, ERC20-02, ERC20-03, ERC20-04, ERC20-05]
tags: [e2e, version-line, sc-4, roadmap-finalization, ui-16]
discretion_exercised: "Planner exercises CONTEXT D-08 discretion to ship UI-16 (append `@openzeppelin/wizard X.Y.Z` to `formatVersionLine` output) per UI-SPEC recommendation. Default per D-08 was 'leave untouched'; this revision documents the intentional override."

must_haves:
  truths:
    - "tests/cli.spec.ts SC-4 placeholder at line ~106 stays as an `it.skip` pointer (with a comment naming tests/cli.sc4.spec.ts as the new home), and the three real SC-4 in-process cases live in tests/cli.sc4.spec.ts (mocked @clack/prompts at module-top + dynamic SUT imports)"
    - "formatVersionLine() includes @openzeppelin/wizard's version (UI-16) — `node dist/cli.js --version` output includes 'wizard 0.10.8' (or current version)"
    - "tests/version.spec.ts asserts the third parenthetical segment without breaking the Phase 1 contract"
    - "Full vitest suite is green (Phase 1 + Phase 2)"
    - "Phase 2 ROADMAP plans-list reflects the five plan files this phase shipped"
    - "VALIDATION.md CLI-07 row's automated command points at tests/cli.sc4.spec.ts (not tests/cli.spec.ts)"
  artifacts:
    - path: "tests/cli.sc4.spec.ts"
      provides: "Three SC-4 in-process cases — vi.mock(@clack/prompts) + dynamic SUT import; the in-process e2e home for CLI-07 residual coverage"
      contains: "SC-4"
    - path: "tests/cli.spec.ts"
      provides: "Phase 1 spawn-based suite (unchanged) + a comment at line ~106 pointing readers at tests/cli.sc4.spec.ts"
      contains: "tests/cli.sc4.spec.ts"
    - path: "src/lib/version.ts"
      provides: "formatVersionLine including @openzeppelin/wizard version segment"
      contains: "@openzeppelin/wizard"
  key_links:
    - from: "src/lib/version.ts"
      to: "@openzeppelin/wizard"
      via: "safeReadVersion('@openzeppelin/wizard')"
      pattern: "safeReadVersion\\(\"@openzeppelin/wizard\"\\)"
    - from: "tests/cli.sc4.spec.ts"
      to: "src/commands/create.ts (via program.parseAsync)"
      via: "in-process dispatcher exercise; CLI-07 residual coverage"
      pattern: "parseAsync"
---

<objective>
Close out Phase 2 with the SC-4 e2e fill-in (in a NEW file `tests/cli.sc4.spec.ts` per W5), the optional UI-16 version-line extension (per D-08 discretion override documented above), and ROADMAP + VALIDATION.md finalization.

Purpose:
- VALIDATION.md tracks `tests/cli.spec.ts` line ~106 SC-4 placeholder as one of the Wave 0 gaps. Plan 04 covered SC-4 at the unit/command level; this plan exercises it end-to-end through the actual program tree (still in-process per RESEARCH §e2e Test Strategy — no stdin-piped binary spawn for the wizard prompts). The new file `tests/cli.sc4.spec.ts` keeps the module-top `vi.mock("@clack/prompts", ...)` block out of the spawn-based suite in `tests/cli.spec.ts` (W5 — Vitest 4's mock hoisting could otherwise interfere with the spawn-based runCli tests' subprocess invocations).
- UI-16 (planner's call per D-08, recommended in UI-SPEC): append `@openzeppelin/wizard <ver>` to `formatVersionLine()`. CONTEXT D-08 defaulted to "leave untouched"; this plan documents the deliberate override using planner discretion granted by D-08 itself. Phase 1's dual-strategy `safeReadVersion` already supports it.
- Finalize ROADMAP.md plans-list and VALIDATION.md CLI-07 row so verifier can sample.

Output:
- `tests/cli.sc4.spec.ts` (NEW) — three SC-4 in-process cases covering --force, accept-overwrite, and decline-overwrite end-to-end through the dispatcher.
- `tests/cli.spec.ts` — the existing `it.skip("SC-4: ...")` at line ~106 stays as a placeholder; ONE-LINE comment added pointing at `tests/cli.sc4.spec.ts` as the actual home. The spawn-based suite above is otherwise unchanged.
- `src/lib/version.ts` extended with the wizard segment.
- `tests/version.spec.ts` updated to allow the new third parenthetical segment.
- `.planning/phases/02-erc-20-canary-template/02-VALIDATION.md` — CLI-07 row's automated command updated from `tests/cli.spec.ts -t "SC-4"` to `tests/cli.sc4.spec.ts`.
- `.planning/ROADMAP.md` Phase 2 plans-list updated to reflect the five shipped plan files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-erc-20-canary-template/02-CONTEXT.md
@.planning/phases/02-erc-20-canary-template/02-RESEARCH.md
@.planning/phases/02-erc-20-canary-template/02-PATTERNS.md
@.planning/phases/02-erc-20-canary-template/02-UI-SPEC.md
@.planning/phases/02-erc-20-canary-template/02-VALIDATION.md
@.planning/phases/02-erc-20-canary-template/02-04-PLAN.md
@src/lib/version.ts
@tests/cli.spec.ts
@tests/version.spec.ts
@src/commands/create.ts
@src/templates/erc20/index.ts
@src/templates/erc20/wizard.ts

<interfaces>
<!-- Plan 01-02 contracts already in scope; this plan adds two surfaces. -->

From src/lib/version.ts (Phase 1):
```
export function safeReadVersion(pkgName: string): string | null;
export function formatVersionLine(): string;  // currently emits "smartc X (solcStr, ozStr)"
```

Phase 1 dual-strategy resolve already supports any package whose `main` is exports-map-unrestricted — `@openzeppelin/wizard@0.10.8` has `main: dist/index.js` (no exports map restriction) per RESEARCH §Standard Stack lines 110-115, so `safeReadVersion("@openzeppelin/wizard")` will return the semver string out-of-the-box.

From tests/cli.spec.ts (Phase 1 e2e harness):
```
function runCli(args: string[], env?: Record<string, string>): { stdout, stderr, status };
// beforeAll: npm run build once
// it.skip("SC-4: overwrite prompt + --force (deferred to Phase 2)", () => {}); // at line ~106 — KEEPS as a placeholder pointing at tests/cli.sc4.spec.ts
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend formatVersionLine() with @openzeppelin/wizard version (UI-16, D-08 discretion override)</name>
  <files>src/lib/version.ts, tests/version.spec.ts</files>
  <read_first>
    - src/lib/version.ts (existing formatVersionLine — lines ~79-86 are the locked Phase 1 shape; safeReadVersion is the helper)
    - tests/version.spec.ts (existing assertion regex — needs to allow the new third parenthetical segment per PATTERNS §src/lib/version.ts test impact note)
    - .planning/phases/02-erc-20-canary-template/02-UI-SPEC.md (UI-16: recommended ship in Phase 2; one-line safeReadVersion call; "Test impact: tests/version.spec.ts needs widening")
    - .planning/phases/02-erc-20-canary-template/02-RESEARCH.md (§Open Questions (RESOLVED) #2 ~lines 851-854 — surface wizard version in formatVersionLine; recommendation: yes, append)
    - .planning/phases/02-erc-20-canary-template/02-PATTERNS.md (§src/lib/version.ts — exact ternary pattern to copy from existing solcStr/ozStr; existing function returns `smartc ${ownVer} (${solcStr}, ${ozStr})`)
    - .planning/phases/02-erc-20-canary-template/02-CONTEXT.md (D-08 — original default was "leave untouched"; planner discretion to override is granted by D-08 itself; UI-SPEC UI-16 recommends ship; this plan exercises that discretion)
  </read_first>
  <behavior>
    - `formatVersionLine()` returns a string of the form `smartc <ownVer> (solc <ver|not bundled>, @openzeppelin/contracts <ver|not bundled>, @openzeppelin/wizard <ver|not bundled>)`.
    - With @openzeppelin/wizard@0.10.8 installed (Plan 01), the wizard segment is `@openzeppelin/wizard 0.10.8` (the literal version string).
    - solc and @openzeppelin/contracts segments STILL emit "not bundled" because they're Phase 3 deps (Phase 1 contract preserved).
    - `node dist/cli.js --version` prints the full line including the new segment.
    - tests/version.spec.ts assertions still pass (the regex is widened, not replaced).
  </behavior>
  <action>
    Edit `src/lib/version.ts`. Append a third ternary block immediately after the existing `ozStr` ternary (lines ~83-84 per PATTERNS §src/lib/version.ts):
    ```
    const wiz = safeReadVersion("@openzeppelin/wizard");
    const wizStr = wiz ? `@openzeppelin/wizard ${wiz}` : "@openzeppelin/wizard not bundled";
    ```
    And update the return:
    ```
    return `smartc ${ownVer} (${solcStr}, ${ozStr}, ${wizStr})`;
    ```

    Do NOT change `safeReadVersion`, `readOwnVersion`, or any other helper. The existing dual-strategy resolve is what makes this one-line addition load-bearing — Phase 1 designed this case exactly per 01-02-SUMMARY.

    Edit `tests/version.spec.ts`. The existing assertion that locks the Phase 1 shape (likely a regex like `/^smartc \S+ \(solc.*, @openzeppelin\/contracts.*\)$/`) needs widening to allow a third parenthetical segment. Two options:
    - (a) Widen the regex to `/^smartc \S+ \(solc.*, @openzeppelin\/contracts.*, @openzeppelin\/wizard.*\)$/`.
    - (b) Split into two assertions: one that asserts the prefix `smartc <ver> (...)`, and per-segment `toContain` assertions for each of the three dep segments.
    Prefer (b) for resilience — future dep additions won't require regex churn. Add one new assertion: `expect(formatVersionLine()).toContain("@openzeppelin/wizard 0.10.8")` (NOT "not bundled" — the dep is installed in this phase).
  </action>
  <verify>
    <automated>npm run build</automated>
    <automated>node dist/cli.js --version</automated>
    <automated>node -e "const out=require('child_process').execSync('node dist/cli.js --version',{encoding:'utf8'}); if(!out.includes('@openzeppelin/wizard')){console.error('missing wizard segment in:',out);process.exit(1)} console.log('ok wizard segment')"</automated>
    <automated>npx vitest run tests/version.spec.ts</automated>
    <automated>node -e "const t=require('fs').readFileSync('src/lib/version.ts','utf8'); for (const k of ['safeReadVersion(\"@openzeppelin/wizard\")','wizStr','@openzeppelin/wizard not bundled']) if (!t.includes(k)) {console.error('missing:',k); process.exit(1)} console.log('ok')"</automated>
  </verify>
  <done>
    `node dist/cli.js --version` prints the three-segment line including `@openzeppelin/wizard 0.10.8`. tests/version.spec.ts passes with the widened assertions. Phase 1 dual-strategy `safeReadVersion` did the heavy lifting (no code change there). D-08 discretion override documented in the plan frontmatter.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create tests/cli.sc4.spec.ts with three SC-4 in-process cases; leave tests/cli.spec.ts placeholder as a pointer (W5)</name>
  <files>tests/cli.sc4.spec.ts, tests/cli.spec.ts</files>
  <read_first>
    - tests/cli.spec.ts (the `it.skip` at line ~106 — documenting comment + `runCli` helper at lines 13-28 + invocation patterns lines 51-74; the spawn-based suite must NOT gain a module-top vi.mock block — that would interfere with subprocess assertions even though it shouldn't, and W5 names the move as the fix)
    - tests/prompt.spec.ts (canonical Phase 1 vi.mock + dynamic-import pattern + per-test method-call assertion + cancel-path assertion shape; this is the template for tests/cli.sc4.spec.ts)
    - tests/commands/create.spec.ts (Plan 04 Task 4 — already covers SC-4 at unit level; this is the e2e analog through the full program tree, also in-process)
    - .planning/phases/02-erc-20-canary-template/02-RESEARCH.md (§e2e Test Strategy ~lines 757-811 — IN-PROCESS recommendation; concrete sketch ~lines 772-808; Pitfall 4 ~lines 968-972 on TTY/stdin fragility)
    - .planning/phases/02-erc-20-canary-template/02-PATTERNS.md (§tests/cli.spec.ts — runCli execFileSync pattern; "the harder bits" for stdin-piping; SC-4 fill-in via in-process dispatcher)
    - .planning/phases/02-erc-20-canary-template/02-VALIDATION.md (CLI-07 residual row — automated cmd needs to be updated by Task 3 of this plan to point at tests/cli.sc4.spec.ts)
    - .planning/phases/02-erc-20-canary-template/02-UI-SPEC.md (post-write footer copy — used as assertions for the happy-path SC-4 e2e)
    - src/lib/prompt.ts (confirmOverwrite throws CliError(E_FILE_EXISTS))
    - src/commands/create.ts (Plan 04 — dispatcher to exercise via program.parseAsync; --template is required, so SC-4 cases all pass --template erc20)
  </read_first>
  <behavior>
    SC-4 (CLI-07 residual): the overwrite prompt fires when output path exists; `--force` skips the prompt. Per RESEARCH §e2e Test Strategy, the SC-4 e2e is IN-PROCESS (not a binary spawn) — @clack/prompts on piped stdin is fragile (Pitfall 4) and the in-process dispatcher path is the recommended Phase 2 fill.

    Cases (all in tests/cli.sc4.spec.ts; tests/cli.spec.ts line ~106 stays as it.skip placeholder):
    1. "SC-4: --force overwrites an existing file without prompting" — pre-create target path with stale content; mock @clack/prompts.confirm (which confirmOverwrite uses); set up wizard-prompt mocks to return valid inputs; run dispatcher via `parseAsync(["create","--template","erc20","--out",tmpPath,"--force"], { from:"user" })`; assert file now contains `contract MyToken`; assert confirm WAS NOT called for an overwrite prompt (confirmOverwrite skips it under --force).
    2. "SC-4: without --force, the overwrite prompt fires and user can accept (overwrites)" — same setup, no --force, mock @clack confirm to return true for the confirmOverwrite call; assert confirm WAS called with a message matching `/overwrite/i`; assert file overwritten.
    3. "SC-4: without --force, the overwrite prompt fires and user can decline (E_FILE_EXISTS)" — same setup, mock @clack confirm to return false for the confirmOverwrite call; assert dispatcher rejects with CliError code E_FILE_EXISTS; assert pre-existing content unchanged.

    tests/cli.spec.ts at line ~106: the existing `it.skip("SC-4: overwrite prompt + --force (deferred to Phase 2 when create writes files)", () => {});` STAYS as a placeholder. ONE additional comment line is added immediately above it: `// Filled in tests/cli.sc4.spec.ts — kept here as it.skip placeholder so the spawn suite stays free of vi.mock("@clack/prompts") hoisting (W5).`
  </behavior>
  <action>
    Create NEW file `tests/cli.sc4.spec.ts`. Use the LOCKED Phase 1 Vitest 4 ESM mock pattern from tests/prompt.spec.ts.

    File header comment (top of file): `// SC-4 (CLI-07 residual) in-process e2e per RESEARCH §e2e Test Strategy. Moved out of tests/cli.spec.ts (W5) so the spawn-based suite stays free of module-top vi.mock("@clack/prompts") hoisting that could interfere with subprocess invocations. True TTY-driven e2e is deferred to a later cross-platform-distribution hardening phase.`

    Mock factory (at module top, BEFORE imports of the SUT):
    ```
    vi.mock("@clack/prompts", () => ({
      text: vi.fn(), select: vi.fn(), confirm: vi.fn(),
      multiselect: vi.fn(), isCancel: vi.fn(() => false), cancel: vi.fn(),
    }));
    ```

    SUT imports (dynamic, AFTER the mock — same pattern as tests/commands/create.spec.ts):
    ```
    const { buildProgram } = await import("../src/program.js");
    const { clear } = await import("../src/registry/index.js");
    const { registerErc20Template } = await import("../src/templates/erc20/index.js");
    const clack = await import("@clack/prompts");
    ```

    `beforeEach`: `clear()` the registry, call `registerErc20Template()`, `mkdtempSync(join(tmpdir(),"smartc-sc4-"))` for filesystem isolation; reset every mock (vi.clearAllMocks()) and re-default `isCancel` to false.
    `afterEach`: `rmSync(dir, { recursive: true, force: true })`.

    For each test, build the program in-test via `buildProgram()` and call `program.parseAsync(["create","--template","erc20","--out",tmpPath, ...], { from:"user" })`.

    Wizard-prompt mock setup (each test):
    - The wizard has six prompts in the happy path (text×3 then confirm×3) returning name="MyToken", symbol="MTK", premint="1000000", mintable=false, burnable=false, pausable=false (so prompt 7 is skipped per Plan 03).
    - For Test 1 (--force, no overwrite-prompt expected), confirmOverwrite is bypassed under --force, so only the three wizard `confirm.mockResolvedValueOnce(false)` calls are needed.
    - For Test 2 (overwrite accept), set up wizard confirm mocks first (three `mockResolvedValueOnce(false)`), then `mockResolvedValueOnce(true)` for the confirmOverwrite call (the 4th confirm).
    - For Test 3 (overwrite decline), same as Test 2 but the 4th confirm returns false.

    Test 1 (--force) assertions: after parseAsync, `readFileSync(tmpPath,"utf8")` contains "contract MyToken"; the confirm mock was called exactly 3 times (the three wizard confirm prompts), NOT 4 times (no overwrite prompt fired).

    Test 2 (accept) assertions: parseAsync resolves; `readFileSync(tmpPath,"utf8")` contains "contract MyToken"; the confirm mock was called 4 times, and the 4th call's `message` argument matches `/overwrite/i`.

    Test 3 (decline) assertions: `await expect(program.parseAsync(...)).rejects.toMatchObject({ code: "E_FILE_EXISTS" })`; `readFileSync(tmpPath,"utf8")` still contains the original stale content (file unchanged).

    Then edit `tests/cli.spec.ts`: at line ~106, KEEP the existing `it.skip("SC-4: overwrite prompt + --force (deferred to Phase 2 when create writes files)", () => {});` exactly as-is. Insert ONE new comment line immediately above it:
    ```
    // Filled in tests/cli.sc4.spec.ts — kept here as it.skip placeholder so the spawn suite stays free of vi.mock("@clack/prompts") hoisting (W5).
    ```
    Do NOT remove or modify anything else in tests/cli.spec.ts — Phase 1's 14 spawn-based e2e cases remain green.
  </action>
  <verify>
    <automated>npx vitest run tests/cli.sc4.spec.ts --reporter=verbose</automated>
    <automated>npx vitest run tests/cli.spec.ts tests/cli.sc4.spec.ts</automated>
    <automated>node -e "require('fs').statSync('tests/cli.sc4.spec.ts'); console.log('sc4 file exists')"</automated>
    <automated>node -e "const t=require('fs').readFileSync('tests/cli.sc4.spec.ts','utf8'); for (const k of ['vi.mock(\"@clack/prompts\"','await import','registerErc20Template','SC-4','--force','--template','E_FILE_EXISTS','/overwrite/i']) if(!t.includes(k)){console.error('missing in sc4 file:',k);process.exit(1)} console.log('ok sc4 contents')"</automated>
    <automated>node -e "const t=require('fs').readFileSync('tests/cli.spec.ts','utf8'); if(!t.includes('tests/cli.sc4.spec.ts')){console.error('cli.spec.ts missing pointer comment to cli.sc4.spec.ts');process.exit(1)} if(!t.includes('it.skip(\"SC-4')){console.error('cli.spec.ts SC-4 placeholder removed — should stay');process.exit(1)} if(t.includes('vi.mock(\"@clack/prompts\"')){console.error('cli.spec.ts gained vi.mock block — W5 forbids');process.exit(1)} console.log('ok cli.spec.ts pointer + clean spawn suite')"</automated>
  </verify>
  <done>
    `tests/cli.sc4.spec.ts` exists with three SC-4 in-process cases (all green): --force happy-path, no-force-accept, no-force-decline. `tests/cli.spec.ts` line ~106 still carries the `it.skip` placeholder PLUS a one-line pointer comment naming `tests/cli.sc4.spec.ts` as the home; the spawn suite has NO module-top vi.mock block. Full suite passes.
  </done>
</task>

<task type="auto">
  <name>Task 3: Finalize ROADMAP.md Phase 2 plans-list and update VALIDATION.md CLI-07 row</name>
  <files>.planning/ROADMAP.md, .planning/phases/02-erc-20-canary-template/02-VALIDATION.md</files>
  <read_first>
    - .planning/ROADMAP.md (Phase 2 section ~lines 45-58)
    - .planning/phases/02-erc-20-canary-template/02-VALIDATION.md (CLI-07 row — automated command currently points at tests/cli.spec.ts -t "SC-4"; per W5 it must point at tests/cli.sc4.spec.ts)
  </read_first>
  <action>
    Edit `.planning/ROADMAP.md` Phase 2 section:

    1. Find the "Plans: TBD" line in the Phase 2 details (line ~54). Change to "Plans: 5 plans in 4 waves".
    2. Replace the placeholder `- [ ] 02-01: TBD` with the five real plan stubs in this order:
       - `- [ ] 02-01-PLAN.md — Wave 0: spike (@openzeppelin/wizard probes), Template generic widening, error-code constants, opts.ts type contracts, spec skeletons`
       - `- [ ] 02-02-PLAN.md — Wave 1 (parallel with 02-03): validators, filename derivation, generate() wrapper + golden snapshot fixtures`
       - `- [ ] 02-03-PLAN.md — Wave 1 (parallel with 02-02): wizard.ts (seven-prompt sequence, cancelGuard, centralization warning)`
       - `- [ ] 02-04-PLAN.md — Wave 2: registerErc20Template factory, dispatcher .action() body (required --template), canary retirement, in-process command spec`
       - `- [ ] 02-05-PLAN.md — Wave 3: SC-4 e2e fill-in (tests/cli.sc4.spec.ts), version-line @openzeppelin/wizard segment (UI-16), VALIDATION + ROADMAP finalization`
    3. Leave the Phase 2 Goal and Success Criteria unchanged — they are already accurate.
    4. Leave the Progress table at the bottom of ROADMAP.md unchanged — it will be updated by the verifier after the phase wraps (per CLAUDE.md push-after-phase workflow).

    Do NOT mark any of the plan checkboxes as `[x]` — the executor that runs each plan will check them off in sequence per the standard GSD workflow.

    Edit `.planning/phases/02-erc-20-canary-template/02-VALIDATION.md`:
    - Find the CLI-07 row whose "Automated Command" column currently reads `npx vitest run tests/cli.spec.ts -t "SC-4"` (or similar reference to tests/cli.spec.ts for SC-4).
    - Update the automated command to `npx vitest run tests/cli.sc4.spec.ts` (W5 — the SC-4 cases moved to a dedicated file).
    - If the row has a "Notes" or "Owner" column, add: "Moved to dedicated file per W5 (revision pass 1) — keeps vi.mock(\"@clack/prompts\") out of the spawn suite."
    - Do NOT touch other VALIDATION.md rows.
  </action>
  <verify>
    <automated>node -e "const t=require('fs').readFileSync('.planning/ROADMAP.md','utf8'); for (const k of ['02-01-PLAN.md','02-02-PLAN.md','02-03-PLAN.md','02-04-PLAN.md','02-05-PLAN.md']) if(!t.includes(k)){console.error('missing in ROADMAP:',k);process.exit(1)} if(t.includes('02-01: TBD')){console.error('TBD placeholder still present');process.exit(1)} console.log('ok roadmap')"</automated>
    <automated>node -e "const t=require('fs').readFileSync('.planning/phases/02-erc-20-canary-template/02-VALIDATION.md','utf8'); if(!t.includes('tests/cli.sc4.spec.ts')){console.error('VALIDATION.md CLI-07 row not updated to point at tests/cli.sc4.spec.ts');process.exit(1)} console.log('ok validation')"</automated>
  </verify>
  <done>
    .planning/ROADMAP.md Phase 2 plans-list contains the five real plan files in order, with brief one-line objectives matching the actual plan content. The "TBD" placeholder is gone. Plans count line says "5 plans in 4 waves". VALIDATION.md CLI-07 row's automated command points at tests/cli.sc4.spec.ts.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `--out <path>` → fs.writeFile | Same accepted boundary as Plan 04 — user is the trust boundary; documented and accepted. |
| Test fixtures (tmpdir) → fs | Isolated tmpdir per test; afterEach cleanup. No shared filesystem state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-FS-01 | Tampering (filesystem) | `--out <traversal>` | accept | Inherited from Plan 04 disposition. Local dev tool, user is trust boundary. |
| T-02-12 | Information Disclosure | Version line leaks installed @openzeppelin/wizard version | accept | This is intentional disclosure per UI-16 / RESEARCH Q2 — helps debugging snapshot drift. The version is a public published npm version; no secrets exposed. |
| T-02-14 | Test Isolation | Module-top `vi.mock("@clack/prompts")` in tests/cli.spec.ts could affect Vitest's hoisting behavior for the spawn-based suite | mitigate | W5: SC-4 in-process cases moved to dedicated file tests/cli.sc4.spec.ts. The spawn suite in tests/cli.spec.ts remains free of vi.mock() blocks. Test isolation preserved. |
</threat_model>

<verification>
- `npx vitest run` — full suite green (Phase 1 + Phase 2 + Plan 05).
- `node dist/cli.js --version` includes the wizard segment.
- `tests/cli.sc4.spec.ts` is green with three new in-process cases.
- `tests/cli.spec.ts` line ~106 still carries `it.skip("SC-4: ...")` + the one-line pointer comment.
- VALIDATION.md CLI-07 row points at tests/cli.sc4.spec.ts.
- ROADMAP.md Phase 2 lists the five plan files.
</verification>

<success_criteria>
1. SC-4 (CLI-07 residual) is observably covered at the e2e level — `npx vitest run tests/cli.sc4.spec.ts` is green with three cases (--force, accept-overwrite, decline-overwrite).
2. UI-16 shipped (D-08 discretion override documented in plan frontmatter): `node dist/cli.js --version` includes `@openzeppelin/wizard 0.10.8` segment.
3. ROADMAP.md Phase 2 plans-list reflects the five plan files this phase shipped.
4. VALIDATION.md CLI-07 row's automated command points at tests/cli.sc4.spec.ts (not tests/cli.spec.ts).
5. tests/cli.spec.ts spawn-based suite is unchanged except for the one-line pointer comment at line ~106.
6. Full vitest suite is green.
7. Build is clean.
</success_criteria>

<output>
Create `.planning/phases/02-erc-20-canary-template/02-05-SUMMARY.md` when done. Include: the full `node dist/cli.js --version` output, the three SC-4 case names (from tests/cli.sc4.spec.ts), the ROADMAP diff (before/after the plans-list), the VALIDATION.md CLI-07 row diff, the D-08 discretion override note, and a Phase 2 done-checklist mapping each ERC20-01..05 + ROADMAP SC-1..4 to the specific test command that verifies it.
</output>
</content>
</invoke>