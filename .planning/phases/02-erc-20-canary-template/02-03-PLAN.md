---
phase: 02-erc-20-canary-template
plan: 03
type: execute
wave: 1
depends_on: [02-01]
files_modified:
  - src/templates/erc20/wizard.ts
  - tests/templates/erc20/wizard.spec.ts
autonomous: true
requirements: [ERC20-01, ERC20-02, ERC20-03, ERC20-04, ERC20-05]
tags: [erc20, wizard, clack, cancel-handling, centralization-warning, newbie-mode]

must_haves:
  truths:
    - "runWizard(io) walks the seven UI-SPEC-locked prompts in order, with prompt 7 (access control) firing only when mintable||pausable"
    - "Every @clack prompt return passes through a cancelGuard helper; on cancel, CliError(E_WIZARD_CANCEL, exitCode:130) is thrown with the prompt name in the WHAT line"
    - "The centralization warning (output.warn) fires when mintable=true and access=='ownable' — even in non-newbie and --json modes (output.warn is always-on)"
    - "Newbie-mode preamble (one explain + two reference lines) fires before prompt 1; per-prompt explain lines fire before each of the seven prompts; non-newbie mode is silent on these channels"
    - "Validators from Plan 02 are wired into the @clack `validate` callbacks for prompts 1, 2, 3"
    - "Prompt copy (messages, placeholders, defaultValues, select options) is byte-exact from UI-SPEC §Wizard Prompt Sequence"
    - "Returned Erc20Opts has the locked shape — access:false when prompt 7 was skipped"
  artifacts:
    - path: "src/templates/erc20/wizard.ts"
      provides: "runWizard(io: WizardIo) interactive prompt sequence with cancelGuard + centralization warning"
      contains: "cancelGuard"
    - path: "tests/templates/erc20/wizard.spec.ts"
      provides: "Mocked-clack tests for prompt sequence, cancel paths, centralization warning, newbie preamble"
      contains: "vi.mock"
  key_links:
    - from: "src/templates/erc20/wizard.ts"
      to: "@clack/prompts"
      via: "named imports: text, select, confirm, isCancel"
      pattern: "from \"@clack/prompts\""
    - from: "src/templates/erc20/wizard.ts"
      to: "src/templates/erc20/validators.ts"
      via: "validators wired into prompt validate callbacks"
      pattern: "isSolidityIdentifier|isAsciiSymbol|isNonNegativeDecimal"
    - from: "src/templates/erc20/wizard.ts"
      to: "src/lib/errors.ts"
      via: "CliError + ERR_WIZARD_CANCEL on cancel"
      pattern: "ERR_WIZARD_CANCEL"
    - from: "src/templates/erc20/wizard.ts"
      to: "src/lib/output.ts"
      via: "io.output.explain / reference / warn / nextStep"
      pattern: "output\\.(explain|reference|warn|nextStep)"
---

<objective>
Build the interactive wizard half of the ERC-20 template plugin. Implement `runWizard(io: WizardIo): Promise<Erc20Opts>` with the seven UI-SPEC-locked prompts, a single `cancelGuard` helper for Ctrl+C handling, the non-silenceable Mintable+Ownable centralization warning, and the newbie-mode preamble + per-prompt explain lines. Lock behavior with the mocked-clack spec.

Purpose: Delivers ERC20-05 directly (the conditional access-control prompt) and ERC20-01..04 indirectly (the wizard collects the inputs that Plan 02's `generate()` then transforms). Lays the canonical wizard pattern future templates (ERC-721, ERC-1155, SPL) will copy.

Output:
- `src/templates/erc20/wizard.ts` — `runWizard(io)` with the seven-prompt sequence, inline `cancelGuard<T>` helper (kept inline in Phase 2 per UI-SPEC Components Inventory; hoists to `src/lib/wizard.ts` in Phase 4 when ERC-721 lands as the second consumer), and the centralization warning call site.
- `tests/templates/erc20/wizard.spec.ts` — fully filled-in mocked-clack spec covering happy path, conditional step 7, cancel at each prompt, centralization warning, and newbie preamble.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-erc-20-canary-template/02-CONTEXT.md
@.planning/phases/02-erc-20-canary-template/02-RESEARCH.md
@.planning/phases/02-erc-20-canary-template/02-PATTERNS.md
@.planning/phases/02-erc-20-canary-template/02-UI-SPEC.md
@.planning/phases/02-erc-20-canary-template/02-VALIDATION.md
@.planning/phases/02-erc-20-canary-template/02-01-PLAN.md
@.planning/phases/01-cli-foundation/01-02-SUMMARY.md
@src/templates/erc20/opts.ts
@src/lib/prompt.ts
@src/lib/errors.ts
@src/lib/output.ts
@tests/prompt.spec.ts

<interfaces>
<!-- Contracts consumed by this plan (created by Plan 01 + co-existing in Wave 1 with Plan 02). -->

From src/templates/erc20/opts.ts (Plan 01):
```
export interface Erc20Opts {
  readonly name: string;
  readonly symbol: string;
  readonly premint: string;
  readonly mintable: boolean;
  readonly burnable: boolean;
  readonly pausable: boolean;
  readonly access: false | "ownable" | "roles";
}
export interface WizardIo { readonly output: Output; }
```

From src/lib/errors.ts (Phase 1 + Plan 01 additions):
```
export class CliError extends Error { readonly code; readonly what; readonly why; readonly fix; readonly exitCode; }
export const ERR_WIZARD_CANCEL = "E_WIZARD_CANCEL" as const;
```

From src/lib/output.ts (Phase 1):
```
export interface Output {
  result(text: string): void;
  warn(text: string): void;           // ALWAYS-ON — fires in default + newbie + --json modes
  error(text: string): void;
  explain(text: string): void;         // newbie + not-json only
  reference(label: string, url: string): void;  // newbie + not-json only
  nextStep(text: string): void;        // newbie + not-json only
}
```

From src/templates/erc20/validators.ts (Plan 02 Task 1):
```
export function isSolidityIdentifier(v: string | undefined): string | undefined;
export function isAsciiSymbol(v: string | undefined): string | undefined;
export function isNonNegativeDecimal(v: string | undefined): string | undefined;
```

From @clack/prompts ^0.11 (Phase 1 dep — already installed):
```
text({ message, placeholder?, defaultValue?, validate? }): Promise<string | symbol>
confirm({ message, initialValue? }): Promise<boolean | symbol>
select({ message, options, initialValue? }): Promise<TValue | symbol>
isCancel(v: unknown): boolean   // returns true for the cancel sentinel
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement wizard.ts (runWizard + cancelGuard) per UI-SPEC locked copy</name>
  <files>src/templates/erc20/wizard.ts</files>
  <read_first>
    - .planning/phases/02-erc-20-canary-template/02-UI-SPEC.md (§Wizard Prompt Sequence ~lines 60-180 — every message, placeholder, defaultValue, validator-failure-string, newbie-explain-line is LOCKED; paste byte-exact)
    - .planning/phases/02-erc-20-canary-template/02-UI-SPEC.md (UI-02 centralization warning ~lines 164-180 — paste verbatim; UI-03 newbie preamble ~lines 64-77; UI-11 cancelGuard ~lines 247-277; UI-14 warning scope — Mintable+Ownable ONLY in Phase 2)
    - .planning/phases/02-erc-20-canary-template/02-RESEARCH.md (§Wizard Cancel Handling ~lines 721-755 — cancelGuard signature; §Wizard Order + Newbie-Mode ~lines 405-449)
    - .planning/phases/02-erc-20-canary-template/02-PATTERNS.md (§src/templates/erc20/wizard.ts — single-prompt + cancel-guard pattern from src/lib/prompt.ts; "Multi-prompt sequential flow with conditional step 7 — first such flow in the repo")
    - .planning/phases/02-erc-20-canary-template/02-CONTEXT.md (D-06: no separate validate(opts) method — validators live in @clack callbacks; non-negotiable copy hooks)
    - src/lib/prompt.ts (Phase 1 confirmOverwrite — single @clack prompt + isCancel guard pattern)
    - src/lib/errors.ts (CliError constructor + ERR_WIZARD_CANCEL)
    - src/templates/erc20/opts.ts (Erc20Opts, WizardIo)
    - src/templates/erc20/validators.ts (Plan 02 Task 1)
  </read_first>
  <behavior>
    Seven-prompt flow (UI-SPEC §Wizard Prompt Sequence):

    1. PREFLIGHT (newbie only, before prompt 1): one `output.explain` line, two `output.reference` lines (EIP-20, OZ docs) — exact strings per UI-SPEC §Pre-flight.
    2. PROMPT 1 — text, message "Contract name (Solidity identifier)", placeholder "MyToken", defaultValue "MyToken", validate isSolidityIdentifier. Newbie explain before. Returns name:string.
    3. PROMPT 2 — text, message "Token symbol (1-11 ASCII letters/digits)", placeholder "MTK", defaultValue "MTK", validate isAsciiSymbol. Newbie explain before. Returns symbol:string.
    4. PROMPT 3 — text, message "Initial supply (human-readable, e.g. 1000000 or 1.5)", placeholder "1000000", defaultValue "1000000", validate isNonNegativeDecimal. Newbie explain before. Returns premint:string.
    5. PROMPT 4 — confirm, message "Enable Mintable? (an authorized account can mint more tokens after deploy)", initialValue false. Newbie explain before. Returns mintable:boolean.
    6. PROMPT 5 — confirm, message "Enable Burnable? (holders can burn their own tokens)", initialValue false. Newbie explain before. Returns burnable:boolean.
    7. PROMPT 6 — confirm, message "Enable Pausable? (an authorized account can freeze all transfers)", initialValue false. Newbie explain before. Returns pausable:boolean.
    8. PROMPT 7 (CONDITIONAL: only if mintable||pausable) — select, message "Access control style:", options [{value:"ownable", label:"Ownable — one address controls Mint/Pause"}, {value:"roles", label:"AccessControl — separate MINTER_ROLE / PAUSER_ROLE / DEFAULT_ADMIN_ROLE"}], initialValue "ownable". Newbie explain before. Returns access:"ownable"|"roles". If skipped, access:false.
    9. CENTRALIZATION WARNING — if mintable && access==="ownable", call `io.output.warn(<UI-SPEC §Post-prompt copy verbatim>)`. Fires in all modes (output.warn is always-on per UI-13 / Phase 1 contract).
    10. RETURN Erc20Opts.

    On Ctrl+C / cancel at ANY prompt: cancelGuard throws CliError(ERR_WIZARD_CANCEL, exitCode:130) with the WHAT field set to "Wizard cancelled at: <promptName>." where <promptName> is one of "contract name" / "token symbol" / "initial supply" / "mintable" / "burnable" / "pausable" / "access control" per UI-SPEC §E_WIZARD_CANCEL line 270.

    Non-cancellation errors are NOT caught here — they bubble up to the dispatcher's top-level handler in Plan 04 / Phase 1 cli.ts.
  </behavior>
  <action>
    Create `src/templates/erc20/wizard.ts`. Single exported function `runWizard(io: WizardIo): Promise<Erc20Opts>`.

    Imports:
    - `import { text, select, confirm, isCancel } from "@clack/prompts";` (named imports — Phase 1 prompt.ts already uses this shape; `@clack/prompts` is the existing dep)
    - `import { CliError, ERR_WIZARD_CANCEL } from "../../lib/errors.js";`
    - `import { isSolidityIdentifier, isAsciiSymbol, isNonNegativeDecimal } from "./validators.js";`
    - `import type { Erc20Opts, WizardIo } from "./opts.js";`

    Inline helper `cancelGuard<T>(answer: T | symbol, promptName: string): T` — paste verbatim from RESEARCH §Wizard Cancel Handling lines ~735-746. Throws CliError with locked WHAT/WHY/FIX from UI-SPEC §E_WIZARD_CANCEL lines 263-269:
    - what: `Wizard cancelled at: ${promptName}.`
    - why: `You pressed Ctrl+C or otherwise dismissed the prompt.`
    - fix: `Re-run 'smartc create --template erc20' to start over.`
    - exitCode: 130
    Kept inline per UI-SPEC Components Inventory (do NOT hoist to src/lib/wizard.ts in this phase — Phase 4 hoists when ERC-721 arrives as the second consumer).

    Pre-flight newbie preamble (paste verbatim from UI-SPEC §Pre-flight lines 65-69):
    - `io.output.explain("ERC-20 is the fungible-token standard on Ethereum and EVM chains. This wizard asks for the basics, then a few optional features.");`
    - `io.output.reference("EIP-20 spec", "https://eips.ethereum.org/EIPS/eip-20");`
    - `io.output.reference("OpenZeppelin ERC20 docs", "https://docs.openzeppelin.com/contracts/5.x/erc20");`
    Newbie/json gating is HANDLED by the Output factory (Phase 1 contract) — wizard.ts NEVER branches on newbie mode itself. Just call the channel; the factory decides whether to write.

    Each of the seven prompts: emit the per-prompt newbie explain line FIRST (verbatim from UI-SPEC §Prompt N "Newbie explain (before prompt)" row), then await the @clack prompt, then `cancelGuard(answer, "<promptName>")`. Prompt 7 fires only when `mintable || pausable`.

    Prompt 7 select options shape (UI-SPEC §Prompt 7 lines 156-160):
    `[{ value: "ownable", label: "Ownable — one address controls Mint/Pause" }, { value: "roles", label: "AccessControl — separate MINTER_ROLE / PAUSER_ROLE / DEFAULT_ADMIN_ROLE" }]`. initialValue `"ownable"`. The select return type is inferred from the options' value union — `"ownable" | "roles"`.

    Centralization warning (UI-SPEC §Post-prompt — Centralization warning lines 168-180):
    ```
    if (opts.mintable && opts.access === "ownable") {
      io.output.warn(
        "Mintable + Ownable: a single key can mint unlimited tokens. " +
        "Consider AccessControl (multi-role) or transferring ownership to a multisig before deploy."
      );
    }
    ```
    Paste verbatim. UI-14: this is the ONLY output.warn call in Phase 2's happy path.

    Build and return `Erc20Opts`: include `access: false` when prompt 7 was skipped; include the user's `"ownable"` or `"roles"` selection otherwise. Field order: name, symbol, premint, mintable, burnable, pausable, access (matches Erc20Opts declaration order in opts.ts).

    Do NOT add a separate `validate(opts)` per CONTEXT D-06 — validators in @clack `validate` callbacks are sufficient.

    Do NOT branch on newbie mode anywhere — the Output factory handles gating (Phase 1 contract from 01-02-SUMMARY).

    No process.exit, no console.log — surfaces happen via `io.output` channels only.
  </action>
  <verify>
    <automated>npm run typecheck</automated>
    <automated>node -e "const t=require('fs').readFileSync('src/templates/erc20/wizard.ts','utf8'); for (const k of ['cancelGuard','ERR_WIZARD_CANCEL','isSolidityIdentifier','isAsciiSymbol','isNonNegativeDecimal','Contract name (Solidity identifier)','Token symbol (1-11 ASCII letters/digits)','Initial supply (human-readable','Enable Mintable?','Enable Burnable?','Enable Pausable?','Access control style:','Mintable + Ownable: a single key can mint unlimited tokens','EIP-20 spec','OpenZeppelin ERC20 docs','exitCode: 130']) if (!t.includes(k)) {console.error('missing:',k); process.exit(1)} console.log('ok')"</automated>
    <automated>node -e "const t=require('fs').readFileSync('src/templates/erc20/wizard.ts','utf8'); for (const k of ['contract name','token symbol','initial supply','mintable','burnable','pausable','access control']) if (!t.includes(k)) {console.error('missing promptName:',k); process.exit(1)} console.log('ok')"</automated>
    <automated>node -e "const t=require('fs').readFileSync('src/templates/erc20/wizard.ts','utf8'); const lower=t.toLowerCase(); for (const k of ['if (newbie','opts.newbie','process.stdout','console.log']) if (lower.includes(k.toLowerCase())) {console.error('forbidden:',k); process.exit(1)} console.log('no newbie/io leaks')"</automated>
  </verify>
  <done>
    `src/templates/erc20/wizard.ts` exists, exports `runWizard(io: WizardIo): Promise<Erc20Opts>`, contains the inline `cancelGuard<T>`, and includes the verbatim UI-SPEC-locked copy for all seven prompts + preamble + centralization warning + cancel error block. `npm run typecheck` clean. No newbie-mode branching or direct stdout/console.log writes — all output flows through `io.output.*`.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fill wizard.spec.ts — happy path, conditional step 7, cancel at each prompt, centralization warning, newbie preamble</name>
  <files>tests/templates/erc20/wizard.spec.ts</files>
  <read_first>
    - .planning/phases/02-erc-20-canary-template/02-RESEARCH.md (§Vitest 4 Mock Pattern ~lines 544-607 — canonical mocked-clack tests including happy path, conditional select, cancel branch)
    - .planning/phases/02-erc-20-canary-template/02-PATTERNS.md (§tests/templates/erc20/wizard.spec.ts — locked Vitest 4 ESM mock pattern; per-test method-call assertion shape; cancel-path assertion shape)
    - .planning/phases/02-erc-20-canary-template/02-UI-SPEC.md (the seven prompt names from UI-SPEC §E_WIZARD_CANCEL line 270 — "contract name", "token symbol", "initial supply", "mintable", "burnable", "pausable", "access control")
    - tests/prompt.spec.ts (canonical Phase 1 vi.mock + dynamic-import pattern + per-test method-call assertion + cancel-path assertion shape)
    - src/templates/erc20/wizard.ts (Task 1 of this plan — runWizard signature)
    - src/templates/erc20/opts.ts (Erc20Opts shape)
    - src/lib/output.ts (Output interface — for the makeMockOutput helper)
  </read_first>
  <behavior>
    Replace the `it.todo` placeholders from Plan 01 Task 3 with real assertions. Vitest reports all cases green.

    Cases (one `it` per case):
    1. "happy path — no flags set — returns Erc20Opts with access:false and skips prompt 7" → mock text×3 returning "MyToken","MTK","1000000"; mock confirm×3 returning false,false,false. Assert select NOT called. Assert returned opts deep-equals `{ name:"MyToken", symbol:"MTK", premint:"1000000", mintable:false, burnable:false, pausable:false, access:false }`. Assert output.warn NOT called.
    2. "step 7 fires when mintable is true and returns 'ownable' selection" → mintable=true (4th confirm), burnable=false, pausable=false; select returns "ownable". Assert opts.access === "ownable" AND select called once. AND output.warn called with the locked centralization string (Mintable+Ownable).
    3. "step 7 fires when pausable is true and returns 'roles' selection" → mintable=false, burnable=false, pausable=true; select returns "roles". Assert opts.access === "roles" AND select called. Assert output.warn NOT called (Pausable+Roles does not warn per UI-14).
    4. "step 7 returns 'roles' when both mintable and pausable are true and roles is selected" → mintable=true, pausable=true; select returns "roles". Assert opts.access === "roles". Assert output.warn NOT called (Mintable+Roles is the MITIGATION per UI-14 — multi-role IS the safer path; only Mintable+Ownable warns).
    5. "step 7 select is configured with both ownable and roles options" → mintable=true; capture the select call's options argument and assert it deep-equals `[{ value:"ownable", label:"Ownable — one address controls Mint/Pause" }, { value:"roles", label:"AccessControl — separate MINTER_ROLE / PAUSER_ROLE / DEFAULT_ADMIN_ROLE" }]`.
    6. "prompt 1 message and validator are wired correctly" → make text.mock.calls[0][0] inspectable; assert `message === "Contract name (Solidity identifier)"`, `placeholder === "MyToken"`, `defaultValue === "MyToken"`, and `arg.validate("3Bad")` returns the locked Solidity-identifier error string.
    7. "prompt 2 message and validator wired" → analogous for prompt 2 (symbol).
    8. "prompt 3 message and validator wired" → analogous for prompt 3 (premint).
    9-15. "cancel at <promptName> throws E_WIZARD_CANCEL exit 130 with promptName in WHAT" → one test per prompt (seven tests). For each: mock the relevant prompt to return Symbol("cancel"), mock `isCancel` to return true on that call, run runWizard, assert rejects with `{ code: "E_WIZARD_CANCEL", exitCode: 130 }` and that the `what` contains the prompt name. (Use the pattern from tests/prompt.spec.ts lines ~65-72.)
    16. "newbie preamble fires before prompt 1" → make output.explain and output.reference into vi.fn()s on the mock Output; assert explain called with the locked preamble string at least ONCE before text.mock.calls[0]; assert reference called with both ("EIP-20 spec", "https://eips.ethereum.org/EIPS/eip-20") AND ("OpenZeppelin ERC20 docs", "https://docs.openzeppelin.com/contracts/5.x/erc20"). NOTE: Output factory's newbie gating is irrelevant in this unit test — wizard.ts calls the channels unconditionally and the mock captures every call.
    17. "centralization warning fires for Mintable+Ownable" — covered by case 2 above; restate as a dedicated case for clarity. Assert the warn argument string contains "Mintable + Ownable: a single key can mint unlimited tokens".
  </behavior>
  <action>
    Replace the Plan 01 Task 3 skeleton with a fully-implemented spec. Use the LOCKED Phase 1 Vitest 4 ESM mock pattern from `tests/prompt.spec.ts` lines 1-23 + extensions for `text` and `select` (RESEARCH §Vitest 4 Mock Pattern lines 552-579).

    Helper at the top of the file: `function makeMockOutput(): Output` returns an Output where every method is a `vi.fn()`. Used by every test for inspecting which channels were called and with what args.

    Mock factory:
    ```
    vi.mock("@clack/prompts", () => ({
      text: vi.fn(), select: vi.fn(), confirm: vi.fn(),
      multiselect: vi.fn(), isCancel: vi.fn(() => false), cancel: vi.fn(),
    }));
    const { runWizard } = await import("../../../src/templates/erc20/wizard.js");
    const clack = await import("@clack/prompts");
    ```

    `beforeEach` resets every mock and re-defaults `isCancel` to false. Each test sets up `mockResolvedValueOnce`s in the order the wizard calls prompts (three texts, three confirms, optional one select). For cancel cases, set `isCancelMock.mockReturnValueOnce(true)` for the relevant call and have that prompt's `mockResolvedValueOnce(Symbol("cancel"))`.

    For the select-options-shape case (case 5), capture via:
    ```
    const arg = selectMock.mock.calls[0]?.[0] as { message: string; options: Array<{value:string;label:string}>; initialValue: string };
    expect(arg.message).toBe("Access control style:");
    expect(arg.options).toEqual([...]);
    expect(arg.initialValue).toBe("ownable");
    ```

    For validator-wiring cases (6-8), capture via:
    ```
    const arg = textMock.mock.calls[0]?.[0] as { message: string; placeholder: string; defaultValue: string; validate: (v:string|undefined)=>string|undefined };
    expect(arg.validate("3Bad")).toBe("Must start with a letter or underscore, contain only letters/digits/underscores, max 64 chars.");
    ```

    For cancel-tests (9-15), follow the tests/prompt.spec.ts pattern verbatim:
    ```
    textMock.mockResolvedValueOnce(Symbol("cancel"));
    isCancelMock.mockReturnValueOnce(true);
    await expect(runWizard({ output: makeMockOutput() })).rejects.toMatchObject({
      code: "E_WIZARD_CANCEL",
      exitCode: 130,
    });
    ```
    And separately assert the WHAT field includes the prompt name (e.g., `"contract name"`).

    For the centralization-warning case: `const warnArg = (mockOutput.warn as Mock).mock.calls[0]?.[0]; expect(warnArg).toContain("Mintable + Ownable: a single key can mint unlimited tokens");`.

    No real TTY interaction, no fs writes — fully in-process and deterministic per RESEARCH §e2e Test Strategy.
  </action>
  <verify>
    <automated>npx vitest run tests/templates/erc20/wizard.spec.ts --reporter=verbose</automated>
    <automated>node -e "const t=require('fs').readFileSync('tests/templates/erc20/wizard.spec.ts','utf8'); for (const k of ['vi.mock(\"@clack/prompts\"','await import','runWizard','E_WIZARD_CANCEL','exitCode: 130','Mintable + Ownable','contract name','token symbol','initial supply','access control','EIP-20 spec','Access control style:']) if (!t.includes(k)) {console.error('missing:',k); process.exit(1)} console.log('ok')"</automated>
    <automated>node -e "const t=require('fs').readFileSync('tests/templates/erc20/wizard.spec.ts','utf8'); if (t.includes('it.todo')) {console.error('still has it.todo placeholders'); process.exit(1)} console.log('all todos replaced')"</automated>
  </verify>
  <done>
    `npx vitest run tests/templates/erc20/wizard.spec.ts` is fully green with all 17+ cases passing; zero `it.todo` remaining. The seven cancel-path tests assert the locked WHAT/WHY/FIX block + exitCode 130. The centralization-warning test asserts output.warn is called with the UI-SPEC-locked string. The newbie-preamble test asserts explain + both references fire before prompt 1. The select-options-shape test asserts both "ownable" and "roles" options with the locked labels.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user keyboard → wizard | All input must pass through validators before becoming `Erc20Opts`. |
| wizard.ts → cancelGuard | Every @clack return value MUST pass through cancelGuard before being treated as a value (defense against the cancel sentinel being interpreted as a string/boolean). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03 | Tampering | Wizard input → command injection (special chars escape Solidity-string contexts) | mitigate | Validators in `validators.ts` (Plan 02) reject characters that could escape; `erc20.print` constructs Solidity source programmatically (Plan 02 generate.ts) — two layers. wizard.ts wires the validators into @clack `validate` callbacks (prompts 1, 2, 3). |
| T-02-06 | Information Disclosure (on-chain, in deployed contract) | Mintable+Ownable centralization risk | mitigate | UI-02 / UI-14: `output.warn` fires non-silenceably (default, newbie, AND `--json` modes — `output.warn` is always-on per Phase 1 contract). Tested by case 17 of wizard.spec.ts which asserts the exact warn string. Phase 5 DEPLOY.md will expand the centralization-warning surface (deferred per CONTEXT specifics). |
| T-02-07 | Denial of Service (UX) | User presses Ctrl+C mid-wizard | mitigate | cancelGuard throws CliError(E_WIZARD_CANCEL, exitCode:130) — Unix SIGINT convention. Tested by cancel-path cases 9-15 (one per prompt). The top-level handler in src/cli.ts renders the three-part block and exits 130. |
| T-02-08 | Repudiation (silent failure mode) | Centralization warning silenced under --json | accept | Per UI-13 / Phase 1 contract, `output.warn` is ALWAYS-ON — including under --json. Phase 2 inherits this design; --json doesn't reach the wizard anyway because dispatcher (Plan 04) refuses `create --json` with E_USAGE per UI-10. Documented for clarity. |
</threat_model>

<verification>
- `npx vitest run tests/templates/erc20/wizard.spec.ts` — green, all cases pass, zero `it.todo`.
- `npx vitest run` — full suite still green (Phase 1 specs + Wave 1 Plan 02 + this plan).
- `npm run typecheck` — clean.
- `npm run build` — clean.
- Source-level audit: no `if (newbie)` branches in wizard.ts; no direct stdout/stderr writes; no process.exit.
- Every output.warn call site is the UI-SPEC §Post-prompt centralization-warning block — UI-14 ("only output.warn call in Phase 2's happy path") observed.
</verification>

<success_criteria>
1. ERC20-01 / ERC20-02 / ERC20-03 / ERC20-04: wizard collects name/symbol/premint and three flag confirms, returning a well-typed Erc20Opts. Tested by happy-path case + validator-wiring cases 6-8.
2. ERC20-05: when mintable||pausable, prompt 7 fires with exactly two options (ownable, roles) and returns the user's selection. Tested by cases 2-5. When neither, prompt 7 is skipped and access:false. Tested by case 1.
3. Centralization warning fires non-silenceably for Mintable+Ownable only. Tested by case 2 + case 17 (the explicit warn-content assertion).
4. Cancel at any of seven prompts throws CliError(E_WIZARD_CANCEL, exitCode:130) with the prompt name in WHAT. Tested by cases 9-15.
5. Newbie-mode preamble (1 explain + 2 reference calls with locked URLs) fires before prompt 1. Tested by case 16.
6. wizard.ts never branches on newbie mode — gating happens in the Output factory (Phase 1 contract preserved). Verified by the source-level audit grep in Task 1 verify step.
</success_criteria>

<output>
Create `.planning/phases/02-erc-20-canary-template/02-03-SUMMARY.md` when done. Include: line counts for wizard.ts, the seven cancel-path test names, the exact warn string asserted, and confirmation that no `if (newbie)` branches exist in wizard.ts.
</output>
