---
phase: 02-erc-20-canary-template
plan: 04
type: execute
wave: 2
depends_on: [02-02, 02-03]
files_modified:
  - src/templates/erc20/index.ts
  - src/commands/create.ts
  - src/program.ts
  - src/cli.ts
  - tests/commands/create.spec.ts
  - tests/registry.spec.ts
autonomous: true
requirements: [ERC20-01, ERC20-02, ERC20-03, ERC20-04, ERC20-05]
tags: [dispatcher, registry, integration, canary-retirement, e_usage-json-refusal]

must_haves:
  truths:
    - "registerErc20Template() registers the `erc20` template with the Phase 1 locked five-field shape PLUS the new runWizard and generate methods"
    - "`smartc list-templates` shows exactly ONE row (the erc20 template) — foundation-smoke canary is retired"
    - "`smartc create --template erc20` runs the full pipeline: registry lookup → runWizard → generate → confirmOverwrite → fs.writeFile → output.result + nextStep"
    - "`smartc create --template erc20 --json` is refused early with CliError(E_USAGE, exitCode:2) per UI-10 — locked WHAT/WHY/FIX copy"
    - "`smartc create --template <unknown-id>` throws CliError(E_USAGE) suggesting list-templates"
    - "`smartc create` without --template is refused with CliError(E_USAGE, exitCode:2) — Phase 2 ships one template (erc20) and requires explicit selection; Phase 4 (multi-template) introduces the interactive picker"
    - "Wizard cancel propagates as CliError(E_WIZARD_CANCEL, exitCode:130) through to the top-level cli.ts handler"
    - "Phase 3 splice point exists between template.generate() and confirmOverwrite — exactly one line of code where compile-verify will be inserted (per UI-SPEC Coordination Seams)"
  artifacts:
    - path: "src/templates/erc20/index.ts"
      provides: "registerErc20Template() factory — idempotent registration with locked five fields + runWizard + generate"
      contains: "registerErc20Template"
    - path: "src/commands/create.ts"
      provides: "createCommand() (renamed from createCommandStub) with dispatcher .action() body"
      contains: "tpl.runWizard"
    - path: "src/cli.ts"
      provides: "Boot sequence calling registerErc20Template() — foundation-smoke import dropped"
      contains: "registerErc20Template"
  key_links:
    - from: "src/cli.ts"
      to: "src/templates/erc20/index.ts"
      via: "registerErc20Template() called at boot (replaces registerStubTemplates)"
      pattern: "registerErc20Template"
    - from: "src/commands/create.ts"
      to: "src/templates/erc20/index.ts"
      via: "registry.get('erc20') returns the template; dispatcher calls runWizard + generate"
      pattern: "tpl\\.runWizard"
    - from: "src/commands/create.ts"
      to: "src/lib/prompt.ts"
      via: "confirmOverwrite called after generate, before fs.writeFile"
      pattern: "confirmOverwrite"
---

<objective>
Wire the ERC-20 template plugin into the CLI: register it via a template factory, replace the Phase 1 create command stub with the real dispatcher, and retire the foundation-smoke canary. Lock the integration behavior with an in-process command spec.

Purpose: Delivers all five Phase 2 requirements end-to-end. This is the wave where the previously-built pieces (validators + filename + generate from Plan 02 + wizard from Plan 03) compose into a working `smartc create --template erc20`. Also locks the Phase 3 splice seam (per UI-SPEC Coordination Seams) so compile-verify can insert in Phase 3 without modifying the plugin contract.

Output:
- `src/templates/erc20/index.ts` — `registerErc20Template()` factory (PATTERNS §src/templates/erc20/index.ts pattern, copying src/registry/stub.ts shape).
- `src/commands/create.ts` — Phase 1 `createCommandStub` renamed to `createCommand`; `.action()` body replaced with the dispatcher (RESEARCH §Pattern 3 lines 311-358 verbatim skeleton). `--template` is now REQUIRED (no silent default to erc20); missing-flag → CliError(E_USAGE).
- `src/program.ts` — function-name import updated (createCommandStub → createCommand).
- `src/cli.ts` — `registerStubTemplates()` import + call replaced with `registerErc20Template()` import + call (one-line swap per CONTEXT D-03 / PATTERNS).
- `tests/commands/create.spec.ts` — NEW in-process command spec covering the full dispatcher path (registry hit, wizard run, generate, overwrite, write).
- `tests/registry.spec.ts` — minor edit: PRESERVE the two foundation-smoke tests at lines 70-82 verbatim (registerStubTemplates still exists in src/registry/stub.ts as a pattern reference; Task 3 of this plan does NOT delete the stub file). ADD a NEW test case AFTER them registering registerErc20Template and asserting id="erc20", status="alpha", typeof runWizard === "function", typeof generate === "function".
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-erc-20-canary-template/02-CONTEXT.md
@.planning/phases/02-erc-20-canary-template/02-RESEARCH.md
@.planning/phases/02-erc-20-canary-template/02-PATTERNS.md
@.planning/phases/02-erc-20-canary-template/02-UI-SPEC.md
@.planning/phases/02-erc-20-canary-template/02-VALIDATION.md
@.planning/phases/02-erc-20-canary-template/02-02-PLAN.md
@.planning/phases/02-erc-20-canary-template/02-03-PLAN.md
@.planning/phases/01-cli-foundation/01-04-SUMMARY.md
@src/commands/create.ts
@src/cli.ts
@src/program.ts
@src/registry/index.ts
@src/registry/stub.ts
@src/lib/prompt.ts
@src/lib/output.ts
@src/lib/errors.ts
@src/templates/erc20/opts.ts
@tests/commands/list-templates.spec.ts
@tests/registry.spec.ts

<interfaces>
<!-- Contracts consumed by this plan. -->

From src/registry/index.ts (Phase 1):
```
register(t: Template): void   // throws on duplicate id
list(): Template[]
get(id: string): Template | undefined
clear(): void   // test-isolation only
```

From src/templates/erc20/wizard.ts (Plan 03):
```
export function runWizard(io: WizardIo): Promise<Erc20Opts>;
```

From src/templates/erc20/generate.ts (Plan 02):
```
export function generate(opts: Erc20Opts): GenerateResult; // { filename, source }
```

From src/lib/prompt.ts (Phase 1):
```
export async function confirmOverwrite(path: string, opts: { force?: boolean }): Promise<boolean>;
// Throws CliError(E_FILE_EXISTS) on user refusal or cancel. Honors force.
```

Locked Phase 1 createCommandStub option surface (DO NOT CHANGE — per 01-04-SUMMARY.md notes for Phase 2):
- `.addOption(new Option("--template <id>", "Skip template picker; use this template directly"))`
- `.addOption(new Option("--out <path>", "Write generated file to this path (default: ./<name>.sol)"))`

Global commander opts available via optsWithGlobals() (Phase 1 contract):
- `template?: string; out?: string; newbie?: boolean; json?: boolean; force?: boolean; color?: boolean`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create registerErc20Template() factory in src/templates/erc20/index.ts</name>
  <files>src/templates/erc20/index.ts</files>
  <read_first>
    - src/registry/stub.ts (the analog factory — copy import shape, idempotency guard, five-field literal)
    - src/registry/index.ts (register/get/list contract)
    - src/templates/erc20/opts.ts (Erc20Template interface — Plan 01)
    - src/templates/erc20/wizard.ts (Plan 03 — exports runWizard)
    - src/templates/erc20/generate.ts (Plan 02 — exports generate)
    - .planning/phases/02-erc-20-canary-template/02-UI-SPEC.md (UI-09: locked five-field literal — id="erc20", name="ERC-20 Token", chain="evm", status="alpha", description="Fungible token (ERC-20) on EVM chains. Opt-in Mintable/Burnable/Pausable.")
    - .planning/phases/02-erc-20-canary-template/02-PATTERNS.md (§src/templates/erc20/index.ts — locked factory shape with `if (!get("erc20")) register(tpl)` idempotency guard)
    - .planning/phases/02-erc-20-canary-template/02-RESEARCH.md (§Pattern 2 lines ~272-305 — Erc20Template extends Template; §Code Examples lines ~1059-1071 — verbatim factory)
  </read_first>
  <behavior>
    - First call to `registerErc20Template()` calls `registry.register(tpl)` with `tpl: Erc20Template`.
    - Second/Nth call is a no-op (idempotency guard via `if (!get("erc20"))`) — required so test isolation that doesn't `clear()` first won't throw.
    - The registered template's five-field literal matches UI-09 exactly.
    - The registered template's `runWizard` and `generate` are the functions exported from `./wizard.js` and `./generate.js`.
    - `registry.list()` after this call returns exactly one entry (assuming the registry was clear of foundation-smoke — Task 3 of this plan retires it from src/cli.ts).
  </behavior>
  <action>
    Create `src/templates/erc20/index.ts`. Verbatim skeleton from RESEARCH §Code Examples lines ~1059-1071, adapted to PATTERNS §Idempotent registration (lines ~745-757).

    Imports:
    - `import { register, get } from "../../registry/index.js";`
    - `import type { Template } from "../../registry/types.js";`
    - `import { runWizard } from "./wizard.js";`
    - `import { generate } from "./generate.js";`
    - `import type { Erc20Opts } from "./opts.js";`

    The factory:
    ```
    export function registerErc20Template(): void {
      if (get("erc20")) return;
      const tpl: Template<Erc20Opts> = {
        id: "erc20",
        name: "ERC-20 Token",
        chain: "evm",
        status: "alpha",
        description: "Fungible token (ERC-20) on EVM chains. Opt-in Mintable/Burnable/Pausable.",
        runWizard,
        generate,
      };
      register(tpl);
    }
    ```

    JSDoc above the function: "Registers the ERC-20 canary template. Idempotent — safe to call multiple times. Phase 2 retires the foundation-smoke stub; this is the first real template plugin and the model for ERC-721/ERC-1155/SPL."

    Do NOT export `runWizard`/`generate` from this barrel — keep them internal to the template module so the dispatcher only depends on the registry-returned `Template` shape (D-05 opacity discipline).

    The locked five-field literal (UI-09) must be byte-exact — `list-templates` table assertions and the JSON shape lock both check it.
  </action>
  <verify>
    <automated>npm run typecheck</automated>
    <automated>node -e "const t=require('fs').readFileSync('src/templates/erc20/index.ts','utf8'); for (const k of ['registerErc20Template','if (get(\"erc20\"))','id: \"erc20\"','name: \"ERC-20 Token\"','chain: \"evm\"','status: \"alpha\"','Fungible token (ERC-20) on EVM chains. Opt-in Mintable/Burnable/Pausable.','runWizard,','generate,']) if (!t.includes(k)) {console.error('missing:',k); process.exit(1)} console.log('ok')"</automated>
  </verify>
  <done>
    `src/templates/erc20/index.ts` exists with the idempotent `registerErc20Template()` factory. Five-field literal matches UI-09 byte-exact. typecheck clean.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rewrite src/commands/create.ts dispatcher .action() body</name>
  <files>src/commands/create.ts, src/program.ts</files>
  <read_first>
    - src/commands/create.ts (existing createCommandStub — Phase 1; the option surface lines 8-12 is LOCKED)
    - src/commands/list-templates.ts (canonical `.action(function (this: Command) { ... })` shape; optsWithGlobals usage; --no-color detection via `opts.color === false`)
    - src/program.ts (imports createCommandStub — needs rename)
    - src/lib/prompt.ts (confirmOverwrite contract — throws CliError on refusal; honors force)
    - src/lib/output.ts (makeOutput factory + Output contract)
    - src/lib/color.ts (makeColor — `--no-color` detection)
    - src/lib/env.ts (resolveNewbie — flag-over-env precedence)
    - src/lib/errors.ts (CliError + ERR_USAGE + ERR_WIZARD_CANCEL + ERR_NOT_IMPLEMENTED)
    - src/registry/index.ts (get(id) returns Template | undefined)
    - .planning/phases/02-erc-20-canary-template/02-RESEARCH.md (§Pattern 3 dispatcher delta ~lines 307-358 — LOCKED skeleton; §Open Q3 / e2e Test Strategy ~lines 856-859 on in-process testing)
    - .planning/phases/02-erc-20-canary-template/02-UI-SPEC.md (UI-05: post-generation footer copy LOCKED; UI-10: --json refusal copy LOCKED; UI-05 honesty framing — no "Created"/"Generated"/"Ready to deploy")
    - .planning/phases/02-erc-20-canary-template/02-PATTERNS.md (§src/commands/create.ts — replace .action() body using RESEARCH Pattern 3 verbatim; function rename createCommandStub → createCommand; use `function (this: Command)` not arrow)
    - .planning/phases/02-erc-20-canary-template/02-CONTEXT.md (D-03 / D-04 / D-05 — opacity discipline at registry.get boundary; D-06 — no validate(opts))
  </read_first>
  <behavior>
    - `smartc create --template erc20 --out /tmp/X.sol` runs full pipeline and writes file at /tmp/X.sol.
    - `smartc create --template erc20` writes file at `<cwd>/<contractNameSlug>.sol` (filename from template.generate's returned filename).
    - `smartc create` WITHOUT `--template` throws CliError(E_USAGE, exitCode:2). WHAT="Missing --template flag.", WHY="`smartc create` requires --template in Phase 2 (one template ships: erc20). Phase 4 introduces the interactive multi-template picker.", FIX="Re-run with `--template erc20`. Run `smartc list-templates` to see available templates."
    - `smartc create --template <unknown>` throws CliError(E_USAGE) with WHAT mentioning the unknown id; WHY explaining; FIX pointing at `smartc list-templates`.
    - `smartc create --template <id-without-generate>` (e.g., a future stub-only template) throws CliError(E_USAGE) per UI: "Template 'foo' is not generatable" (RESEARCH Pattern 3 line ~327).
    - `smartc create --template erc20 --json` throws CliError(E_USAGE, exitCode:2) per UI-10 locked copy — fires BEFORE the registry lookup so no wizard side-effects.
    - When confirmOverwrite refuses, CliError(E_FILE_EXISTS) bubbles unchanged (Phase 1 contract).
    - When runWizard cancels, CliError(E_WIZARD_CANCEL, exitCode:130) bubbles unchanged (Plan 03).
    - On success: `output.result("Wrote " + outPath)` + two `output.nextStep` lines (UI-05 locked copy).
    - The Phase 3 splice seam is exactly ONE line between `const { filename, source } = template.generate(opts);` and the next code line — marked with the LOCKED comment block per UI-SPEC §Coordination Seams.
  </behavior>
  <action>
    Edit `src/commands/create.ts` and `src/program.ts`.

    In `src/commands/create.ts`:
    1. Rename `createCommandStub()` → `createCommand()` (export name change) per PATTERNS §src/commands/create.ts.
    2. KEEP the option surface unchanged (`--template <id>`, `--out <path>`) — locked from Phase 1 per 01-04-SUMMARY.md.
    3. KEEP `new Command("create").description("Launch the interactive wizard to scaffold a new contract")` unchanged.
    4. REPLACE the `.action(() => { throw ... E_NOT_IMPLEMENTED ... })` body with the dispatcher from RESEARCH §Pattern 3 lines ~314-357, adapted with the W3 amendment below.
    5. The .action callback uses `async function (this: Command) { ... }` (per PATTERNS §src/commands/create.ts and list-templates.ts analog).

    Imports needed at top of file:
    - `import { Command, Option } from "commander";` (existing)
    - `import { existsSync } from "node:fs";`
    - `import { writeFile } from "node:fs/promises";`
    - `import path from "node:path";`
    - `import { CliError, ERR_USAGE } from "../lib/errors.js";`
    - `import { makeColor } from "../lib/color.js";`
    - `import { makeOutput } from "../lib/output.js";`
    - `import { resolveNewbie } from "../lib/env.js";`
    - `import { get as getTemplate } from "../registry/index.js";`
    - `import { confirmOverwrite } from "../lib/prompt.js";`
    - Drop `import { CliError, ERR_NOT_IMPLEMENTED } from "../lib/errors.js";` — ERR_NOT_IMPLEMENTED is no longer thrown from create (it remains in errors.ts for forward use).

    Dispatcher body (paste from RESEARCH Pattern 3, adapted: --json refusal FIRST, then explicit --template required-flag check, then registry lookup):
    ```
    cmd.action(async function (this: Command) {
      const globalOpts = this.optsWithGlobals() as {
        template?: string; out?: string; newbie?: boolean;
        json?: boolean; force?: boolean; color?: boolean;
      };

      // UI-10: --json + create is refused early (wizard requires TTY, mode is in Deferred Ideas).
      if (globalOpts.json) {
        throw new CliError({
          code: ERR_USAGE,
          what: "'smartc create' cannot run in --json mode.",
          why: "The wizard requires an interactive TTY, which is incompatible with machine-readable output.",
          fix: "Re-run without --json. Flag-driven non-interactive generation is planned for a future release; track it in .planning/STATE.md.",
          exitCode: 2,
        });
      }

      // Phase 2 requires explicit --template selection. Phase 4 introduces the interactive picker
      // when multiple templates ship; until then, refuse missing --template to avoid a silent
      // default that would mask the future picker decision.
      if (!globalOpts.template) {
        throw new CliError({
          code: ERR_USAGE,
          what: "Missing --template flag.",
          why: "`smartc create` requires --template in Phase 2 (one template ships: erc20). Phase 4 introduces the interactive multi-template picker.",
          fix: "Re-run with `--template erc20`. Run `smartc list-templates` to see available templates.",
          exitCode: 2,
        });
      }

      const noColor = globalOpts.color === false;
      const color = makeColor(noColor);
      const newbie = resolveNewbie({ newbieFlag: globalOpts.newbie });
      const output = makeOutput({ newbie, json: false, color });

      // 1. Resolve template — Phase 2 ships only --template lookup (no interactive picker yet; Phase 4 problem).
      const templateId = globalOpts.template;  // Phase 2: required-flag, no silent default.
      const tpl = getTemplate(templateId);
      if (!tpl) {
        throw new CliError({
          code: ERR_USAGE,
          what: `Template '${templateId}' was not found in the registry.`,
          why: "The --template id does not match any registered template.",
          fix: "Run 'smartc list-templates' to see available templates.",
          exitCode: 2,
        });
      }
      if (!tpl.runWizard || !tpl.generate) {
        throw new CliError({
          code: ERR_USAGE,
          what: `Template '${tpl.id}' is not generatable (status: ${tpl.status}).`,
          why: "This template is registered for discoverability but has no generator wired.",
          fix: "Run 'smartc list-templates' to see generatable templates.",
          exitCode: 2,
        });
      }

      // 2. Wizard — runs prompts; throws E_WIZARD_CANCEL on Ctrl+C.
      const opts = await tpl.runWizard({ output });

      // 3. Generate — pure transform, no I/O.
      const { filename, source } = tpl.generate(opts);

      // ◄─── PHASE 3 SPLICE POINT: compileVerify(source, tpl.chain) inserts HERE per UI-SPEC §Coordination Seams ───►

      // 4. Resolve output path.
      const outPath = globalOpts.out ?? path.resolve(process.cwd(), filename);

      // 5. Overwrite gate (Phase 1 contract — confirmOverwrite respects --force and throws CliError(E_FILE_EXISTS) on refusal).
      if (existsSync(outPath)) {
        await confirmOverwrite(outPath, { force: globalOpts.force });
      }

      // 6. Write.
      await writeFile(outPath, source, "utf8");

      // 7. Surface result + newbie next steps (UI-05 locked copy).
      output.result(`Wrote ${outPath}`);
      output.nextStep("Run 'smartc list-templates' to see other templates.");
      output.nextStep("Phase 3 will add automatic compile-verify before write — for now the .sol references @openzeppelin/contracts which you'll need installed to compile.");
    });
    ```

    The UI-05 footer strings are LOCKED (UI-SPEC §Post-Generation Footer ~lines 215-230). Paste byte-exact.

    The Phase 3 splice comment block is exactly one line; the executor MUST NOT add extra commentary above or below it — UI-SPEC Coordination Seams obligates "exactly one line of code as the splice point" (Phase 5 SUMMARY referenced too in 01-04 notes).

    In `src/program.ts`: update the import name `createCommandStub` → `createCommand` and the `.addCommand(createCommandStub())` → `.addCommand(createCommand())` accordingly. That is the only change to program.ts.
  </action>
  <verify>
    <automated>npm run typecheck</automated>
    <automated>npm run build</automated>
    <automated>node -e "const t=require('fs').readFileSync('src/commands/create.ts','utf8'); for (const k of ['createCommand','tpl.runWizard','tpl.generate','confirmOverwrite','writeFile','\\'smartc create\\' cannot run in --json mode','Missing --template flag.','Wrote ${outPath}','Phase 3 will add automatic compile-verify','PHASE 3 SPLICE POINT','exitCode: 2']) if (!t.includes(k)) {console.error('missing:',k); process.exit(1)} console.log('ok')"</automated>
    <automated>node -e "const t=require('fs').readFileSync('src/commands/create.ts','utf8'); if (t.includes('ERR_NOT_IMPLEMENTED')) {console.error('still imports ERR_NOT_IMPLEMENTED'); process.exit(1)} if (t.includes('createCommandStub')) {console.error('still exports createCommandStub'); process.exit(1)} if (t.match(/globalOpts\\.template\\s*\\?\\?\\s*\"erc20\"/)) {console.error('silent default ?? \"erc20\" still present — W3 not fixed'); process.exit(1)} console.log('cleaned up')"</automated>
    <automated>node -e "const t=require('fs').readFileSync('src/program.ts','utf8'); if (t.includes('createCommandStub')) {console.error('program.ts still imports createCommandStub'); process.exit(1)} if (!t.includes('createCommand')) {console.error('program.ts missing createCommand'); process.exit(1)} console.log('ok')"</automated>
  </verify>
  <done>
    `createCommand()` factory exports the dispatcher with the full pipeline. `src/program.ts` updated to import the new name. `--json` refusal is the first check (UI-10). `--template` is now a required flag (no silent default — missing flag throws E_USAGE). Phase 3 splice point is marked with the exact-one-line comment block. UI-05 footer copy pasted byte-exact. `npm run build` produces a clean `dist/cli.js`.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Swap canary in src/cli.ts (registerStubTemplates → registerErc20Template)</name>
  <files>src/cli.ts</files>
  <read_first>
    - src/cli.ts (existing line 3 import + line 9 call — exactly two-line change per CONTEXT D-03 / PATTERNS §src/cli.ts)
    - .planning/phases/02-erc-20-canary-template/02-PATTERNS.md (§src/cli.ts — "Current line 3 / Current line 9" — exact replacement; "Do not change the rest of src/cli.ts")
    - .planning/phases/02-erc-20-canary-template/02-CONTEXT.md (D-03 + "Canary stub fate" — drop the import; "Phase 2 swaps the stub call for registerErc20Template — one-line change")
    - .planning/phases/02-erc-20-canary-template/02-RESEARCH.md (Pitfall 7 ~lines 985-990 — Drop the registerStubTemplates() call AND the import; spot-verify list-templates shows only `erc20`)
  </read_first>
  <behavior>
    - `smartc list-templates` shows exactly one row: `erc20`. The `foundation-smoke` canary is no longer registered at boot (the function still exists in src/registry/stub.ts as a pattern reference).
    - All Phase 1 cli.ts behavior preserved: error handling, SIGINT exit-130, top-level CliError rendering, commander usage-error exit-2 mapping.
    - Nothing else in src/cli.ts changes.
  </behavior>
  <action>
    Two-line edit in `src/cli.ts`:

    1. Line ~3 (the import): change `import { registerStubTemplates } from "./registry/stub.js";` → `import { registerErc20Template } from "./templates/erc20/index.js";`.
    2. Line ~9 (the call inside `main()`): change `registerStubTemplates();` → `registerErc20Template();`.

    Do NOT delete `src/registry/stub.ts` — it remains in the codebase as the canary-pattern analog for future template plugins (RESEARCH/PATTERNS treat it as the reference implementation). It's just no longer wired into the boot sequence. (Task 4 of this plan relies on this: the `tests/registry.spec.ts` foundation-smoke tests at lines 70-82 continue to exercise `registerStubTemplates` directly.)

    Do NOT touch the rest of src/cli.ts: error handler, SIGINT, parseAsync, commander.help/version exit code mapping all stay verbatim.

    Per RESEARCH Pitfall 7: AFTER this edit, run `node dist/cli.js list-templates` (rebuild first) and confirm only one row appears (`erc20`). If two rows appear, the stub import was not removed — investigate.
  </action>
  <verify>
    <automated>npm run build</automated>
    <automated>node -e "const t=require('fs').readFileSync('src/cli.ts','utf8'); if (t.includes('registerStubTemplates')) {console.error('still imports registerStubTemplates'); process.exit(1)} if (!t.includes('registerErc20Template')) {console.error('missing registerErc20Template'); process.exit(1)} console.log('ok')"</automated>
    <automated>node -e "const o=JSON.parse(require('child_process').execSync('node dist/cli.js list-templates --json',{encoding:'utf8'})); if(o.templates.length!==1){console.error('expected 1 template, got',o.templates.length,o);process.exit(1)} if(o.templates[0].id!=='erc20'){console.error('expected erc20, got',o.templates[0].id);process.exit(1)} if(o.templates[0].status!=='alpha'){console.error('expected alpha status, got',o.templates[0].status);process.exit(1)} console.log('canary retired, erc20 registered')"</automated>
  </verify>
  <done>
    src/cli.ts boots via registerErc20Template(). `node dist/cli.js list-templates --json` returns exactly one entry: id=erc20, status=alpha, matching UI-09 byte-exact. registerStubTemplates is no longer referenced anywhere except src/registry/stub.ts itself (which stays as the pattern analog) and tests/registry.spec.ts (which still exercises it directly via the foundation-smoke tests at lines 70-82).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: In-process create command spec (tests/commands/create.spec.ts) + registry test update</name>
  <files>tests/commands/create.spec.ts, tests/registry.spec.ts</files>
  <read_first>
    - tests/commands/list-templates.spec.ts (canonical in-process command spec — uses `parseAsync(args, { from: "user" })`; vi.spyOn(process.stdout, "write"); registry clear() in beforeEach)
    - tests/prompt.spec.ts (vi.mock("@clack/prompts", ...) + top-level await import — the canonical pattern for mocking the wizard's @clack calls in-process)
    - src/commands/create.ts (Task 2 of this plan — the dispatcher to spec; note: --template is required, missing throws E_USAGE)
    - src/templates/erc20/index.ts (Task 1 — registerErc20Template)
    - tests/registry.spec.ts (existing tests at lines 70-82 — the two foundation-smoke tests; these stay verbatim per B2)
    - .planning/phases/02-erc-20-canary-template/02-VALIDATION.md (per-task verification map — canary retirement row needs a test)
    - .planning/phases/02-erc-20-canary-template/02-PATTERNS.md (§tests for in-process command shape)
  </read_first>
  <behavior>
    All cases run in-process (no spawn). Cases:
    1. "happy path — registry hit, wizard returns minimal opts, writes file" — mock @clack/prompts to return ("MyToken","MTK","1000000",false,false,false); use `--out <tmpdir>/X.sol`; assert file exists with valid Solidity content (contains "contract MyToken"); assert stdout includes "Wrote ".
    2. "--template unknown — throws E_USAGE" — `parseAsync(["create","--template","does-not-exist"], { from: "user" })` rejects with `code: "E_USAGE"`, exitCode 2; the WHAT mentions the unknown id; the FIX mentions list-templates.
    3. "--json — refused with E_USAGE exit 2 BEFORE wizard runs" — assert @clack/prompts.text not called; assert thrown CliError has the locked UI-10 WHAT/WHY/FIX strings byte-exact.
    4. "create without --template → exit 2 with E_USAGE (W3)" — `parseAsync(["create"], { from: "user" })` rejects with `code: "E_USAGE"`, exitCode 2; the WHAT equals "Missing --template flag." (byte-exact); the FIX mentions `--template erc20` and `smartc list-templates`. Assert NO wizard side-effects (text/confirm/select not called).
    5. "wizard cancel — E_WIZARD_CANCEL propagates with exitCode 130" — mock first prompt to return Symbol("cancel") + isCancel true; assert reject with `{ code: "E_WIZARD_CANCEL", exitCode: 130 }`; assert NO file written.
    6. "overwrite refused — E_FILE_EXISTS propagates" — pre-create the target file; mock confirmOverwrite's underlying confirm to return false; assert reject with `code: "E_FILE_EXISTS"`; assert pre-existing file content unchanged.
    7. "--force bypasses overwrite prompt and overwrites" — pre-create target with stale content; pass --force; assert confirmOverwrite skipped (i.e., the @clack confirm called with overwrite-prompt-message NOT invoked); assert file now contains new content (`contract MyToken`).
    8. "Phase 3 splice point comment present" — read src/commands/create.ts as a file and assert the locked splice-point comment exists exactly once. (This is a structural-lock test, not a behavior test.)

    tests/registry.spec.ts updates (three sub-edits):
    - (1) KEEP the two foundation-smoke tests at lines 70-82 ("registers the foundation-smoke stub canary with correct shape" and "is idempotent when registerStubTemplates() is called twice") UNTOUCHED. They continue to exercise `registerStubTemplates` directly via the stub file (which still exists per Task 3). Their existence proves the stub pattern is still discoverable as a reference for future templates.
    - (2) ADD a NEW test case AFTER the foundation-smoke tests: "registers the ERC-20 template with id='erc20', status='alpha', and runWizard/generate as functions". Import `registerErc20Template` from `../src/templates/erc20/index.js`. Register on a fresh registry (the beforeEach `clear()` already ensures this), assert `list().length === 1`, `list()[0].id === "erc20"`, `list()[0].status === "alpha"`, `typeof (list()[0] as Record<string, unknown>).runWizard === "function"`, `typeof (list()[0] as Record<string, unknown>).generate === "function"`.
    - (3) DO NOT modify the relaxed-canary test at lines 58-68 (already updated by Plan 02-01 Task 2 Edit 5 — the "extras must be functions" canary). Leave it as-is.
  </behavior>
  <action>
    Create `tests/commands/create.spec.ts` as a new in-process command spec. Use the LOCKED Phase 1 Vitest 4 ESM mock pattern from tests/prompt.spec.ts + the optsWithGlobals pattern from tests/commands/list-templates.spec.ts.

    Mock factory (top of file, BEFORE imports of the SUT):
    ```
    vi.mock("@clack/prompts", () => ({
      text: vi.fn(), select: vi.fn(), confirm: vi.fn(),
      multiselect: vi.fn(), isCancel: vi.fn(() => false), cancel: vi.fn(),
    }));
    ```

    SUT imports (dynamic, AFTER the mock):
    ```
    const { createCommand } = await import("../../src/commands/create.js");
    const { buildProgram } = await import("../../src/program.js");
    const { register, clear, get } = await import("../../src/registry/index.js");
    const { registerErc20Template } = await import("../../src/templates/erc20/index.js");
    const clack = await import("@clack/prompts");
    ```

    `beforeEach`: `clear()` the registry, call `registerErc20Template()` so each test starts with the erc20 plugin registered (mirrors src/cli.ts boot — see PATTERNS §Test isolation via clear() in beforeEach). EXCEPTION: case 4 ("create without --template") must run BEFORE the dispatcher reaches registry lookup; since the missing-flag check fires first, the registered template is irrelevant for that case but the beforeEach is still safe to run.

    Build the program in each test via `buildProgram()` and call `program.parseAsync(["create", ...userArgs], { from: "user" })` per the 01-04-SUMMARY rectified pattern (no `["node","smartc"]` prefix).

    For the file-write cases, use `mkdtempSync(join(tmpdir(),"smartc-create-"))` for isolation; `rmSync(dir, { recursive: true, force: true })` in afterEach or in a try/finally.

    For the confirmOverwrite test, mock the `confirm` call in the mock factory to return false for that one case.

    For the structural splice-point test, simply `expect(readFileSync("src/commands/create.ts","utf8")).toMatch(/PHASE 3 SPLICE POINT/)` — confirms the seam is documented at source.

    For case 4 (missing --template): assert via `await expect(program.parseAsync(["create"], { from: "user" })).rejects.toMatchObject({ code: "E_USAGE", exitCode: 2, what: "Missing --template flag." })`. Then assert `clack.text` was not called (`expect(textMock).not.toHaveBeenCalled()`).

    Update `tests/registry.spec.ts` per the three sub-edits in the behavior block above:
    - Keep lines 70-82 verbatim.
    - ADD a new `it("registers the ERC-20 template with id='erc20', status='alpha', and runWizard/generate as functions", () => { ... })` block AFTER line 82, inside the existing `describe("registry", ...)`. The new test imports `registerErc20Template` at the top of the file (add to the existing import list), relies on `beforeEach`'s `clear()`, calls `registerErc20Template()`, and asserts the four properties.
    - Do not touch the relaxed-canary test at lines 58-68.
  </action>
  <verify>
    <automated>npx vitest run tests/commands/create.spec.ts tests/registry.spec.ts --reporter=verbose</automated>
    <automated>npx vitest run</automated>
    <automated>node -e "try{require('child_process').execSync('node dist/cli.js create --template does-not-exist',{encoding:'utf8',stdio:'pipe'});console.error('expected non-zero exit');process.exit(1)}catch(e){const blob=(e.stderr||'')+(e.stdout||''); if(!blob.includes('E_USAGE')){console.error('missing E_USAGE in:',blob);process.exit(1)} console.log('ok unknown-template')}"</automated>
    <automated>node -e "try{require('child_process').execSync('node dist/cli.js create --template erc20 --json',{encoding:'utf8',stdio:'pipe'});console.error('expected non-zero exit');process.exit(1)}catch(e){const blob=(e.stderr||'')+(e.stdout||''); if(!blob.includes('cannot run in --json mode')){console.error('missing UI-10 json refusal in:',blob);process.exit(1)} console.log('ok json-refusal')}"</automated>
    <automated>node -e "try{require('child_process').execSync('node dist/cli.js create',{encoding:'utf8',stdio:'pipe'});console.error('expected non-zero exit');process.exit(1)}catch(e){const blob=(e.stderr||'')+(e.stdout||''); if(!blob.includes('Missing --template flag')){console.error('missing W3 required-flag refusal in:',blob);process.exit(1)} console.log('ok missing-template')}"</automated>
    <automated>node -e "const t=require('fs').readFileSync('tests/registry.spec.ts','utf8'); for (const k of ['foundation-smoke','registerStubTemplates','registerErc20Template','status: \"alpha\"','typeof','runWizard']) if(!t.includes(k)){console.error('missing token:',k);process.exit(1)} console.log('ok')"</automated>
  </verify>
  <done>
    `tests/commands/create.spec.ts` passes all eight cases (including the new W3 missing-template case). `tests/registry.spec.ts` updated: lines 70-82 verbatim, new ERC-20 registration test added after them, lines 58-68 untouched. Full vitest suite passes. CLI smoke tests confirm --json refusal (UI-10), unknown-template error (E_USAGE), and missing-template error (W3). Canary is observably retired at the boot level: list-templates --json returns exactly one row (erc20/alpha).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `--out <path>` (user input) → `fs.writeFile` | Per RESEARCH §Security Domain, path traversal via `--out` is **accepted** — the user IS the trust boundary for a local dev tool. No extra check; document the accepted risk. |
| `--template <id>` (user input) → registry.get | Registry lookup is an exact-string-equality check; no glob/regex injection vector. |
| Wizard input → @clack validate → erc20.print | Defense in depth: validators in Plan 02 reject malformed input; `erc20.print` constructs Solidity strings programmatically (no concatenation). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-FS-01 | Tampering (filesystem) | `--out ../../etc/passwd` style path traversal | accept | Documented in RESEARCH §Security Domain — local dev tool, user is trust boundary, no extra check. fs.writeFile permissions are the OS-level mitigation. |
| T-02-09 | Tampering (downstream consumer) | Generated `.sol` references unpinned `@openzeppelin/contracts` | mitigate (Phase 3) / mitigate-now (UI-05 footer copy) | UI-05 footer nextStep #2 explicitly tells the user "you'll need @openzeppelin/contracts installed to compile" — honest disclosure per Phase 2's "honesty framing." Phase 3 bundles the pinned version. |
| T-02-10 | Repudiation | Silent failure path on --json | mitigate | UI-10 refuses early with CliError(E_USAGE, exitCode:2) and the LOCKED WHAT/WHY/FIX block. Tested by Task 4 case 3. No wizard side-effects occur in --json mode. |
| T-02-11 | Denial of Service (UX) | Unknown --template id | mitigate | CliError(E_USAGE) with FIX pointing at list-templates. Tested by Task 4 case 2. |
| T-02-13 | Repudiation | Missing --template silently defaulting to erc20 (legacy ergonomic) | mitigate | Removed in this plan per W3. The dispatcher now throws CliError(E_USAGE, exitCode:2) with WHAT="Missing --template flag." so the user is never surprised by a silent picker. Phase 4 introduces the interactive multi-template picker; until then, explicit is safer than implicit. Tested by Task 4 case 4. |
</threat_model>

<verification>
- `npx vitest run` — full suite green (Phase 1 + Wave 0 + Wave 1 + this plan).
- `npm run build` — clean.
- `node dist/cli.js list-templates --json` returns exactly one entry: `{ id:"erc20", name:"ERC-20 Token", chain:"evm", status:"alpha", description:"..." }` matching UI-09 byte-exact.
- `node dist/cli.js create --template does-not-exist` exits 2 with E_USAGE block.
- `node dist/cli.js create --json` exits 2 with the locked UI-10 WHAT/WHY/FIX block.
- `node dist/cli.js create` (no --template) exits 2 with the W3 missing-flag block.
- `src/commands/create.ts` contains the exact-one-line Phase 3 splice point comment block.
- `src/commands/create.ts` does NOT contain a `?? "erc20"` silent default for the template flag.
- `src/cli.ts` no longer references `registerStubTemplates`.
- `tests/registry.spec.ts` keeps the two foundation-smoke tests verbatim AND adds the ERC-20 registration test.
</verification>

<success_criteria>
1. ERC20-01..05 end-to-end: `smartc create --template erc20` walks the wizard and writes a `.sol` file at the chosen path with all five requirement combinations represented in the wizard's Erc20Opts.
2. Canary retired at boot: list-templates shows only `erc20`. The stub function still exists in src/registry/stub.ts and is still exercised by the foundation-smoke unit tests in tests/registry.spec.ts.
3. UI-10 --json refusal observable from the binary.
4. W3: --template is a required flag — missing throws CliError(E_USAGE, exitCode:2) with the locked WHAT/WHY/FIX block.
5. Phase 3 splice point exists at exactly one line in `src/commands/create.ts`.
6. All Phase 1 e2e tests (Phase 1 SC-1, SC-2, SC-3, SC-5) still pass with the dispatcher change.
7. SC-4 unit-level coverage (overwrite prompt + --force) lives in tests/commands/create.spec.ts cases 6 and 7; the e2e fill-in for tests/cli.spec.ts line 106 is Plan 05's job.
</success_criteria>

<output>
Create `.planning/phases/02-erc-20-canary-template/02-04-SUMMARY.md` when done. Include: list-templates --json output (one row), the five locked UI-09 fields, the exact UI-10 refusal string, the exact W3 missing-flag refusal string, the Phase 3 splice point line number in src/commands/create.ts, and a count of the test cases shipped in tests/commands/create.spec.ts.
</output>
</content>
</invoke>