---
phase: 05-deploy-md-generation
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - src/deploy/types.ts
  - src/deploy/constructorArgs.ts
  - src/deploy/warnings.ts
  - src/lib/version.ts
  - tests/deploy/ctorArgs.spec.ts
  - tests/deploy/warnings.spec.ts
files_modified_optional: []
autonomous: true
requirements: [DEPLOY-06]
tags: [deploy, types, warnings, constructor-args, wave-0, foundation]
user_setup: []

must_haves:
  truths:
    - "centralizationWarnings(meta) returns the exact warning set for any (standard, flags) combo, with per-standard body text"
    - "The constructor-arg builders produce arg lists that match the committed .sol constructor signatures for every fixture"
    - "The DeployMeta / CentralizationWarning / ConstructorArg / DeployFlags types compile and are importable by downstream deploy modules"
    - "readOwnVersion() is exported from src/lib/version.ts so the header section can read smartc's own version"
  artifacts:
    - path: "src/deploy/types.ts"
      provides: "DeployMeta, CentralizationWarning, ConstructorArg, DeployFlags, DeployChain, DeployStandard, AccessMode, WarningSeverity"
      contains: "export interface DeployMeta"
    - path: "src/deploy/warnings.ts"
      provides: "centralizationWarnings({ standard, flags }) single source of truth"
      contains: "export function centralizationWarnings"
    - path: "src/deploy/constructorArgs.ts"
      provides: "erc20ConstructorArgs / erc721ConstructorArgs / erc1155ConstructorArgs fixture-locked builders"
      contains: "export function erc20ConstructorArgs"
    - path: "tests/deploy/ctorArgs.spec.ts"
      provides: "fixture-lock test parsing each committed .sol constructor and asserting builder equality"
    - path: "tests/deploy/warnings.spec.ts"
      provides: "DEPLOY-06 warning matrix + byte-locked body text per standard"
  key_links:
    - from: "src/deploy/warnings.ts"
      to: "src/deploy/types.ts"
      via: "import type { CentralizationWarning, DeployFlags, DeployStandard }"
      pattern: "from \"./types.js\""
    - from: "src/deploy/constructorArgs.ts"
      to: "src/deploy/types.ts"
      via: "import type { ConstructorArg, DeployFlags }"
      pattern: "from \"./types.js\""
    - from: "tests/deploy/ctorArgs.spec.ts"
      to: "tests/fixtures/{erc20,erc721,erc1155}/*.sol"
      via: "readFileSync + regex parse of constructor(...) arg list"
      pattern: "constructor\\("
---

<objective>
Build the Wave 0 foundation of `src/deploy/`: the normalized type contract (`DeployMeta` and friends), the single-source centralization-warning function (D-03, with per-standard body branching), and the fixture-locked constructor-arg builders (the #1 footgun guard). Also export `readOwnVersion` from `version.ts` so the header section can read smartc's version. Everything in Waves 1–2 imports from here.

Purpose: Lock the data contract and the two load-bearing computations (warnings, constructor args) behind tests BEFORE any rendering or dispatcher wiring exists. The warning matrix is the DEPLOY-06 lock; the ctor-args fixture-lock is the strongest guard against emitting wrong deploy commands.
Output: `src/deploy/types.ts`, `src/deploy/warnings.ts`, `src/deploy/constructorArgs.ts`, an `export` added to `src/lib/version.ts`, and two spec files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-deploy-md-generation/05-CONTEXT.md
@.planning/phases/05-deploy-md-generation/05-RESEARCH.md
@.planning/phases/05-deploy-md-generation/05-PATTERNS.md

<interfaces>
<!-- Existing contracts the executor needs. Use directly — no codebase exploration. -->

src/templates/erc20/opts.ts — Erc20Opts:
  { name, symbol, premint: string, mintable, burnable, pausable, access: false | "ownable" | "roles" }
src/templates/erc721/opts.ts — Erc721Opts adds:
  enumerable, uriStorage, royalty: { enabled: boolean; feeNumerator: number; receiver: string }
src/templates/erc1155/opts.ts — Erc1155Opts:
  { name, uri, mintable, burnable, supply, pausable, access } — NO updatableUri field (hardcoded true in generate.ts)

src/lib/version.ts — `readOwnVersion(): string` exists but is NOT exported (line 53). `safeReadVersion(pkg): string|null` IS exported.

Committed fixture constructor signatures (the fixture-lock targets):
  erc20/bare-default.sol      → constructor(address recipient)                                  [premint>0, access=none]
  erc20/all-flags-on.sol      → constructor(address recipient, address defaultAdmin, address pauser, address minter)
  erc721/bare-default.sol     → constructor()                                                   [access=none]
  erc721/all-flags-on.sol     → constructor(address defaultAdmin, address pauser, address minter)
  erc721/all-flags-on-with-royalty.sol → constructor(address defaultAdmin, address pauser, address minter)
  erc1155/bare-default.sol    → constructor(address initialOwner)                               [access=none → Ownable forced]
  erc1155/all-flags-on.sol    → constructor(address defaultAdmin, address pauser, address minter)
</interfaces>

<patterns>
- Type-only module convention (all readonly fields, header doc comment): mirror src/templates/erc20/opts.ts.
- NodeNext relative imports carry `.js` extension even for `.ts` source.
- Exact DeployMeta shape is in 05-RESEARCH §DeployMeta Design (lines 114-159) — copy verbatim.
- Exact ctor-arg builders are in 05-RESEARCH §Constructor-Arg Matrix (lines 256-286) — copy verbatim.
- Warning matrix + per-standard body text: 05-RESEARCH §Centralization-Warning Matrix (lines 418-451) and 05-PATTERNS lines 116-128. The three "Mintable + Ownable" bodies DIFFER per standard (tokens / NFTs / quantities of any token id) — see byte-locked strings in 05-RESEARCH lines 420-426.
</patterns>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Type contract + fixture-locked constructor-arg builders</name>
  <files>src/deploy/types.ts, src/deploy/constructorArgs.ts, tests/deploy/ctorArgs.spec.ts</files>
  <read_first>
    - 05-RESEARCH.md §DeployMeta Design (lines 114-159) — the exact type block to copy.
    - 05-RESEARCH.md §Constructor-Arg Matrix (lines 225-288) — verified signatures table + reference builders.
    - 05-PATTERNS.md lines 132-155 — fixture-lock requirement + verified mapping.
    - tests/fixtures/{erc20,erc721,erc1155}/*.sol — the constructor lines the builders must match.
    - src/templates/erc20/opts.ts (whole file) — type-only module style to mirror.
  </read_first>
  <behavior>
    - Test (ctorArgs.spec.ts): for each committed fixture, read the .sol, regex-extract the `constructor(...)` parameter list (param NAMES in declaration order), then build the matching DeployFlags for that fixture and assert `ercXXConstructorArgs(flags).map(a => a.name)` equals the parsed names.
      - erc20/bare-default.sol → flags {premintNonZero:true, access:"none", mintable/burnable/pausable:false} → ["recipient"]
      - erc20/all-flags-on.sol → flags {premintNonZero:true, access:"roles", mintable:true, burnable:true, pausable:true} → ["recipient","defaultAdmin","pauser","minter"]
      - erc721/bare-default.sol → flags {access:"none"} → [] (no-arg ctor)
      - erc721/all-flags-on.sol → flags {access:"roles", mintable:true, pausable:true} → ["defaultAdmin","pauser","minter"]
      - erc721/all-flags-on-with-royalty.sol → same flags (+royalty:true) → ["defaultAdmin","pauser","minter"] (royalty adds NO ctor arg)
      - erc1155/bare-default.sol → flags {access:"none"} → ["initialOwner"] (updatableUri forces Ownable)
      - erc1155/all-flags-on.sol → flags {access:"roles", mintable:true, pausable:true, supply:true} → ["defaultAdmin","pauser","minter"]
    - Test: also assert every ConstructorArg has `type:"address"` and `exampleValue:"<YOUR_WALLET_ADDRESS>"` (never a real key — security T-05-01).
    - Test: erc20 zero-arg edge — `erc20ConstructorArgs({premintNonZero:false, access:"none", ...})` returns `[]`.
  </behavior>
  <action>
    Create `src/deploy/types.ts` copying the type block from 05-RESEARCH §DeployMeta Design verbatim: `DeployChain`, `DeployStandard`, `AccessMode`, `WarningSeverity` unions; `ConstructorArg` (readonly name/type/exampleValue), `DeployFlags` (readonly mintable/burnable/pausable/access plus optional enumerable/royalty/supply/updatableUri/premintNonZero), `CentralizationWarning` (readonly id/severity/title/body), and `DeployMeta` (readonly contractName/chain/standard/constructorArgs/flags/warnings). All fields `readonly`. Type-only module — no runtime code.
    Create `src/deploy/constructorArgs.ts` copying the reference builders from 05-RESEARCH §Constructor-Arg Matrix lines 256-286: `ADDR(name)` helper returning `{ name, type:"address", exampleValue:"<YOUR_WALLET_ADDRESS>" }`; `rolesArgs(flags)` = [defaultAdmin] + (pauser if pausable) + (minter if mintable); `erc20ConstructorArgs` = (recipient if premintNonZero) + (initialOwner if ownable | rolesArgs if roles); `erc721ConstructorArgs` = ownable→[initialOwner], roles→rolesArgs, else []; `erc1155ConstructorArgs` = roles→rolesArgs, else [initialOwner]. Export all three builders (the spec imports them). Import types from `./types.js`.
    Write `tests/deploy/ctorArgs.spec.ts` per the behavior block. Resolve fixture paths relative to the spec file. Use a small regex like `/constructor\(([^)]*)\)/` then split params on comma and take the last whitespace-delimited token of each as the name.
    Do NOT hard-code arg counts; always derive from the builders. Do NOT add a runtime .sol parser to production code — parsing lives ONLY in the test.
  </action>
  <acceptance_criteria>
    - `src/deploy/types.ts` exports DeployMeta, CentralizationWarning, ConstructorArg, DeployFlags, DeployChain, DeployStandard, AccessMode, WarningSeverity.
    - `src/deploy/constructorArgs.ts` exports erc20ConstructorArgs, erc721ConstructorArgs, erc1155ConstructorArgs (and rolesArgs).
    - All ConstructorArg.exampleValue values are the placeholder `<YOUR_WALLET_ADDRESS>` — no `0x`-prefixed hex literal anywhere in the file.
    - ctorArgs.spec.ts passes for all 7 fixture rows + the zero-arg edge case.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/deploy/ctorArgs.spec.ts</automated>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>Builders are fixture-locked against all committed .sol constructors; types compile; no real keys in source.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Single-source centralization-warning function + matrix test (D-03 / D-06)</name>
  <files>src/deploy/warnings.ts, tests/deploy/warnings.spec.ts</files>
  <read_first>
    - 05-RESEARCH.md §Centralization-Warning Matrix (lines 412-451) — byte-locked warning bodies + the returned matrix.
    - 05-PATTERNS.md lines 98-128 — current wizard warn literals + per-standard text-drift NOTE (single most important detail).
    - src/templates/erc20/wizard.ts lines 143-150, src/templates/erc721/wizard.ts lines 217-236, src/templates/erc1155/wizard.ts lines 146-165 — the exact current warning strings (byte-source).
  </read_first>
  <behavior>
    - Test: erc20 {mintable:true, access:"ownable"} → exactly one warning, id "mintable-ownable", severity "critical", body BYTE-IDENTICAL to the erc20 wizard literal ("...mint unlimited tokens...").
    - Test: erc721 {mintable:true, access:"ownable"} → "mintable-ownable" body says "unlimited NFTs"; {royalty:true, access:"ownable"} adds "royalty-ownable"; {pausable:true, access:"ownable"} adds "pausable-ownable". All critical, all byte-identical to the erc721 wizard literals.
    - Test: erc1155 {mintable:true, access:"ownable"} → "mintable-ownable" body says "unlimited quantities of any token id"; {pausable:true, access:"ownable"} adds "pausable-ownable"; the ALWAYS-ON "erc1155-uri-owner" warning is present on EVERY erc1155 result (even no-flags / access:none) — byte-identical to the erc1155 wizard always-on literal.
    - Test: any standard {access:"roles", mintable||pausable} → includes "roles-multi-key" with severity "info" (NEW — DEPLOY.md only). access:"roles" with no mintable/pausable → no roles-multi-key.
    - Test: any standard, no flags, access:"none" → erc20/erc721 return [], erc1155 returns just [erc1155-uri-owner].
    - Test: the critical subset (severity==="critical") for each ownable combo equals exactly today's wizard-emitted strings — this is the byte-equality lock that lets the wizard refactor (05-03) preserve visible behavior.
  </behavior>
  <action>
    Create `src/deploy/warnings.ts` exporting `centralizationWarnings({ standard, flags }: { standard: DeployStandard; flags: DeployFlags }): CentralizationWarning[]`. Branch the body text on `standard` for the mintable-ownable warning (tokens / NFTs / quantities of any token id) — copy the three exact strings from the wizard sources / 05-RESEARCH lines 420-426. Build the array in this order: mintable-ownable (if mintable && access==="ownable"), pausable-ownable (if pausable && access==="ownable"), royalty-ownable (erc721 only, if flags.royalty && access==="ownable"), erc1155-uri-owner (erc1155 only, ALWAYS), roles-multi-key (info, if access==="roles" && (mintable||pausable)). Each warning gets a stable `id`, a short `title`, the byte-locked `body`, and the right `severity`. Import types from `./types.js`.
    Write `tests/deploy/warnings.spec.ts` per the behavior block. For the byte-equality asserts, inline the expected strings as string literals copied from the wizard sources (the 05-03 wizard refactor will later import the function; this test pins the bodies regardless). Add a security assertion: no warning body contains a `0x`-prefixed 40/64-hex string.
    The roles-multi-key body is new prose — write a concise info-severity message ("This contract uses AccessControl — multiple addresses hold privileged roles (DEFAULT_ADMIN_ROLE / MINTER_ROLE / PAUSER_ROLE). Confirm each is granted to the intended address.").
  </action>
  <acceptance_criteria>
    - `centralizationWarnings` returns the exact id set per the matrix in 05-RESEARCH lines 442-451 for every tested combo.
    - mintable-ownable body differs across erc20/erc721/erc1155 and matches the respective wizard literal byte-for-byte.
    - erc1155-uri-owner is present on every erc1155 result; absent for erc20/erc721.
    - roles-multi-key has severity "info"; all others "critical".
    - warnings.spec.ts passes.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/deploy/warnings.spec.ts</automated>
  </verify>
  <done>centralizationWarnings is the single source for DEPLOY-06; the critical subset is byte-identical to current wizard output; the matrix test is green.</done>
</task>

<task type="auto">
  <name>Task 3: Export readOwnVersion from version.ts</name>
  <files>src/lib/version.ts</files>
  <read_first>
    - src/lib/version.ts lines 52-73 — the existing private `readOwnVersion()`.
    - 05-RESEARCH.md §Provenance header (lines 783-796) — why the header section needs it.
    - 05-PATTERNS.md lines 333-335.
  </read_first>
  <action>
    Change `function readOwnVersion()` (line 53) to `export function readOwnVersion()`. No other change. `formatVersionLine` (the current only consumer) is unaffected. This is additive — the header section in 05-02 imports it for the D-11 provenance line.
  </action>
  <acceptance_criteria>
    - `readOwnVersion` is exported from src/lib/version.ts.
    - `npx tsc --noEmit` clean.
    - Existing version-line tests still pass.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/commands/create.compile.spec.ts -t "version line"</automated>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>readOwnVersion is exported; no behavior change; suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| wizard opts → DeployMeta | User-supplied contract name + option flags cross into the deploy module. Already validated by Phase 2/4 validators before reaching here. |
| builders → rendered commands | Constructor-arg builders feed copy-pasteable deploy commands; a wrong arg list would mislead the user into a bad deploy. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Information Disclosure | constructorArgs.ts example values | mitigate | All `exampleValue` are the literal `<YOUR_WALLET_ADDRESS>` placeholder; ctorArgs.spec.ts asserts no `0x`-hex literal appears in source. The tool NEVER emits a real key/address. |
| T-05-02 | Tampering / Elevation | constructor-arg ordering | mitigate | Builders are fixture-locked against committed .sol constructors (ctorArgs.spec.ts); wrong arg count/order fails the test. |
| T-05-03 | Repudiation / trust | warnings.ts | mitigate | centralizationWarnings is the single source (D-03); the warning matrix test pins the disclosed risks per option combo — DEPLOY-06 becomes structural, not copy-paste-and-hope. |
| T-05-SC | Tampering | npm/pip/cargo installs | n/a | Phase 5 installs ZERO new packages (05-RESEARCH §Package Legitimacy Audit). No install task in this phase. |
</threat_model>

<verification>
- `npx vitest run tests/deploy` — both spec files green.
- `npx tsc --noEmit` — clean.
- Manual spot-check: open src/deploy/constructorArgs.ts and confirm no `0x`-prefixed literal.
</verification>

<success_criteria>
- DeployMeta type contract exists and compiles (Wave 1 can import it).
- Constructor-arg builders match every committed .sol constructor (fixture-locked).
- centralizationWarnings returns the correct, byte-locked, per-standard warning set (DEPLOY-06 lock).
- readOwnVersion exported for the header section.
</success_criteria>

<output>
Create `.planning/phases/05-deploy-md-generation/05-01-SUMMARY.md` when done.
</output>
