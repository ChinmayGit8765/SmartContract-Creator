---
phase: 05-deploy-md-generation
plan: 04
type: execute
wave: 3
depends_on: ["05-03"]
files_modified:
  - src/deploy/README.md
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
files_modified_optional: []
autonomous: true
requirements: [DEPLOY-01, DEPLOY-02, DEPLOY-03, DEPLOY-04, DEPLOY-06, DEPLOY-07, DEPLOY-08]
tags: [deploy, docs, finalization, wave-3]
user_setup: []

must_haves:
  truths:
    - "src/deploy/README.md documents the module contract: DeployMeta, the chain-keyed section registry (Phase 7 seam), the single-source warnings, and the fixture-locked ctor builders"
    - "ROADMAP.md Phase 5 shows the final plan list and 'N plans in M waves'"
    - "REQUIREMENTS.md marks DEPLOY-01/02/03/04/06/07/08 Complete and leaves DEPLOY-05 (Phase 7) pending"
  artifacts:
    - path: "src/deploy/README.md"
      provides: "module contract notes mirroring src/templates/*/README.md style"
      contains: "DeployMeta"
  key_links:
    - from: ".planning/REQUIREMENTS.md"
      to: "Phase 5 deliverables"
      via: "DEPLOY-01..08 status flip (05 excluded)"
      pattern: "DEPLOY-0"
---

<objective>
Finalize Phase 5: write the `src/deploy/README.md` module contract note, update ROADMAP.md Phase 5 (plan list + wave count + status), and flip the Phase 5 DEPLOY requirements to Complete in REQUIREMENTS.md (leaving DEPLOY-05 pending for Phase 7).

Purpose: Documentation + bookkeeping so the next phase planner sees an accurate state. This wave is a no-code-behavior change — README + planning docs only. Mirrors Phase 3/4 Wave-3 finalization plans.
Output: 1 README, 2 planning-doc edits.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-deploy-md-generation/05-CONTEXT.md
@.planning/phases/05-deploy-md-generation/05-RESEARCH.md
@.planning/phases/05-deploy-md-generation/05-01-SUMMARY.md
@.planning/phases/05-deploy-md-generation/05-02-SUMMARY.md
@.planning/phases/05-deploy-md-generation/05-03-SUMMARY.md

<interfaces>
<!-- For the README, document what 05-01..03 actually shipped (read the SUMMARYs). -->
src/deploy/ structure: types.ts, warnings.ts, constructorArgs.ts, index.ts (generateDeployDoc + deployDocPath), sections/{index,header,warnings,safety-checklist,remix,hardhat,foundry,etherscan,constructor-args}.ts
Per-template: src/templates/{erc20,erc721,erc1155}/deployMeta.ts
Key contracts: DeployMeta (normalized descriptor), centralizationWarnings (single source — D-03), chain-keyed sectionsFor (Phase 7 seam), fixture-locked ctor builders (the #1 footgun guard).
</interfaces>

<patterns>
- README style: mirror src/templates/erc721/README.md (module-contract note: what it is, the public surface, the locked decisions, the Phase 7 seam). Analog noted in 05-PATTERNS line 24.
- ROADMAP finalization: see the prior Phase entries (Phase 4 §Plans). Update the `**Plans:** TBD` → `N plans in M waves`, replace the `- [ ] 05-01: TBD` placeholder with the real plan list, and update the Progress table row + Goal if it's still a placeholder (it is not — Phase 5 Goal is already written).
- REQUIREMENTS finalization: mirror the Phase 4 update at the bottom of REQUIREMENTS.md — check the `- [ ]` boxes for the completed DEPLOY ids, flip the Traceability table Status to Complete, and update the footer note. DEPLOY-05 stays Pending (Phase 7).
</patterns>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write src/deploy/README.md module contract</name>
  <files>src/deploy/README.md</files>
  <read_first>
    - src/templates/erc721/README.md — the README style/structure to mirror.
    - 05-01-SUMMARY.md, 05-02-SUMMARY.md, 05-03-SUMMARY.md — what actually shipped (the source of truth for the README; do not invent).
    - 05-CONTEXT.md D-01..D-14 — the locked decisions to summarize.
  </read_first>
  <action>
    Write `src/deploy/README.md` documenting: (1) module purpose — turns a normalized DeployMeta into a per-template, per-option DEPLOY.md; (2) the public surface — `generateDeployDoc(meta, { now? })`, `deployDocPath(solPath)`, `centralizationWarnings({ standard, flags })`, the ctor builders, `sectionsFor(chain)`; (3) the locked design decisions worth knowing — DeployMeta is the chain-agnostic contract (D-02), warnings are single-source (D-03), the section registry is chain-keyed so Phase 7 adds solana renderers without touching EVM ones (the seam), the ctor builders are fixture-locked (the #1 footgun guard), the header date is injectable for deterministic snapshots; (4) what Phase 7 plugs in — DEPLOY-05 SPL/Anchor sections via `chain:"solana"`. Keep it concise and accurate to the SUMMARYs. Do NOT inline full code; reference the files.
  </action>
  <acceptance_criteria>
    - src/deploy/README.md exists and accurately names the public surface (generateDeployDoc, deployDocPath, centralizationWarnings, sectionsFor, the ctor builders).
    - It documents the Phase 7 solana seam and the D-03 single-source warnings.
    - No fabricated APIs — everything traces to a shipped file.
  </acceptance_criteria>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('src/deploy/README.md','utf8'); for (const t of ['generateDeployDoc','deployDocPath','centralizationWarnings','sectionsFor','DeployMeta','solana']) if(!s.includes(t)){console.error('missing '+t);process.exit(1);} console.log('README ok')"</automated>
  </verify>
  <done>Module README documents the deploy contract + Phase 7 seam, traceable to the shipped files.</done>
</task>

<task type="auto">
  <name>Task 2: Finalize ROADMAP.md + REQUIREMENTS.md for Phase 5</name>
  <files>.planning/ROADMAP.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/ROADMAP.md §Phase 5 (lines ~99-112) + Progress table (lines ~175-185).
    - .planning/REQUIREMENTS.md §DEPLOY.md Generation (lines 67-76) + Traceability rows for DEPLOY-01..08 (lines 186-193) + footer note.
  </read_first>
  <action>
    ROADMAP.md: in §Phase 5, change `**Plans:** TBD` to `**Plans:** 4 plans in 4 waves (all complete)` (Wave 0/1/2/3). Replace the `Plans:` placeholder list with:
      - [x] 05-01-PLAN.md — Wave 0: DeployMeta types + fixture-locked ctor builders + single-source centralizationWarnings + readOwnVersion export
      - [x] 05-02-PLAN.md — Wave 1: 8 section renderers + chain-keyed registry + generateDeployDoc assembler + 4 golden DEPLOY.md fixtures
      - [x] 05-03-PLAN.md — Wave 2: deployMeta? interface + 3 per-template mappings + D-03 wizard refactor + confirmOverwriteMany + create.ts wiring + E2E
      - [x] 05-04-PLAN.md — Wave 3: src/deploy/README.md + ROADMAP/REQUIREMENTS finalization
    Update the top phase list line for Phase 5 to `- [x]` and the Progress table row `5. DEPLOY.md Generation | 4/4 | Complete | 2026-05-29`.
    REQUIREMENTS.md: check the `- [x]` boxes for DEPLOY-01, DEPLOY-02, DEPLOY-03, DEPLOY-04, DEPLOY-06, DEPLOY-07, DEPLOY-08 (leave DEPLOY-05 as `- [ ]`). In the Traceability table flip those seven rows' Status to `Complete` (DEPLOY-05 stays Pending, Phase 7). Update the footer `*Last updated:*` note to record Phase 5 completion + the DEPLOY-05/Phase-7 carve-out.
    Do NOT mark DEPLOY-05 complete and do NOT touch any non-DEPLOY requirement.
  </action>
  <acceptance_criteria>
    - ROADMAP Phase 5 shows 4 plans in 4 waves, the real plan list, `- [x]` status, and the Progress row Complete.
    - REQUIREMENTS DEPLOY-01/02/03/04/06/07/08 are checked + Traceability Complete; DEPLOY-05 remains Pending.
    - No other requirement rows changed.
  </acceptance_criteria>
  <verify>
    <automated>node -e "const r=require('fs').readFileSync('.planning/REQUIREMENTS.md','utf8'); const lines=r.split('\n'); const ok=['DEPLOY-01','DEPLOY-02','DEPLOY-03','DEPLOY-04','DEPLOY-06','DEPLOY-07','DEPLOY-08'].every(id=>lines.some(l=>l.includes(id)&&l.includes('[x]'))); const d5=lines.some(l=>l.includes('DEPLOY-05')&&l.includes('[ ]')); if(!ok){console.error('a DEPLOY id not checked');process.exit(1);} if(!d5){console.error('DEPLOY-05 should stay unchecked');process.exit(1);} console.log('requirements ok')"</automated>
  </verify>
  <done>ROADMAP + REQUIREMENTS reflect Phase 5 complete with DEPLOY-05 correctly deferred to Phase 7.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Docs + planning bookkeeping only — no runtime code, no new input surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-08 | Repudiation (inaccurate status) | ROADMAP/REQUIREMENTS edits | mitigate | Verify commands assert DEPLOY-05 stays unchecked and the seven Phase-5 ids are marked complete; no silent over-marking. |
| T-05-SC | Tampering | npm/pip/cargo installs | n/a | No package installs in this phase. |
</threat_model>

<verification>
- `node -e` README + REQUIREMENTS asserts pass (in the task verify blocks).
- `npx vitest run` still green (no code changed, but confirm nothing regressed).
- Manual: skim ROADMAP §Phase 5 and the Progress table for accuracy.
</verification>

<success_criteria>
- src/deploy/README.md documents the module contract + Phase 7 seam.
- ROADMAP Phase 5 finalized (4 plans / 4 waves / complete).
- REQUIREMENTS DEPLOY-01/02/03/04/06/07/08 Complete; DEPLOY-05 deferred to Phase 7.
</success_criteria>

<output>
Create `.planning/phases/05-deploy-md-generation/05-04-SUMMARY.md` when done.
</output>
