---
phase: 09-cross-platform-distribution
plan: 01
type: execute
wave: 0
depends_on: ["01", "02", "03", "04", "05", "06", "07", "08"]
files_modified:
  - package.json
  - LICENSE
  - README.md
  - vitest.config.ts
  - .github/workflows/ci.yml
  - docs/HOWTO.md
  - docs/FORKING.md
  - docs/EMBEDDING.md
  - AGENTS.md
  - llms.txt
autonomous: true
requirements: [CLI-01, DIST-01, DIST-02, DIST-03]
tags: [distribution, packaging, ci, docs, public-repo]
user_setup: ["Set NPM_TOKEN + run `npm publish` to release (manual, gated on green CI)"]

must_haves:
  truths:
    - "The package installs globally (npm install -g) and runs `smartc --help` on Windows/macOS/Linux"
    - "CI runs typecheck+test+build AND a global-install smoke on all three OSes for every push/PR (gate before publish)"
    - "README has install instructions, a quickstart, a license, and example output for each of the 4 templates"
    - "A how-to guide, a forkability guide, and an embedding guide (humans + LLMs + MCP agents) exist"
    - "Machine-discoverable AGENTS.md + llms.txt describe the tool's capability + invocation contract"
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "3-OS x Node 20/22 matrix + global-install smoke"
      contains: "npm install -g"
    - path: "LICENSE"
      provides: "MIT license"
      contains: "MIT License"
    - path: "AGENTS.md"
      provides: "agent-readable capability + invocation contract"
      contains: "smartc"
---

<objective>
Finalize the project for public release: npm packaging metadata + MIT license, a
comprehensive README (install/quickstart/4 template examples/license), a CI matrix
that proves global install works on Windows/macOS/Linux, and the public-repo docs
the owner asked for — a how-to guide, a forkability guide, and embedding guides
for people, LLMs, and MCP agents (plus machine-discoverable AGENTS.md + llms.txt).

Purpose: make the repo usable, forkable, and embeddable by humans and agents, and
make `npm install -g smartc` a verified, cross-platform path.
</objective>

<design>
- package.json: license MIT, author, repository/homepage/bugs, keywords, files
  (dist+README+LICENSE), prepublishOnly (typecheck+test+build). Version stays
  0.1.0 (the deploy golden fixtures assert "smartc 0.1.0").
- LICENSE: MIT.
- README.md: rewrite from the Phase-1 stub into the public front door — what/why,
  install (global + npx + from-source), quickstart, command table, all 4 templates
  with example snippets (DIST-03), DEPLOY.md + doctor + AI sections, safety, doc
  links, contributing/forking, license.
- vitest.config.ts: raise testTimeout (solc E2E specs flaked at the 5s default,
  worse on slower CI runners) — CI hardening.
- .github/workflows/ci.yml: matrix {ubuntu,windows,macos} x node {20,22}; npm ci,
  typecheck, test, build, then a bash-uniform global-install smoke (npm pack ->
  npm i -g -> smartc --version/--help/list-templates/doctor) for DIST-01.
- docs/: HOWTO.md (usage), FORKING.md (layout + additive plugin model + add a
  template/chain + invariants), EMBEDDING.md (scripts/LLM/MCP, stable JSON +
  exit-code contracts, MCP server sketch, honest note that `create` is interactive).
- AGENTS.md + llms.txt: machine-discoverable capability + invocation summary.
- Bookkeeping: also fix the pre-existing ERC20-01..05 + COMP-01 requirement marks
  (shipped in Phases 2-3 but never flipped) so the public milestone reads true.
</design>

<verification>
- tsc clean; full suite green (374 passed, 1 skipped) with the new timeout.
- `npm pack` ships dist + README + LICENSE; isolated tarball install runs the bin
  (verified locally on Windows); CI validates the global path on 3 OSes.
- All v1 requirements Complete.
</verification>

<output>
Create `.planning/phases/09-cross-platform-distribution/09-01-SUMMARY.md` when done.
</output>
